const mobileDevice=matchMedia('(pointer:coarse)').matches||'ontouchstart'in window;const $=id=>document.getElementById(id),canvas=$('gameCanvas');let ctx=canvas.getContext('2d'),game=$('game');
const store={get(key,fallback='0'){try{return localStorage.getItem(key)??fallback}catch{return fallback}},set(key,value){try{localStorage.setItem(key,value)}catch{}}};
const music=$('gameMusic');const introMusic=$('introMusic');music.volume=.42;if(introMusic)introMusic.volume=.34;const MUSIC_TRACKS={copperhead:{src:'assets/audio/copperhead-stomp.mp3',label:'THE COPPERHEAD STOMP'},highway:{src:'assets/audio/highway-fever.mp3',label:'HIGHWAY FEVER'},roadhouse:{src:'assets/audio/roadhouse-rhythm.mp3',label:'THE ROADHOUSE RHYTHM'},roadkill:{src:'assets/audio/copperhead-roadkill.mp3',label:'COPPERHEAD ROADKILL'},tiroler:{src:'assets/audio/tiroler-polka.mp3',label:'TIROLER POLKA'},ecstasy:{src:'assets/audio/ecstasy-of-gold-intro.mp3',label:'THE ECSTASY OF GOLD'}};let activeMusicId=null;
const ui={score:$('score'),speed:$('speed'),surface:$('surface'),distance:$('distance'),lives:$('lives'),hud:$('hud'),title:$('titleScreen'),routes:$('routeScreen'),learn:$('learnScreen'),countdown:$('countdown'),countdownValue:$('countdownValue'),routeName:$('routeName'),pause:$('pauseScreen'),results:$('resultsScreen'),toast:$('toast'),combo:$('combo'),comboValue:$('comboValue'),comboBar:$('comboBar'),scoreDelta:$('scoreDelta'),objectiveText:$('objectiveText'),objectiveProgress:$('objectiveProgress'),trick:$('trickReadout'),trickName:$('trickName')};
const ROUTES=[
 {name:'ECHO MOUNTAIN',place:'IDAHO SPRINGS',elevation:10650,unlock:0,sky:['#15244d','#e17b72'],snow:['#dce7f4','#94a9c5'],peak:'#34496c',tree:'#1d3048',weather:'night',hazards:['snowboarder','speedPatrol','shortcut','patch','ramp','gate','skiPatrol','tree']},
 {name:'ELDORA',place:'NEDERLAND',elevation:10600,unlock:0,sky:['#244f59','#90b7ab'],snow:['#e8f3ec','#b9d2c7'],peak:'#416b67',tree:'#173f39',weather:'mist',hazards:['tree','snowboarder','gate','rock','ramp','tree']},
 {name:'LOVELAND',place:'CONTINENTAL DIVIDE',elevation:13010,unlock:0,sky:['#3d80a4','#d3ebf0'],snow:['#f4fbfd','#c8e2eb'],peak:'#6d96aa',tree:'#315b65',weather:'powder',hazards:['rock','shortcut','patch','snowboarder','gate','ramp','rock']},
 {name:'ARAPAHOE BASIN',place:'THE LEGEND',elevation:13050,unlock:0,sky:['#101d49','#69527e'],snow:['#cbd7ea','#8fa5c7'],peak:'#29365f',tree:'#172544',weather:'storm',hazards:['rock','tree','snowboarder','gate','ramp','rock']},
 {name:'STEAMBOAT',place:'STEAMBOAT SPRINGS',elevation:10568,unlock:0,sky:['#315f66','#a6c8bc'],snow:['#eef7f2','#c2ddd1'],peak:'#4a736d',tree:'#173d36',weather:'powder',hazards:['tree','skiPatrol','snowboarder','speedPatrol','gate','rock']},
 {name:'HAUSBERG',place:'GARMISCH-PARTENKIRCHEN',elevation:1338,elevationUnit:'M',unlock:0,sky:['#294d63','#e7a36d'],snow:['#f1f7f8','#b9d3db'],peak:'#557989',tree:'#244b45',weather:'groomed',hazards:['tree','snowboarder','gate','ramp','rock']},
 {name:'ZUGSPITZE',place:'GERMANY’S HIGHEST',elevation:2962,elevationUnit:'M',unlock:0,sky:['#173452','#b7d4e3'],snow:['#f5fbff','#b4d0df'],peak:'#7897a8',tree:'#34515d',weather:'storm',hazards:['rock','rock','gate','snowboarder','ramp']},
 {name:'ALPSPITZE',place:'WETTERSTEIN ALPS',elevation:2628,elevationUnit:'M',unlock:0,sky:['#402d4d','#e7a06f'],snow:['#f8fbfc','#c1d6df'],peak:'#687a8d',tree:'#263f42',weather:'powder',hazards:['rock','tree','snowboarder','gate','ramp']},
 {name:'HOEDOWN HILL',place:'WINDSOR · COLORADO',elevation:4904,elevationUnit:'FT',vertical:130,unlock:0,sky:['#2b6e9b','#e6b777'],snow:['#f7fbfc','#c5dce4'],peak:'#6c91a1',tree:'#355b4b',weather:'groomed',hazards:['ramp','speedPatrol','shortcut','patch','snowboarder','gate','skiPatrol','rail']},
 {name:'PURGATORY',place:'DURANGO · COLORADO',elevation:10822,unlock:0,sky:['#234c68','#e8ad78'],snow:['#f5fbfc','#bfd9e1'],peak:'#536f7e',tree:'#285343',weather:'powder',hazards:['tree','rock','snowboarder','speedPatrol','shortcut','gate','patch','skiPatrol']}
];
const SNOW_REPORT_COORDS=[[39.684,-105.64],[39.937,-105.583],[39.68,-105.897],[39.642,-105.871],[40.457,-106.805],[47.477,11.09],[47.421,10.985],[47.455,11.118],[40.424,-104.91],[37.63,-107.81]];
function updateTrailStatus(index=routeIndex,report=liveSnowReport){const card=$('trailStatus');if(!card)return;card.classList.remove('status-limited','status-closed');const month=new Date().getMonth();if(!(month>=9||month<=3)&&(!report||report.routeIndex!==index)){$('trailStatusTitle').textContent='OFF SEASON';$('trailStatusAdvice').textContent='Lifts are parked · come back for winter conditions';$('liftStatus').textContent='CLOSED';$('openRuns').textContent='0 / 0';return}if(!report||report.routeIndex!==index){$('trailStatusTitle').textContent='ALL LIFTS OPEN';$('trailStatusAdvice').textContent='Local game status · conditions still loading';$('liftStatus').textContent='OPEN';$('openRuns').textContent=`${RUNS[index].length} / ${RUNS[index].length}`;return}const severe=report.wind>=75,limited=!severe&&(report.wind>=45||report.fresh>=12),status=severe?'CLOSED':limited?'LIMITED':'OPEN',total=RUNS[index].length,open=severe?0:limited?Math.max(1,Math.ceil(total*.6)):total;card.classList.toggle(`status-${status.toLowerCase()}`,status!=='OPEN');$('trailStatusTitle').textContent=`${status} · ${ROUTES[index].name}`;$('trailStatusAdvice').textContent=severe?'Wind hold · patrol is clearing the ridge':limited?'Limited terrain · expect wind holds and fresh hazard gates':'All planned lines available · choose your fall line';$('liftStatus').textContent=severe?'HOLD':status;$('openRuns').textContent=`${open} / ${total}`}
const RUNS=[
 [{name:'Hollywood',length:980,difficulty:'BLUE',terrain:'park',line:[[0,.46,.29],[.18,.40,.25],[.42,.55,.3],[.7,.48,.34],[1,.58,.42]]},{name:'Gold Digger',length:760,difficulty:'BLUE',terrain:'groomer',line:[[0,.62,.25],[.28,.54,.22],[.55,.42,.24],[.78,.5,.3],[1,.58,.42]]},{name:'Full Send',length:690,difficulty:'BLUE',terrain:'park',line:[[0,.4,.2],[.3,.47,.23],[.64,.57,.25],[1,.58,.4]]},{name:'The Glades',length:620,difficulty:'BLACK',terrain:'glades',line:[[0,.67,.16],[.22,.59,.15],[.48,.68,.14],[.72,.54,.16],[1,.58,.35]]}],
 [{name:'Corona',length:1650,difficulty:'BLACK',terrain:'glades',line:[[0,.68,.17],[.2,.6,.15],[.43,.49,.16],[.66,.39,.18],[.84,.5,.23],[1,.56,.38]]},{name:'Muleshoe',length:1420,difficulty:'BLACK',terrain:'moguls',line:[[0,.35,.18],[.2,.46,.17],[.43,.38,.16],[.67,.54,.18],[.84,.47,.24],[1,.56,.38]]},{name:'International',length:1180,difficulty:'BLACK',terrain:'steep',line:[[0,.53,.18],[.32,.49,.17],[.58,.57,.18],[.8,.51,.24],[1,.56,.39]]}],
 [{name:"Cat's Meow",length:1806,difficulty:'BLACK',terrain:'groomer',line:[[0,.27,.19],[.23,.36,.19],[.5,.46,.2],[.72,.57,.22],[1,.55,.4]]},{name:'The Plunge',length:1370,difficulty:'BLACK',terrain:'steep',line:[[0,.72,.15],[.24,.64,.14],[.47,.52,.16],[.7,.39,.18],[1,.55,.4]]},{name:'Fire Bowl',length:2447,difficulty:'BLUE',terrain:'bowl',line:[[0,.66,.43],[.25,.57,.46],[.54,.43,.44],[.77,.5,.35],[1,.55,.4]]}],
 [{name:'High Noon',length:1760,difficulty:'BLUE',terrain:'groomer',line:[[0,.52,.3],[.28,.48,.29],[.56,.56,.3],[.78,.48,.34],[1,.52,.43]]},{name:'The Spine',length:980,difficulty:'DOUBLE BLACK',terrain:'spine',line:[[0,.3,.1],[.2,.39,.09],[.43,.47,.08],[.68,.4,.1],[.84,.49,.18],[1,.52,.4]]},{name:'Lenawee Face',length:1460,difficulty:'BLACK',terrain:'alpine',line:[[0,.69,.31],[.27,.61,.29],[.52,.51,.27],[.76,.43,.29],[1,.52,.42]]}],
 [{name:'Heavenly Daze',length:2050,difficulty:'BLUE',terrain:'groomer',line:[[0,.62,.27],[.18,.55,.25],[.4,.45,.24],[.62,.36,.24],[.82,.47,.3],[1,.53,.42]]},{name:'Vagabond',length:1880,difficulty:'BLUE',terrain:'glades',line:[[0,.34,.2],[.2,.43,.18],[.43,.35,.18],[.66,.49,.2],[.84,.44,.26],[1,.53,.42]]},{name:"Buddy's Run",length:1730,difficulty:'BLUE',terrain:'aspen',line:[[0,.68,.2],[.2,.6,.19],[.42,.66,.18],[.65,.55,.2],[.82,.48,.27],[1,.53,.42]]}]
 ,[{name:'Hausberg Classic',length:1480,difficulty:'BLUE',terrain:'groomer',line:[[0,.58,.28],[.2,.5,.25],[.42,.58,.27],[.65,.45,.3],[.82,.52,.33],[1,.55,.42]]},{name:'Kandahar',length:1890,difficulty:'BLACK',terrain:'steep',line:[[0,.7,.2],[.25,.61,.18],[.48,.48,.19],[.72,.43,.21],[1,.54,.4]]},{name:'Horn Abfahrt',length:1220,difficulty:'BLUE',terrain:'park',line:[[0,.38,.22],[.28,.45,.2],[.56,.38,.23],[.78,.5,.28],[1,.55,.4]]}],
 [{name:'Glacier Run',length:2240,difficulty:'BLACK',terrain:'alpine',line:[[0,.62,.24],[.2,.54,.21],[.42,.43,.2],[.64,.5,.22],[.82,.4,.27],[1,.53,.42]]},{name:'Wetterwand',length:1760,difficulty:'DOUBLE BLACK',terrain:'spine',line:[[0,.32,.13],[.22,.4,.11],[.46,.34,.1],[.68,.48,.12],[1,.54,.4]]},{name:'Platt Traverse',length:2580,difficulty:'BLUE',terrain:'bowl',line:[[0,.7,.4],[.25,.59,.42],[.5,.45,.4],[.76,.51,.37],[1,.54,.42]]}],
 [{name:'Alpspitz Face',length:1980,difficulty:'BLACK',terrain:'steep',line:[[0,.68,.2],[.22,.57,.18],[.46,.46,.17],[.7,.52,.2],[1,.55,.42]]},{name:'Osterfelder',length:1640,difficulty:'BLUE',terrain:'groomer',line:[[0,.34,.25],[.2,.4,.23],[.43,.5,.25],[.7,.43,.29],[1,.54,.42]]},{name:'Nordwand Chute',length:1080,difficulty:'DOUBLE BLACK',terrain:'spine',line:[[0,.73,.12],[.25,.62,.1],[.5,.49,.1],[.76,.43,.13],[1,.54,.4]]}],
 [{name:'Main Street',length:520,difficulty:'BLUE',terrain:'groomer',line:[[0,.54,.34],[.24,.47,.33],[.5,.55,.36],[.74,.49,.38],[1,.54,.43]]},{name:'Clunk Yard',length:430,difficulty:'PARK',terrain:'park',line:[[0,.38,.28],[.2,.48,.26],[.4,.4,.27],[.62,.55,.3],[.82,.45,.34],[1,.54,.42]]},{name:'Hoedown Slalom',length:680,difficulty:'BLACK',terrain:'park',line:[[0,.68,.25],[.2,.58,.22],[.4,.47,.23],[.62,.56,.25],[.8,.46,.3],[1,.54,.42]]}],
 [{name:'Purgatory Plunge',length:2140,difficulty:'BLACK',terrain:'steep',line:[[0,.7,.23],[.2,.61,.2],[.4,.5,.19],[.62,.43,.22],[.8,.5,.29],[1,.55,.42]]},{name:'Paradise Bowl',length:2390,difficulty:'BLUE',terrain:'bowl',line:[[0,.33,.4],[.22,.42,.42],[.46,.55,.4],[.7,.48,.38],[1,.55,.43]]},{name:'Dante’s Run',length:1280,difficulty:'DOUBLE BLACK',terrain:'glades',line:[[0,.72,.15],[.24,.6,.14],[.48,.47,.13],[.7,.54,.16],[1,.55,.42]]}]
];
const SKINS=[{a:'#ff9343',b:'#dd3e27'},{a:'#69e1f5',b:'#1686aa'},{a:'#ffe071',b:'#d78c1e'}];
const HELMETS=['#101d2b','#e8f4f7','#db4c38'];
const GOGGLES=['#70dff1','#ffd260','#ff8d9b'];const SUIT_STYLES=['clean','stripe','split'];
const SETUPS={racer:{name:'RACE / EDGE GRIP',description:'Fastest on groomers · sharpest carving · less float.',speed:.075,grip:1.12,powder:.88,pop:0},powder:{name:'POWDER / FLOAT',description:'Best soft-snow float · stable landings · slower hardpack.',speed:.01,grip:.92,powder:1.18,pop:12},freestyle:{name:'FREESTYLE / POP',description:'Highest jump pop · trick control · softer edge bite.',speed:-.03,grip:.98,powder:.96,pop:105}};
const resortImages=['echo-mountain','eldora','loveland','arapahoe-basin','steamboat','hausberg','zugspitze','alpspitze','hoedown-hill','purgatory'].map(name=>{const image=new Image();image.src=`assets/resorts/${name}.webp`;return image});
let W=0,H=0,dpr=1,last=0,state='title',routeIndex=0,runIndex=0,courseLength=RUNS[0][0].length,skinIndex=+store.get('alpineRushSkin'),helmetIndex=+store.get('alpineRushHelmet'),goggleIndex=+store.get('alpineRushGoggles'),styleIndex=+store.get('alpineRushStyle'),riderType=store.get('alpineRushRider','skier'),best=+store.get('alpineRushBest'),sound=store.get('alpineRushSound','on')!=='off',finishTimer=null,runToken=0,snowReportController=null,selectionRefreshTimer=null;
let score=0,lives=3,speed=1,distance=0,spawnTimer=0,sceneryOffset=0,shake=0,slowmo=0,cameraX=0,cameraY=0,cameraRoll=0,combo=1,comboTime=0,bestCombo=1,clears=0,gatesCleared=0,gatesMissed=0,nearMisses=0,jumps=0,trickCount=0,boostPickups=0,riskLines=0,patrolClears=0,stashFinds=0,rescueFinds=0,gearWear=Number(store.get('alpineRushGearWear','0'))||0,rivalShowdown=null,rivalSeries=null,trickBattleTarget=3,bestTrick='—',bestTrickPoints=0,objectiveDone=false,courseComplete=false,spawnCount=0;
const keys={left:false,right:false,spinL:false,spinR:false,frontFlip:false,backFlip:false,grab:false},player={x:.5,y:.78,vx:0,jump:0,jumpV:0,tilt:0,edge:0,compression:0,anim:0,invincible:0,crash:0,spin:0,spinV:0,flip:0,flipV:0,grabTime:0,trickActive:false};
let obstacles=[],particles=[],flakes=[],trails=[],popups=[],liveSnowReport=null,trailAccessState='open';
const challengeTypes=['clear','trick','near','boost','rescue','rival','line','stash'],challengeType=challengeTypes[new Date().getDate()%challengeTypes.length],dailyTarget=challengeType==='trick'?3:challengeType==='near'?4:challengeType==='boost'?3:['rescue','rival','line','stash'].includes(challengeType)?2:5+(new Date().getDate()%3),dailyLabel={clear:`Clear ${dailyTarget} obstacles`,trick:`Land ${dailyTarget} tricks`,near:`Near-miss ${dailyTarget} hazards`,boost:`Collect ${dailyTarget} boost pickups`,rescue:`Complete ${dailyTarget} rescues`,rival:`Overtake ${dailyTarget} rivals`,line:`Take ${dailyTarget} alternate lines`,stash:`Find ${dailyTarget} secret stashes`}[challengeType];
const cupDate=new Date().toISOString().slice(0,10),cupDay=Math.floor(Date.parse(`${cupDate}T00:00:00Z`)/86400000),cupRouteIndex=cupDay%ROUTES.length,cupRunIndex=cupDay%RUNS[cupRouteIndex].length,cupKey=`${cupDate}:${cupRouteIndex}:${cupRunIndex}`;
function challengeProgress(){return challengeType==='trick'?trickCount:challengeType==='near'?nearMisses:challengeType==='boost'?boostPickups:challengeType==='rescue'?rescueFinds:challengeType==='rival'?rivalPasses:challengeType==='line'?lineChoices:challengeType==='stash'?stashFinds:clears}
function recordChallenge(type){if(type!==challengeType||objectiveDone)return;if(challengeProgress()>=dailyTarget){objectiveDone=true;scorePop(1000,.5*W,.3*H,'CHALLENGE COMPLETE');toast('CHALLENGE COMPLETE','+1,000');beep(1040,.22)}}
function paintChallenge(){const name=$('challengeName'),progress=$('challengeProgress'),fill=$('challengeFill'),reward=$('challengeReward');if(!name)return;const value=Math.min(challengeProgress(),dailyTarget);name.textContent=dailyLabel.toUpperCase();if(progress)progress.textContent=`${value} / ${dailyTarget}`;if(fill)fill.style.width=`${Math.min(100,value/dailyTarget*100)}%`;if(reward)reward.textContent=objectiveDone?'COMPLETED · +1,000 PTS':'+1,000 PTS'}
function snowSurface(){const override=window.AlpinePro?.settings?.condition||'auto',run=RUNS[routeIndex][runIndex],progress=Math.max(0,Math.min(1,distance/courseLength));if(override==='auto'&&liveSnowReport?.routeIndex===routeIndex){if(liveSnowReport.fresh>=3)return'POWDER';if(liveSnowReport.temp>=3)return'SPRING SLUSH';if(liveSnowReport.temp<=-8)return'PACKED ICE'}const weather=override==='auto'?ROUTES[routeIndex].weather:override;if(weather==='storm'){if(progress>.7)return'PACKED ICE';if(progress>.34)return'WHITEOUT';return'POWDER'}if(weather==='night')return'NIGHT GROOM';if(weather==='ice')return'PACKED ICE';if(weather==='powder')return'POWDER';if(weather==='groomed')return'GROOMED';if(run.terrain==='moguls')return'MOGULS';if(run.terrain==='glades'||run.terrain==='aspen')return progress>.58?'TRACKED POWDER':'POWDER';if(run.terrain==='steep'||run.terrain==='spine'||run.terrain==='alpine')return progress>.62?'PACKED ICE':'HARDPACK';if(run.terrain==='groomer')return progress>.72?'SPRING SLUSH':'GROOMED';return'GROOMED'}

function resize(){const r=canvas.getBoundingClientRect();dpr=mobileDevice?Math.min(devicePixelRatio||1,1.25):Math.min(devicePixelRatio||1,2);canvas.width=Math.round(r.width*dpr);canvas.height=Math.round(r.height*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);W=r.width;H=r.height}
function roundedPath(x,y,w,h,r){r=Math.min(r,w/2,h/2);ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h-r);ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);ctx.lineTo(x+r,y+h);ctx.quadraticCurveTo(x,y+h,x,y+h-r);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath()}
addEventListener('resize',resize);resize();$('titleBest').textContent=String(best).padStart(6,'0');ui.objectiveText.textContent=dailyLabel;

function show(el){el.classList.remove('hidden')}function hide(el){el.classList.add('hidden')}
function chooseMusic(){const preference=window.AlpinePro?.settings?.music||'random',germanRoute=routeIndex>=5&&routeIndex<=7,ids=germanRoute?['tiroler']:['copperhead','highway','roadhouse','roadkill'],id=preference==='random'?ids[Math.floor(Math.random()*ids.length)]:MUSIC_TRACKS[preference]?preference:'copperhead';if(id===activeMusicId&&music.src)return;activeMusicId=id;music.src=MUSIC_TRACKS[id].src;music.setAttribute('aria-label',MUSIC_TRACKS[id].label);music.load()}
function setIntroMusic(active){if(!introMusic)return;introMusic.muted=!sound;if(active&&sound){introMusic.play().catch(()=>{});return}introMusic.pause()}
function setMusic(active){if(!music)return;setIntroMusic(false);music.muted=!sound;if(active&&sound){chooseMusic();music.play().catch(()=>{});return}if(!active)music.pause()}
function enterRoutes(){startSelectionRefresh();setIntroMusic(false);state='select';hide(ui.title);hide(ui.results);hide(ui.pause);hide(ui.countdown);hide(ui.learn);show(ui.routes);game.classList.remove('playing');renderUnlocks();populateRuns();updateTrailStatus(routeIndex);syncTrailAccess();updateMountainEvent();loadSnowReport(routeIndex);if(!$('basicsButton')){const button=document.createElement('button');button.id='basicsButton';button.type='button';button.className='text-button basics-button';button.textContent='SKI & SNOWBOARD TUTORIAL';button.onclick=()=>{hide(ui.routes);show(ui.learn)};ui.routes.querySelector('.menu-heading')?.append(button)} }
function populateRuns(){const select=$('runSelect');select.innerHTML='';RUNS[routeIndex].forEach((run,i)=>{const option=document.createElement('option');option.value=i;option.textContent=`${run.name} · ${run.difficulty} · ${run.length.toLocaleString()} M`;select.appendChild(option)});runIndex=0;select.value='0'}
function syncTrailAccess(){trailAccessState=liveSnowReport?.routeIndex===routeIndex&&liveSnowReport.wind>=75?'closed':'open';const button=$('startButton');if(!button)return;const closed=trailAccessState==='closed';button.disabled=closed;button.classList.toggle('trail-closed',closed);const label=button.querySelector('b');if(label)label.textContent=closed?'PATROL HOLD':'DROP IN'}
function updateMountainEvent(){const select=$('runSelect'),title=$('mountainEventTitle'),details=$('mountainEventDetails');if(!select||!title||!details)return;const report=liveSnowReport?.routeIndex===routeIndex?liveSnowReport:null,limited=!!report&&(report.wind>=45||report.fresh>=12),powder=!!report&&report.fresh>=3;if(!report){title.textContent='LOCAL LINEUP';details.textContent='All runs available · choose your line and make your own conditions';}else if(report.wind>=75){title.textContent='WIND HOLD';details.textContent='Lift access paused · patrol will reopen the ridge when the wind drops';}else if(powder){title.textContent=limited?'POWDER WINDOW · LIMITED ACCESS':'POWDER WINDOW';details.textContent=limited?'Fresh snow is open on the mellow line · technical run held for patrol':'Fresh tracks found · the powder line is the fast way down';}else if(report.temp>=3){title.textContent='SPRING SLUSH SHIFT';details.textContent='Lower mountain is soft · the steep line stays the cleanest';}else{title.textContent='CLEAR MOUNTAIN DAY';details.textContent='Full lineup available · chase a clean run or a risky shortcut'};const closedIndex=limited?RUNS[routeIndex].length-1:-1;[...select.options].forEach((option,i)=>{const run=RUNS[routeIndex][i];option.dataset.base=`${run.name} · ${run.difficulty} · ${run.length.toLocaleString()} M`;option.disabled=i===closedIndex;option.textContent=i===closedIndex?`${option.dataset.base} · PATROL HOLD`:powder&&i===0?`${option.dataset.base} · POWDER LINE`:option.dataset.base});if(select.selectedOptions[0]?.disabled){runIndex=0;select.value='0';updatePreview()}}
let snowReportRequest=0;
async function loadSnowReport(index=routeIndex){const card=$('snowReport');if(!card)return;const month=new Date().getMonth();if(!(month>=9||month<=3)){card.hidden=true;liveSnowReport=null;return}card.hidden=false;$('snowReportTitle').textContent=ROUTES[index].name;$('snowReportStatus').textContent='Checking current conditions…';$('snowReportAdvice').textContent='Checking recommended setup…';$('snowDepth').textContent='—';$('snowNew').textContent='—';$('snowTemp').textContent='—';const request=++snowReportRequest,key=`alpineRushSnowReport:${index}:${new Date().toISOString().slice(0,10)}`;let cached=null;try{cached=JSON.parse(localStorage.getItem(key)||'null')}catch{};const render=data=>{if(request!==snowReportRequest)return;const h=data.hourly||{},depth=Math.max(0,Number(h.snow_depth?.[0]||0)),fresh=(h.snowfall||[]).slice(0,24).reduce((sum,value)=>sum+Number(value||0),0),temp=Number(data.current?.temperature_2m??h.temperature_2m?.[0]??0),wind=Number(data.current?.wind_speed_10m||0),imperial=AlpinePro.settings.units==='imperial',code=Number(data.current?.weather_code??0),labels={0:'Clear mountain air',1:'Mostly clear',2:'Partly cloudy',3:'Overcast',45:'Misty ridge',51:'Light drizzle',61:'Rain showers',71:'Light snow',73:'Snow showers',75:'Heavy snow',77:'Snow grains',80:'Rain showers',85:'Snow showers',86:'Heavy snow showers',95:'Storm risk'};liveSnowReport={routeIndex:index,depth:depth*100,fresh,temp,wind,fetchedAt:data.fetchedAt||Date.now()};const advice=fresh>=3?'RECOMMENDED · POWDER / FLOAT':temp<=-8?'RECOMMENDED · RACE / EDGE GRIP':wind>=30?'RECOMMENDED · FREESTYLE / STABLE LANDING':'RECOMMENDED · RACE / EDGE GRIP';$('snowReportDate').textContent=`UPDATED ${new Date(data.fetchedAt||Date.now()).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}`;$('snowReportStatus').textContent=labels[code]||'Mountain weather update';$('snowReportAdvice').textContent=advice;$('snowDepth').textContent=imperial?`${(depth*39.37).toFixed(0)} in`:`${(depth*100).toFixed(0)} cm`;$('snowNew').textContent=imperial?`${(fresh*.3937).toFixed(1)} in`:`${fresh.toFixed(1)} cm`;$('snowTemp').textContent=imperial?`${(temp*9/5+32).toFixed(0)}°F`:`${temp.toFixed(0)}°C`};if(cached&&Date.now()-cached.fetchedAt<30*60*1000){render(cached);return}const [latitude,longitude]=SNOW_REPORT_COORDS[index]||SNOW_REPORT_COORDS[0],url=`https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,weather_code,wind_speed_10m&hourly=temperature_2m,snowfall,snow_depth&forecast_days=2&timezone=auto`;try{const response=await fetch(url,{headers:{accept:'application/json'},signal:AbortSignal.timeout?.(7000)});if(!response.ok)throw new Error('weather unavailable');const data=await response.json();data.fetchedAt=Date.now();try{localStorage.setItem(key,JSON.stringify(data))}catch{}render(data)}catch{if(cached)render(cached);else if(request===snowReportRequest)$('snowReportStatus').textContent='Snow report unavailable · ride the local forecast'}}
function formatElevation(route){if(!route)return'';const unit=route.elevationUnit||'FT';return`${route.elevation.toLocaleString()} ${unit}${route.vertical?` · ${route.vertical} FT VERTICAL`:''}`}
function renderUnlocks(){document.querySelectorAll('.route-card').forEach((card,i)=>{const locked=best<ROUTES[i].unlock;card.classList.toggle('locked',locked);card.setAttribute('aria-disabled',String(locked));card.querySelector('b').textContent=locked?`BEST ${ROUTES[i].unlock.toLocaleString()}`:formatElevation(ROUTES[i])});document.querySelectorAll('.swatches button').forEach((b,i)=>{const locked=best<[0,3500,7500][i];b.classList.toggle('locked',locked);b.title=locked?`Unlock at ${[0,3500,7500][i].toLocaleString()} best score`:''})}
document.querySelectorAll('.route-card').forEach((card,i)=>card.onclick=()=>{if(best<ROUTES[i].unlock){toast('ROUTE LOCKED',`REACH ${ROUTES[i].unlock.toLocaleString()}`);return}routeIndex=i;runIndex=0;populateRuns();updateTrailStatus(routeIndex);syncTrailAccess();updateMountainEvent();loadSnowReport(routeIndex);document.querySelectorAll('.route-card').forEach(c=>c.classList.toggle('selected',c===card));loadWeeklyLeaderboard()});$('runSelect').onchange=e=>{runIndex=+e.currentTarget.value};
document.querySelectorAll('.swatches button').forEach((b,i)=>b.onclick=()=>{if(best<[0,3500,7500][i]){toast('JACKET LOCKED',`BEST ${[0,3500,7500][i].toLocaleString()}`);return}skinIndex=i;store.set('alpineRushSkin',i);document.querySelectorAll('.swatches button').forEach(x=>x.classList.toggle('active',x===b))});document.querySelectorAll('.swatches button')[skinIndex]?.classList.add('active');document.querySelectorAll('[data-rider]').forEach(button=>button.onclick=()=>{riderType=button.dataset.rider;store.set('alpineRushRider',riderType);document.querySelectorAll('[data-rider]').forEach(x=>x.classList.toggle('active',x===button));toast('RIDER SELECTED',riderType==='snowboarder'?'SNOWBOARDER':'SKIER')});document.querySelector(`[data-rider="${riderType}"]`)?.classList.add('active');document.querySelectorAll('[data-helmet]').forEach(button=>button.onclick=()=>{helmetIndex=+button.dataset.helmet;store.set('alpineRushHelmet',helmetIndex);document.querySelectorAll('[data-helmet]').forEach(x=>x.classList.toggle('active',x===button));toast('HELMET DESIGN',['NIGHT','ICE','FLAME'][helmetIndex])});document.querySelector(`[data-helmet="${helmetIndex}"]`)?.classList.add('active');
document.querySelectorAll('[data-goggle]').forEach(button=>button.onclick=()=>{goggleIndex=+button.dataset.goggle;store.set('alpineRushGoggles',goggleIndex);document.querySelectorAll('[data-goggle]').forEach(x=>x.classList.toggle('active',x===button));toast('GOGGLE COLOR',['ICE','GOLD','ROSE'][goggleIndex])});document.querySelector(`[data-goggle="${goggleIndex}"]`)?.classList.add('active');document.querySelectorAll('[data-style]').forEach(button=>button.onclick=()=>{styleIndex=+button.dataset.style;store.set('alpineRushStyle',styleIndex);document.querySelectorAll('[data-style]').forEach(x=>x.classList.toggle('active',x===button));toast('SUIT STYLE',['CLEAN','STRIPE','SPLIT'][styleIndex])});document.querySelector(`[data-style="${styleIndex}"]`)?.classList.add('active');

function beginCountdown(){stopSelectionRefresh();paintDropBriefing();syncTrailAccess();if(trailAccessState==='closed'){toast('PATROL HOLD','WIND TOO STRONG FOR THIS LIFT');return}activeMusicId=null;setMusic(true);hide(ui.routes);hide(ui.results);ui.routeName.textContent=`${ROUTES[routeIndex].name} · ${RUNS[routeIndex][runIndex].name}`;if(liveSnowReport?.routeIndex===routeIndex)toast('MORNING BRIEFING',snowSurface()==='POWDER'?'POWDER FLOAT RECOMMENDED':snowSurface()==='PACKED ICE'?'EDGE GRIP RECOMMENDED':'FRESH LINE · WATCH THE WEATHER');show(ui.countdown);state='countdown';let n=3;ui.countdownValue.textContent=n;const tick=setInterval(()=>{n--;ui.countdownValue.style.animation='none';void ui.countdownValue.offsetWidth;ui.countdownValue.style.animation='';if(n>0){ui.countdownValue.textContent=n;beep(260+n*90,.07)}else if(n===0){ui.countdownValue.textContent='GO';beep(640,.15)}else{clearInterval(tick);hide(ui.countdown);startRun()}},700)}
function reset(){score=0;lives=3;speed=1;distance=0;courseLength=RUNS[routeIndex][runIndex].length;trickBattleTarget=3+((routeIndex+runIndex)%3);courseComplete=false;spawnTimer=.5;spawnCount=0;sceneryOffset=0;shake=0;slowmo=0;cameraX=0;cameraY=0;cameraRoll=0;combo=1;comboTime=0;bestCombo=1;clears=0;gatesCleared=0;gatesMissed=0;nearMisses=0;jumps=0;trickCount=0;boostPickups=0;riskLines=0;patrolClears=0;stashFinds=0;rescueFinds=0;bestTrick='—';bestTrickPoints=0;objectiveDone=false;obstacles=[];particles=[];flakes=[];trails=[];popups=[];Object.assign(player,{x:.5,vx:0,jump:0,jumpV:0,tilt:0,edge:0,compression:0,invincible:0,crash:0,spin:0,spinV:0,flip:0,flipV:0,grabTime:0,trickActive:false});updateUI()}
function startRun(){reset();state='playing';game.classList.add('playing');$('pauseButton').disabled=false;last=performance.now()}
function finish(){if(state==='over')return;state='over';setMusic(false);game.classList.remove('playing');const final=Math.floor(score),wasBest=final>best;best=Math.max(best,final);store.set('alpineRushBest',best);$('titleBest').textContent=String(best).padStart(6,'0');$('finalScore').textContent=final.toLocaleString();$('finalDistance').textContent=`${Math.floor(distance).toLocaleString()} M`;$('finalCombo').textContent=`${bestCombo}×`;$('finalClears').textContent=clears;$('finalTricks').textContent=trickCount;$('finalBestTrick').textContent=bestTrick;$('newBest').classList.toggle('hidden',!wasBest);const rank=final>8000?'S':final>5000?'A':final>2500?'B':'C';$('medal').textContent=rank;$('resultTitle').textContent={S:'LEGENDARY LINE.',A:'MOUNTAIN MASTERED.',B:'SOLID LINE.',C:'ONE MORE RUN.'}[rank];show(ui.results);renderUnlocks()}
function togglePause(){if(state==='playing'){state='paused';Object.keys(keys).forEach(key=>keys[key]=false);game.classList.remove('playing');show(ui.pause);$('pauseButton').setAttribute('aria-pressed','true')}else if(state==='paused'){state='playing';last=performance.now();game.classList.add('playing');hide(ui.pause);$('pauseButton').setAttribute('aria-pressed','false')}}

function beep(freq,duration,type='triangle',volume=.045){if(!sound)return;try{const ac=beep.ac||(beep.ac=new(window.AudioContext||window.webkitAudioContext)()),o=ac.createOscillator(),g=ac.createGain();o.type=type;o.frequency.setValueAtTime(freq,ac.currentTime);o.frequency.exponentialRampToValueAtTime(Math.max(40,freq*.72),ac.currentTime+duration);g.gain.setValueAtTime(volume,ac.currentTime);g.gain.exponentialRampToValueAtTime(.001,ac.currentTime+duration);o.connect(g).connect(ac.destination);o.start();o.stop(ac.currentTime+duration)}catch{}}
function jump(){if(state==='title'){enterRoutes();return}if(state==='select'){beginCountdown();return}if(state==='over'){beginCountdown();return}if(state==='playing'&&player.jump===0){if(AlpinePro.weeklyModifier().id==='no-jump'){toast('NO JUMP WEEK','KEEP IT GROUNDED');beep(160,.08);return}Object.assign(player,{jumpV:580+speed*45,jump:.1,spin:0,spinV:0,flip:0,flipV:0,grabTime:0,trickActive:false,compression:.85});jumps++;burst(player.x*W,player.y*H+25,'#effcff',8);beep(480,.12)}}
function trickLabel(){const degrees=Math.round(Math.abs(player.spin)*180/Math.PI/90)*90,flipProgress=Math.abs(player.flip)/(Math.PI*2),flipName=player.flip<0?'FRONTFLIP':'BACKFLIP',parts=[];if(degrees)parts.push(`${degrees}° ${player.spin<0?'LEFT':'RIGHT'}`);if(flipProgress>.12)parts.push(flipProgress>1.5?`DOUBLE ${flipName}`:flipName);if(player.grabTime>.18)parts.push(riderType==='snowboarder'?'INDY GRAB':'SAFETY GRAB');return parts.join(' + ')||'SETTING UP'}
function landTrick(){const attempted=Math.abs(player.spin)>.35||Math.abs(player.flip)>.35||player.grabTime>.18;if(!attempted)return true;const tau=Math.PI*2,spinR=Math.round(player.spin/tau),flipR=Math.round(player.flip/tau),spinError=Math.abs(player.spin-spinR*tau),flipError=Math.abs(player.flip-flipR*tau);if(spinError>.75||flipError>.75){toast('FAILED LANDING','ROTATION INCOMPLETE');crashPlayer('FAILED LANDING');return false}const rotations=Math.abs(spinR)+Math.abs(flipR),flipLabel=flipR?(Math.abs(flipR)===1?(flipR<0?'FRONTFLIP':'BACKFLIP'):`${Math.abs(flipR)}× ${flipR<0?'FRONTFLIP':'BACKFLIP'}`):'',grabLabel=player.grabTime>.18?(riderType==='snowboarder'?'INDY GRAB':'SAFETY GRAB'):'',label=[spinR?`${Math.abs(spinR)*360}° ${spinR<0?'LEFT':'RIGHT'}`:'',flipLabel,grabLabel].filter(Boolean).join(' + '),wantedSpin=AlpinePro.settings.trickSpin||'any',wantedGrab=AlpinePro.settings.trickGrab||'any',spinMatch=wantedSpin==='any'||Number(wantedSpin)===Math.abs(spinR)*360,grabMatch=wantedGrab==='any'||(wantedGrab==='grab'&&player.grabTime>.18)||(wantedGrab==='nograb'&&player.grabTime<=.18),customBonus=spinMatch&&grabMatch?1.35:1,points=Math.round(Math.max(150,rotations*450+(player.grabTime>.18?250:0))*combo*customBonus);trickCount++;recordChallenge('trick');scorePop(points,player.x*W,player.y*H-45,label);if(points>bestTrickPoints){bestTrickPoints=points;bestTrick=label}combo=Math.min(8,combo+Math.max(1,rotations));comboTime=3.2;bestCombo=Math.max(bestCombo,combo);slowmo=.12;burst(player.x*W,player.y*H,'#ffd260',22);toast(customBonus>1?'SIGNATURE TRICK':'TRICK LANDED',`${label} · +${points}`);beep(880,.18);return true}
function toast(label,value=''){ui.toast.querySelector('small').textContent=label;ui.toast.querySelector('strong').textContent=value;ui.toast.classList.add('show');const announcer=$('importantAnnouncer'),important=['RUN OVER','FINISH LINE','PATROL HOLD','BADGE EARNED','CAREER LEVEL UP','NEW PERSONAL BEST','RIVAL CAMPAIGN','LEGENDARY CHALLENGE'].some(key=>String(label).includes(key));if(announcer&&important)announcer.textContent=value?`${label}: ${value}`:label;clearTimeout(toast.t);toast.t=setTimeout(()=>ui.toast.classList.remove('show'),850)}
function scorePop(points,x,y,label){score+=points;popups.push({x,y,text:`+${points}`,label,life:1});ui.scoreDelta.textContent=`+${points}`;ui.scoreDelta.classList.remove('pop');void ui.scoreDelta.offsetWidth;ui.scoreDelta.classList.add('pop')}
function burst(x,y,color,count=14){if(mobileDevice)count=Math.ceil(count*.55);for(let i=0;i<count;i++)particles.push({x,y,vx:(Math.random()-.5)*170,vy:-20-Math.random()*130,life:.45+Math.random()*.5,size:2+Math.random()*5,color})}

function bindHold(id,key){const el=$(id);el.addEventListener('pointerdown',e=>{e.preventDefault();keys[key]=true;el.setPointerCapture?.(e.pointerId)});for(const ev of ['pointerup','pointercancel','pointerleave'])el.addEventListener(ev,()=>keys[key]=false)}bindHold('leftButton','left');bindHold('rightButton','right');bindHold('spinButton','spinR');bindHold('flipButton','backFlip');bindHold('frontFlipButton','frontFlip');bindHold('grabButton','grab');$('jumpButton').addEventListener('pointerdown',e=>{e.preventDefault();jump()});
$('enterButton').onclick=enterRoutes;$('startButton').onclick=beginCountdown;$('restartButton').onclick=beginCountdown;$('resumeButton').onclick=togglePause;$('pauseButton').onclick=togglePause;$('selectButton').onclick=enterRoutes;$('quitButton').onclick=()=>{hide(ui.pause);enterRoutes()};
$('soundButton').onclick=e=>{sound=!sound;store.set('alpineRushSound',sound?'on':'off');music.muted=!sound;if(sound&&['playing','countdown','select'].includes(state))music.play().catch(()=>{});if(!sound)music.pause();e.currentTarget.setAttribute('aria-pressed',String(sound));e.currentTarget.setAttribute('aria-label',sound?'Mute sound':'Enable sound');e.currentTarget.querySelector('span').textContent=sound?'◖))':'◖×'};$('soundButton').querySelector('span').textContent=sound?'◖))':'◖×';music.muted=!sound;
addEventListener('keydown',e=>{if(['ArrowLeft','a','A'].includes(e.key))keys.left=true;if(['ArrowRight','d','D'].includes(e.key))keys.right=true;if(['q','Q'].includes(e.key))keys.spinL=true;if(['e','E'].includes(e.key))keys.spinR=true;if(['w','W'].includes(e.key))keys.frontFlip=true;if(['s','S'].includes(e.key))keys.backFlip=true;if(e.key==='Shift')keys.grab=true;if(e.code==='Space'){e.preventDefault();jump()}if(['p','P','Escape'].includes(e.key)&&['playing','paused'].includes(state)){e.preventDefault();togglePause()}});addEventListener('keyup',e=>{if(['ArrowLeft','a','A'].includes(e.key))keys.left=false;if(['ArrowRight','d','D'].includes(e.key))keys.right=false;if(['q','Q'].includes(e.key))keys.spinL=false;if(['e','E'].includes(e.key))keys.spinR=false;if(['w','W'].includes(e.key))keys.frontFlip=false;if(['s','S'].includes(e.key))keys.backFlip=false;if(e.key==='Shift')keys.grab=false});document.addEventListener('visibilitychange',()=>{if(document.hidden&&state==='playing')togglePause()});

function courseSample(progress){const points=RUNS[routeIndex][runIndex].line,p=Math.max(0,Math.min(1,progress));let b=points.findIndex(point=>point[0]>=p);if(b<1)b=1;const a=b-1,left=points[a],right=points[Math.min(b,points.length-1)],span=right[0]-left[0]||1,t=(p-left[0])/span;return{center:left[1]+(right[1]-left[1])*t,width:left[2]+(right[2]-left[2])*t}}
function spawn(){const route=ROUTES[routeIndex],mode=window.AlpinePro?.settings?.mode||'freeride',pattern=route.hazards[spawnCount%route.hazards.length],ahead=Math.min(1,(distance+courseLength*.12)/courseLength),sample=courseSample(ahead),usable=Math.max(.08,sample.width*.72),type=mode==='park'?(spawnCount%4===1?'rail':spawnCount%4===2?'ramp':pattern):mode==='slalom'&&spawnCount%2===0?'gate':pattern,x=Math.max(.08,Math.min(.92,sample.center+(Math.random()-.5)*usable)),side=Math.random()>.5?1:-1;spawnCount++;obstacles.push({type,x,y:.05,wobble:Math.random()*6.28,hit:false,cleared:false,color:Math.random()>.5?'#ff5f40':'#53d5ea',side,laneV:type==='snowboarder'?side*(.018+Math.random()*.018):0});if(type==='gate')obstacles.push({type:'gateMate',x:Math.max(.08,Math.min(.92,x+Math.min(.16,usable))),y:.05,wobble:0,hit:false,cleared:false,color:'#ff5f40',side:1,laneV:0})}
function crashPlayer(reason='WIPEOUT'){if(player.invincible>0)return;player.invincible=1.2;player.crash=.68;player.edge=0;player.vx*=-.45;lives--;gearWear=Math.min(100,gearWear+12);store.set('alpineRushGearWear',String(gearWear));combo=1;comboTime=0;shake=17;slowmo=.2;burst(player.x*W,player.y*H,'#ff744f',24);toast(reason,lives?`${lives} HEARTS LEFT`:'RUN OVER');beep(95,.35,'sawtooth',.07);if(lives<=0)setTimeout(finish,500)}
function hitObstacle(o,dx){if(player.invincible>0||o.hit)return;o.hit=true;crashPlayer('WIPEOUT')}
function clearObstacle(o,dx){o.cleared=true;clears++;if(o.type==='gate')gatesCleared++;recordChallenge('clear');const perfect=dx<.032,rail=o.type==='rail',points=(rail?520:perfect?400:220)*combo,label=rail?(riderType==='snowboarder'?'BOARD SLIDE':'SKI SLIDE'):(perfect?'PERFECT CLEAR':combo>2?`${combo}× FLOW`:'CLEAN AIR');scorePop(points,o.x*W,o.y*H,label);combo=Math.min(8,combo+(rail?2:1));comboTime=3.2;bestCombo=Math.max(bestCombo,combo);slowmo=perfect||rail?.1:0;burst(o.x*W,o.y*H,rail?'#ff9d52':perfect?'#ffd45f':'#6be1f5',rail?26:18);toast(label,`+${points}`);beep(rail?980:perfect?820:670,rail?.18:.14)}
function update(dt){if(state!=='playing')return;if(slowmo>0){slowmo-=dt;dt*=.38}const dir=(keys.right?1:0)-(keys.left?1:0),grounded=player.jump===0,edgeTarget=grounded?dir:0;player.edge+=(edgeTarget-player.edge)*dt*(edgeTarget?5.2:3.1);const carveLoad=Math.abs(player.edge),baseSpeed=Math.min(3.25,1+score/5200),surface=snowSurface(),surfaceDrag=surface==='POWDER'?.1:surface==='MOGULS'?.08:surface==='SPRING SLUSH'?.05:surface==='PACKED ICE'?.015:0,carveDrag=grounded?carveLoad*.16:0;speed+=(baseSpeed*(1-surfaceDrag-carveDrag)-speed)*dt*2.4;distance+=dt*42*speed;score+=dt*8*speed;sceneryOffset+=dt*speed;player.invincible=Math.max(0,player.invincible-dt);if(grounded)player.vx+=player.edge*(1.35+speed*.78)*(surface==='PACKED ICE'?1.12:1)*dt;player.vx*=Math.pow(grounded?(surface==='PACKED ICE'?.16:surface==='POWDER'?.08:.11):.38,dt);player.x+=player.vx*dt;const course=courseSample(distance/courseLength),laneLimit=Math.max(.08,course.width*.68),laneDelta=player.x-course.center;if(Math.abs(laneDelta)>laneLimit){player.vx*=.82;speed=Math.max(.72,speed-dt*.65);if(Math.random()<dt*8)burst(player.x*W,player.y*H+25,'#d8f4fa',2)}const left=Math.max(.05,course.center-laneLimit),right=Math.min(.95,course.center+laneLimit);if(player.x<left||player.x>right){player.x=Math.max(left,Math.min(right,player.x));player.vx*=-.28}player.tilt+=(player.edge*.46+player.vx*.09-player.tilt)*dt*7;player.compression+=(Math.min(1,carveLoad*.72+(Math.abs(player.vx)>.35?.14:0))-player.compression)*dt*7;player.anim+=dt*(5+speed*2.2);
 if(player.jump>0){const spinDir=(keys.spinR?1:0)-(keys.spinL?1:0),flipDir=(keys.backFlip?1:0)-(keys.frontFlip?1:0);player.spinV+=spinDir*23*dt;player.flipV+=flipDir*25*dt;player.spinV*=Math.pow(.32,dt);player.flipV*=Math.pow(.4,dt);player.spin+=player.spinV*dt;player.flip+=player.flipV*dt;if(keys.grab)player.grabTime+=dt;player.trickActive=player.trickActive||spinDir!==0||flipDir!==0||keys.grab;ui.trick.classList.toggle('hidden',!player.trickActive);ui.trickName.textContent=trickLabel();player.jumpV-=1480*dt;player.jump+=player.jumpV*dt;if(player.jump<=0){const cleanLanding=landTrick();player.jump=0;player.jumpV=0;player.spin=0;player.spinV=0;player.flip=0;player.flipV=0;player.grabTime=0;player.trickActive=false;player.compression=1;ui.trick.classList.add('hidden');shake=Math.max(shake,cleanLanding?4:17);burst(player.x*W,player.y*H+27,cleanLanding?'#fff':'#ff623f',cleanLanding?12:22);beep(cleanLanding?170:90,cleanLanding?.06:.2)}}
 if(grounded&&carveLoad>.12&&Math.random()<dt*(20+carveLoad*55)){const spraySide=-Math.sign(player.edge||player.vx);particles.push({x:player.x*W+spraySide*12,y:player.y*H+27,vx:spraySide*(45+carveLoad*95)+(Math.random()-.5)*25,vy:-25-Math.random()*55,life:.32+Math.random()*.34,size:2+Math.random()*5,color:'#fff'})}
 cameraX+=( -player.vx*26-player.edge*9-cameraX)*dt*4.5;cameraY+=((speed-1)*-5+(player.jump>0?player.jump*.025:0)-cameraY)*dt*3.2;cameraRoll+=(player.tilt*.035-cameraRoll)*dt*5;
 if(grounded)trails.push({x:player.x,y:player.y+.04,life:1,edge:player.edge});if(trails.length>110)trails.shift();spawnTimer-=dt*speed;if(spawnTimer<=0){spawn();spawnTimer=.72+Math.random()*.65}
 obstacles.forEach(o=>{o.y+=dt*(.43+Math.min(.18,o.y*.09))*speed;const dx=Math.abs(o.x-player.x),dy=Math.abs(o.y-player.y),hitWidth={tree:.052,gate:.045,gateMate:.045,snowboarder:.052}[o.type]||.058,jumpable=!['tree','gate','gateMate'].includes(o.type);if(o.type==='ramp'&&!o.cleared&&dy<.07&&dx<.09&&player.jump===0){o.cleared=true;Object.assign(player,{jumpV:750+speed*35,jump:.1,spin:0,spinV:0,flip:0,flipV:0,grabTime:0,trickActive:false,compression:1});jumps++;burst(player.x*W,player.y*H+25,'#effcff',14);toast('RAMP BOOST','TRICK TIME');beep(560,.12);return}if(!o.hit&&!o.cleared&&dy<.052&&dx<hitWidth){if(player.jump>38&&jumpable)clearObstacle(o,dx);else hitObstacle(o,dx)}else if(!o.cleared&&!o.hit&&o.y>player.y+.075&&o.y<player.y+.11&&dx<.09&&dx>hitWidth){o.cleared=true;if(o.type==='gate')gatesCleared++;nearMisses++;recordChallenge('near');combo=Math.min(8,combo+1);comboTime=2.6;bestCombo=Math.max(bestCombo,combo);const pts=100*combo;scorePop(pts,o.x*W,o.y*H,'NEAR MISS');toast('NEAR MISS',`+${pts}`);beep(760,.07)}else if(AlpinePro.settings.mode==='slalom'&&!o.hit&&!o.cleared&&o.type==='gate'&&o.y>player.y+.16){o.cleared=true;gatesMissed++;combo=1;toast('MISSED GATE','LINE PENALTY');speed=Math.max(.72,speed-.22)}});obstacles=obstacles.filter(o=>o.y<1.2);
 particles.forEach(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=90*dt;p.life-=dt});particles=particles.filter(p=>p.life>0);popups.forEach(p=>{p.y-=35*dt;p.life-=dt});popups=popups.filter(p=>p.life>0);trails.forEach(t=>{t.y+=dt*.43*speed;t.life-=dt*.42});trails=trails.filter(t=>t.life>0&&t.y<1.08);
 comboTime=Math.max(0,comboTime-dt);if(comboTime===0)combo=1;if(Math.random()<dt*(ROUTES[routeIndex].weather==='storm'?70:18)*speed)flakes.push({x:Math.random(),y:-.03,s:1+Math.random()*3,a:.25+Math.random()*.65,drift:(Math.random()-.5)*.08});flakes.forEach(f=>{f.y+=dt*(.32+f.s*.04)*speed;f.x+=f.drift*dt});flakes=flakes.filter(f=>f.y<1.05);updateUI()}
function updateUI(){ui.score.textContent=String(Math.floor(score)).padStart(6,'0');ui.speed.textContent=`${Math.round(34+speed*19)} KM/H`;ui.surface.textContent=snowSurface();ui.distance.textContent=`${Math.floor(distance).toLocaleString()} M`;[...ui.lives.children].forEach((h,i)=>h.classList.toggle('lost',i>=lives));ui.combo.classList.toggle('hidden',combo<2);ui.comboValue.textContent=combo;ui.comboBar.style.transform=`scaleX(${Math.min(1,comboTime/3.2)})`;const mode=window.AlpinePro?.settings?.mode||'freeride';ui.objectiveProgress.textContent=mode==='slalom'?`${gatesCleared} G / ${gatesMissed} MISS`:mode==='park'?`${trickCount} TRICKS`:mode==='timetrial'?`${Math.floor(distance).toLocaleString()} M`:`${Math.min(challengeProgress(),dailyTarget)} / ${dailyTarget}`;game.classList.toggle('fast',speed>2.5)}

function pine(x,y,s,a=1){ctx.save();ctx.globalAlpha=a;ctx.fillStyle=ROUTES[routeIndex].tree;ctx.fillRect(x-2,y-s*.15,4,s*.68);for(let i=0;i<3;i++){ctx.beginPath();ctx.moveTo(x,y-s+i*s*.23);ctx.lineTo(x-s*(.35-i*.03),y-s*.08+i*s*.2);ctx.lineTo(x+s*(.35-i*.03),y-s*.08+i*s*.2);ctx.fill()}ctx.fillStyle='rgba(247,253,255,.68)';ctx.beginPath();ctx.moveTo(x,y-s);ctx.lineTo(x-s*.2,y-s*.33);ctx.lineTo(x+1,y-s*.41);ctx.fill();ctx.restore()}
function drawGermanMountain(r,bank,h){const sky=ctx.createLinearGradient(0,0,0,h);sky.addColorStop(0,r.sky[0]);sky.addColorStop(1,r.sky[1]);ctx.fillStyle=sky;ctx.fillRect(0,0,W,H);const sunX=routeIndex===5?W*.2:W*.78,glow=ctx.createRadialGradient(sunX,H*.13,0,sunX,H*.13,W*.28);glow.addColorStop(0,'rgba(255,235,190,.72)');glow.addColorStop(1,'rgba(255,255,255,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,W,H*.52);ctx.save();ctx.translate(bank*.15,0);ctx.fillStyle='rgba(32,58,72,.48)';ctx.beginPath();ctx.moveTo(-W*.08,h);ctx.lineTo(W*.08,H*.25);ctx.lineTo(W*.2,h*.76);ctx.lineTo(W*.38,H*.18);ctx.lineTo(W*.54,h*.82);ctx.lineTo(W*.7,H*.28);ctx.lineTo(W*1.08,h);ctx.lineTo(W*1.08,H*.57);ctx.lineTo(-W*.08,H*.57);ctx.fill();ctx.fillStyle=r.peak;ctx.beginPath();if(routeIndex===5){ctx.moveTo(-W*.08,h);ctx.lineTo(W*.13,H*.19);ctx.lineTo(W*.27,h*.85);ctx.lineTo(W*.44,H*.31);ctx.lineTo(W*.58,h*.88);ctx.lineTo(W*.78,H*.25);ctx.lineTo(W*1.08,h)}else if(routeIndex===6){ctx.moveTo(-W*.08,h);ctx.lineTo(W*.12,H*.3);ctx.lineTo(W*.25,H*.12);ctx.lineTo(W*.36,H*.31);ctx.lineTo(W*.5,H*.07);ctx.lineTo(W*.64,H*.28);ctx.lineTo(W*.79,H*.18);ctx.lineTo(W*1.08,h)}else{ctx.moveTo(-W*.08,h);ctx.lineTo(W*.12,H*.31);ctx.lineTo(W*.3,H*.1);ctx.lineTo(W*.48,H*.34);ctx.lineTo(W*.63,H*.14);ctx.lineTo(W*.8,H*.36);ctx.lineTo(W*1.08,h)}ctx.lineTo(W*1.08,H*.57);ctx.lineTo(-W*.08,H*.57);ctx.closePath();ctx.fill();ctx.fillStyle='rgba(246,252,253,.88)';ctx.beginPath();if(routeIndex===5){ctx.moveTo(W*.13,H*.19);ctx.lineTo(W*.18,H*.38);ctx.lineTo(W*.27,h*.85);ctx.lineTo(W*.36,H*.35);ctx.lineTo(W*.44,H*.31);ctx.lineTo(W*.52,H*.42);ctx.lineTo(W*.58,h*.88);ctx.closePath()}else if(routeIndex===6){ctx.moveTo(W*.25,H*.12);ctx.lineTo(W*.22,H*.29);ctx.lineTo(W*.3,H*.24);ctx.lineTo(W*.36,H*.31);ctx.lineTo(W*.5,H*.07);ctx.lineTo(W*.57,H*.3);ctx.lineTo(W*.64,H*.28);ctx.closePath()}else{ctx.moveTo(W*.3,H*.1);ctx.lineTo(W*.25,H*.29);ctx.lineTo(W*.36,H*.25);ctx.lineTo(W*.48,H*.34);ctx.lineTo(W*.63,H*.14);ctx.lineTo(W*.68,H*.34);ctx.closePath()}ctx.fill();ctx.restore();const slope=ctx.createLinearGradient(0,h,0,H);slope.addColorStop(0,r.snow[0]);slope.addColorStop(1,r.snow[1]);ctx.fillStyle=slope;ctx.beginPath();ctx.moveTo(0,H*.45);ctx.quadraticCurveTo(W*.5+bank,H*.35,W,H*.45);ctx.lineTo(W,H);ctx.lineTo(0,H);ctx.fill();ctx.save();ctx.translate(bank*.45,0);for(let layer=0;layer<3;layer++){const count=11+layer*4,base=H*(.47+layer*.055),move=(sceneryOffset*(24+layer*17))%(W/count);for(let i=-1;i<=count+1;i++)pine(i*W/count-move,base+(i%3)*7,(layer===2?36:24)+(i%4)*8,layer===2?.9:.55)}ctx.restore();if(routeIndex===6){ctx.strokeStyle='rgba(63,89,103,.7)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(W*.16,H*.28);ctx.lineTo(W*.8,H*.49);ctx.stroke();ctx.fillStyle='rgba(219,239,244,.58)';ctx.beginPath();ctx.moveTo(W*.34,H*.42);ctx.quadraticCurveTo(W*.48,H*.3,W*.61,H*.42);ctx.lineTo(W*.61,H*.5);ctx.lineTo(W*.34,H*.5);ctx.closePath();ctx.fill()}if(routeIndex===7){ctx.strokeStyle='rgba(62,76,85,.8)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(W*.2,H*.24);ctx.lineTo(W*.79,H*.48);ctx.stroke();ctx.fillStyle='#2e4854';ctx.fillRect(W*.72,H*.24,3,H*.27);ctx.fillStyle='#d8eef3';ctx.fillRect(W*.71,H*.23,26,4)}if(routeIndex===5){ctx.fillStyle='rgba(32,68,58,.72)';for(let i=0;i<12;i++)pine(W*(i/11),H*(.49+(i%3)*.025),20+(i%4)*6,.8)}ctx.fillStyle='rgba(6,24,34,.8)';roundedPath(18,H*.47,190,38,6);ctx.fill();ctx.fillStyle='#fff';ctx.font='800 11px DM Sans';ctx.textAlign='left';ctx.fillText(r.name,31,H*.487);ctx.fillStyle='#8dddf0';ctx.font='700 8px DM Sans';ctx.fillText(`${r.place} · ALPINE SERIES`,31,H*.507)}
function mountain(){const r=ROUTES[routeIndex],h=H*.39,bank=player.tilt*35,image=resortImages[routeIndex];if(image?.complete&&image.naturalWidth){const sourceRatio=image.naturalWidth/image.naturalHeight,targetRatio=W/H;let sx=0,sy=0,sw=image.naturalWidth,sh=image.naturalHeight;if(sourceRatio>targetRatio){sw=image.naturalHeight*targetRatio;sx=(image.naturalWidth-sw)/2}else{sh=image.naturalWidth/targetRatio;sy=(image.naturalHeight-sh)/2}ctx.drawImage(image,sx,sy,sw,sh,-bank*.18,0,W+Math.abs(bank*.36),H);const course=ctx.createLinearGradient(0,H*.58,0,H);course.addColorStop(0,'rgba(240,249,252,0)');course.addColorStop(.3,'rgba(224,241,246,.32)');course.addColorStop(1,'rgba(185,218,229,.7)');ctx.fillStyle=course;ctx.beginPath();ctx.moveTo(0,H*.58);ctx.quadraticCurveTo(W*.5+bank,H*.48,W,H*.58);ctx.lineTo(W,H);ctx.lineTo(0,H);ctx.fill();ctx.strokeStyle='rgba(59,117,137,.2)';ctx.lineWidth=2;for(let i=0;i<7;i++){ctx.beginPath();ctx.moveTo(W*(i/6),H*.55);ctx.quadraticCurveTo(W*.5+bank,H*.72,W*(.5+(i-3)*.17),H);ctx.stroke()}const vignette=ctx.createRadialGradient(W*.5,H*.55,H*.18,W*.5,H*.55,W*.78);vignette.addColorStop(.48,'transparent');vignette.addColorStop(1,[0,3].includes(routeIndex)?'rgba(2,7,22,.48)':'rgba(5,25,38,.2)');ctx.fillStyle=vignette;ctx.fillRect(0,0,W,H);return}if(routeIndex===9){drawPurgatoryMountain(r,bank,h);return}if(routeIndex>=5){drawGermanMountain(r,bank,h);return}const sky=ctx.createLinearGradient(0,0,0,h);sky.addColorStop(0,r.sky[0]);sky.addColorStop(1,r.sky[1]);ctx.fillStyle=sky;ctx.fillRect(0,0,W,H);const sunX=routeIndex===2?W*.82:W*.74;const glow=ctx.createRadialGradient(sunX,H*.12,0,sunX,H*.12,W*.26);glow.addColorStop(0,routeIndex===2?'rgba(255,160,120,.65)':'rgba(255,250,216,.9)');glow.addColorStop(1,'rgba(255,255,255,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,W,H*.5);
 ctx.save();ctx.translate(bank*.18,0);ctx.fillStyle=r.peak;ctx.beginPath();ctx.moveTo(-W*.1,h);ctx.lineTo(W*.12,H*.15);ctx.lineTo(W*.27,h*.92);ctx.lineTo(W*.48,H*.08);ctx.lineTo(W*.67,h);ctx.lineTo(W*.84,H*.18);ctx.lineTo(W*1.1,h);ctx.lineTo(W*1.1,H*.56);ctx.lineTo(-W*.1,H*.56);ctx.fill();ctx.fillStyle='rgba(248,253,255,.88)';ctx.beginPath();ctx.moveTo(-W*.1,h);ctx.lineTo(W*.12,H*.15);ctx.lineTo(W*.1,H*.27);ctx.lineTo(W*.17,H*.23);ctx.lineTo(W*.27,h*.92);ctx.lineTo(W*.48,H*.08);ctx.lineTo(W*.44,H*.25);ctx.lineTo(W*.52,H*.2);ctx.lineTo(W*.67,h);ctx.lineTo(W*.84,H*.18);ctx.lineTo(W*.8,H*.3);ctx.lineTo(W*.88,H*.27);ctx.lineTo(W*1.1,h);ctx.lineTo(W*1.1,H*.55);ctx.lineTo(-W*.1,H*.55);ctx.fill();ctx.restore();
 const slope=ctx.createLinearGradient(0,h,0,H);slope.addColorStop(0,r.snow[0]);slope.addColorStop(1,r.snow[1]);ctx.fillStyle=slope;ctx.beginPath();ctx.moveTo(0,H*.44);ctx.quadraticCurveTo(W*.5+bank,H*.35,W,H*.44);ctx.lineTo(W,H);ctx.lineTo(0,H);ctx.fill();
 ctx.save();ctx.translate(bank*.5,0);for(let layer=0;layer<2;layer++){const count=layer?13:9,base=H*(.47+layer*.05),move=(sceneryOffset*(layer?65:34))%(W/count);for(let i=-1;i<=count+1;i++)pine(i*W/count-move,base+(i%3)*8,(layer?30:20)+(i%4)*7,layer?.88:.55)}ctx.restore();
 ctx.strokeStyle='rgba(60,126,150,.2)';ctx.lineWidth=2;for(let i=0;i<7;i++){ctx.beginPath();ctx.moveTo(W*(i/6),H*.45);ctx.quadraticCurveTo(W*.5+bank,H*.68,W*(.5+(i-3)*.17),H);ctx.stroke()}if(r.weather==='mist'){ctx.fillStyle='rgba(220,240,238,.22)';ctx.fillRect(0,H*.3,W,H*.32)}const dayShift=Math.min(.24,distance/12000);if(dayShift){ctx.fillStyle=`rgba(17,24,65,${dayShift})`;ctx.fillRect(0,0,W,H)}const vignette=ctx.createRadialGradient(W*.5,H*.53,H*.2,W*.5,H*.53,W*.75);vignette.addColorStop(.5,'transparent');vignette.addColorStop(1,[0,3].includes(routeIndex)?'rgba(2,7,22,.65)':'rgba(5,25,38,.28)');ctx.fillStyle=vignette;ctx.fillRect(0,0,W,H)}
function drawObstacle(o){const x=o.x*W,y=o.y*H,scale=.65+o.y*.52;ctx.save();ctx.translate(x,y);ctx.scale(scale,scale);if(o.hit)ctx.globalAlpha=.45;if(o.type==='tree'){pine(0,20,58,1)}else if(o.type==='rock'){ctx.fillStyle='#415767';ctx.beginPath();ctx.moveTo(-24,22);ctx.lineTo(-15,-8);ctx.lineTo(4,-18);ctx.lineTo(24,19);ctx.closePath();ctx.fill();ctx.fillStyle='#e7f4f7';ctx.beginPath();ctx.moveTo(-15,-8);ctx.lineTo(4,-18);ctx.lineTo(11,-4);ctx.lineTo(-3,-7);ctx.fill()}else if(o.type==='ramp'){ctx.fillStyle='#d5e8ed';ctx.beginPath();ctx.moveTo(-30,18);ctx.lineTo(20,-8);ctx.lineTo(31,18);ctx.fill();ctx.strokeStyle='#ff693f';ctx.lineWidth=4;ctx.stroke()}else if(o.type==='rail'){ctx.strokeStyle='#263f4c';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-31,15);ctx.lineTo(31,4);ctx.stroke();ctx.strokeStyle='#eaf8fb';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-29,12);ctx.lineTo(29,2);ctx.stroke();ctx.strokeStyle='#263f4c';ctx.lineWidth=3;for(const leg of [-22,22]){ctx.beginPath();ctx.moveTo(leg,11);ctx.lineTo(leg,25);ctx.stroke()}ctx.fillStyle='#ff9d52';ctx.fillRect(-31,1,7,4);ctx.fillRect(24,-10,7,4)}else if(o.type==='gate'||o.type==='gateMate'){ctx.strokeStyle=o.type==='gate'?'#ff5f3d':'#4cd7ee';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(0,-29);ctx.lineTo(0,27);ctx.stroke();ctx.fillStyle=ctx.strokeStyle;ctx.fillRect(0,-28,25*(o.type==='gate'?1:-1),13)}else{drawSkier(0,0,Math.sin(o.wobble+o.y*9)*.12,o.color,false)}ctx.restore()}
function drawSkier(x,y,tilt,color,isPlayer){ctx.save();ctx.translate(x,y);const airborne=isPlayer&&player.jump>0,edge=isPlayer?player.edge:Math.sin(player.anim*.45)*.2,crouch=isPlayer?player.compression:.2,turn=edge*.34,skin=isPlayer?SKINS[skinIndex]:{a:color,b:color},polePlant=Math.sin(player.anim*.72)*3;ctx.rotate(turn);ctx.lineCap='round';ctx.lineJoin='round';/* skis point down the fall line in the chase-camera view */ctx.strokeStyle='#173244';ctx.lineWidth=3.4;ctx.beginPath();ctx.moveTo(-6,-2);ctx.lineTo(-7+edge*4,42);ctx.moveTo(6,-2);ctx.lineTo(7+edge*4,42);ctx.stroke();ctx.strokeStyle='#e9f7fa';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(-6,7);ctx.lineTo(-7+edge*4,39);ctx.moveTo(6,7);ctx.lineTo(7+edge*4,39);ctx.stroke();/* boots, bent knees and hips seen from behind */ctx.strokeStyle='#253e4d';ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(-5,9+crouch*3);ctx.lineTo(-9-edge*3,21+crouch*4);ctx.lineTo(-7+edge*4,31);ctx.moveTo(5,9+crouch*3);ctx.lineTo(9-edge*3,21+crouch*4);ctx.lineTo(7+edge*4,31);ctx.stroke();const lean=edge*5,grad=ctx.createLinearGradient(-14,-16,14,13);grad.addColorStop(0,skin.a);grad.addColorStop(1,skin.b);ctx.fillStyle=grad;ctx.beginPath();ctx.moveTo(-12+lean,-12+crouch*2);ctx.quadraticCurveTo(lean,-17,12+lean,-12+crouch*2);ctx.lineTo(9,12+crouch*3);ctx.quadraticCurveTo(0,16+crouch*3,-9,12+crouch*3);ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(255,255,255,.32)';ctx.lineWidth=1;ctx.stroke();/* arms trail naturally behind the torso while carving */ctx.strokeStyle='#263f4e';ctx.lineWidth=3.2;ctx.beginPath();ctx.moveTo(-10+lean,-8);ctx.lineTo(-19-edge*5,7);ctx.lineTo(-23-edge*7,27+polePlant);ctx.moveTo(10+lean,-8);ctx.lineTo(19-edge*5,7);ctx.lineTo(23-edge*7,27-polePlant);ctx.stroke();ctx.strokeStyle='#49636e';ctx.lineWidth=1.3;ctx.beginPath();ctx.moveTo(-23-edge*7,10);ctx.lineTo(-25-edge*7,36);ctx.moveTo(23-edge*7,10);ctx.lineTo(25-edge*7,36);ctx.stroke();/* rear of helmet and goggle strap */ctx.fillStyle='#0b2332';ctx.beginPath();ctx.arc(lean,-21+crouch*2,9.5,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#74d8ef';ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(lean,-21+crouch*2,7.5,Math.PI*.08,Math.PI*.92);ctx.stroke();if(!airborne){ctx.strokeStyle='rgba(255,255,255,.65)';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(-7,41);ctx.lineTo(-10-edge*7,49);ctx.moveTo(7,41);ctx.lineTo(10-edge*7,49);ctx.stroke()}ctx.restore()}
function drawSnowboarderRider(x,y,tilt,color){const edge=player.edge,crouch=player.compression;ctx.save();ctx.translate(x,y);ctx.rotate(tilt*.55);ctx.lineCap='round';ctx.lineJoin='round';ctx.strokeStyle='#172d3b';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-25,28);ctx.quadraticCurveTo(0,35,25,28);ctx.stroke();ctx.strokeStyle='#e6f7fa';ctx.lineWidth=1.4;ctx.beginPath();ctx.moveTo(-22,27);ctx.quadraticCurveTo(0,32,22,27);ctx.stroke();ctx.strokeStyle='#263f4d';ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(-5,9+crouch*3);ctx.lineTo(-15-edge*3,20+crouch*3);ctx.lineTo(-18,25);ctx.moveTo(5,9+crouch*3);ctx.lineTo(13-edge*2,19+crouch*3);ctx.lineTo(18,25);ctx.stroke();const grad=ctx.createLinearGradient(-14,-16,14,14);grad.addColorStop(0,SKINS[skinIndex].a);grad.addColorStop(1,SKINS[skinIndex].b);ctx.fillStyle=grad;roundedPath(-12+edge*3,-13+crouch*2,24,26,7);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.3)';ctx.lineWidth=1;ctx.stroke();ctx.strokeStyle='#263f4d';ctx.lineWidth=3.2;ctx.beginPath();ctx.moveTo(-10,-8);ctx.lineTo(-21,4);ctx.lineTo(-26,18+Math.sin(player.anim)*3);ctx.moveTo(10,-8);ctx.lineTo(20,5);ctx.lineTo(27,17-Math.sin(player.anim)*3);ctx.stroke();ctx.fillStyle='#0b2332';ctx.beginPath();ctx.arc(edge*3,-21+crouch*2,9.5,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#74d8ef';ctx.lineWidth=2.4;ctx.beginPath();ctx.arc(edge*3,-21+crouch*2,7.5,Math.PI*.08,Math.PI*.92);ctx.stroke();ctx.restore()}
function drawRiderDetails(){const helmet=HELMETS[helmetIndex]||HELMETS[0],jacket=SKINS[skinIndex];ctx.save();ctx.lineCap='round';ctx.strokeStyle='rgba(255,255,255,.38)';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(-8,-9);ctx.lineTo(0,-4);ctx.lineTo(8,-9);ctx.stroke();ctx.strokeStyle='rgba(7,27,39,.45)';ctx.lineWidth=1.4;ctx.beginPath();ctx.moveTo(0,-9);ctx.lineTo(0,11);ctx.stroke();ctx.fillStyle='rgba(7,27,39,.28)';ctx.beginPath();ctx.ellipse(0,7,8,5,0,0,Math.PI*2);ctx.fill();ctx.fillStyle=helmet;ctx.beginPath();ctx.arc(0,-21,9.2,Math.PI*1.05,Math.PI*1.95);ctx.lineTo(8,-20);ctx.quadraticCurveTo(0,-14,-8,-20);ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(255,255,255,.4)';ctx.lineWidth=1;ctx.stroke();ctx.fillStyle='#70dff1';ctx.globalAlpha=.9;ctx.beginPath();ctx.roundRect?.(-7,-22,14,4,2);if(ctx.roundRect)ctx.fill();else{ctx.fillRect(-7,-22,14,4)}ctx.globalAlpha=1;ctx.fillStyle=jacket.b;ctx.beginPath();ctx.arc(-10,7,3,0,Math.PI*2);ctx.fill();ctx.restore()}
function drawRider(){if(riderType==='snowboarder')drawSnowboarderRider(0,0,player.tilt,SKINS[skinIndex].a);else drawSkier(0,0,player.tilt,SKINS[skinIndex].a,true);drawRiderDetails()}
function drawPlayer(){const x=player.x*W,terrainBob=player.jump===0?Math.sin(sceneryOffset*8)*1.2:0,y=player.y*H-player.jump+terrainBob;if(player.invincible>0&&player.crash===0&&Math.floor(player.invincible*12)%2===0)return;ctx.save();ctx.translate(x,y);const shadowAlpha=Math.max(.045,.24-player.jump/600),shadowWidth=30+player.jump*.07;ctx.fillStyle=`rgba(14,52,67,${shadowAlpha})`;ctx.beginPath();ctx.ellipse(0,player.jump+34,shadowWidth,7+player.jump*.025,0,0,Math.PI*2);ctx.fill();if(player.crash>0){ctx.rotate((1-player.crash/.68)*2.4+1.1);ctx.scale(1.12,.72)}else if(player.jump>0){ctx.rotate(player.spin);ctx.scale(1,Math.cos(player.flip));if(player.grabTime>.04){ctx.translate(0,5);ctx.rotate(.12)}}drawRider();ctx.restore()}
function drawSpeedStreaks(){const intensity=Math.max(0,Math.min(1,(speed-1.35)/2.2));if(intensity<=0)return;ctx.save();ctx.globalAlpha=.08+intensity*.13;ctx.strokeStyle='#f6fdff';ctx.lineWidth=1+intensity*2;for(let i=0;i<14;i++){const x=((i*83+sceneryOffset*120)% (W+80))-40;const y=H*.48+((i*47+sceneryOffset*210)%(H*.62));ctx.beginPath();ctx.moveTo(W*.5+(x-W*.5)*.12,H*.47);ctx.lineTo(x,y);ctx.stroke()}ctx.restore()}
function draw(){ctx.save();ctx.imageSmoothingEnabled=true;if(shake>0){ctx.translate((Math.random()-.5)*shake,(Math.random()-.5)*shake);shake*=.86}ctx.translate(W/2,H*.55);ctx.rotate(cameraRoll);ctx.translate(-W/2+cameraX,-H*.55+cameraY);mountain();drawSpeedStreaks();trails.forEach(t=>{ctx.globalAlpha=t.life*.24;ctx.fillStyle='#3c819a';const angle=-t.edge*.28;for(const offset of [-4,4]){ctx.beginPath();ctx.ellipse(t.x*W+offset,t.y*H,1.1,6,angle,0,Math.PI*2);ctx.fill()}});ctx.globalAlpha=1;obstacles.sort((a,b)=>a.y-b.y).forEach(drawObstacle);flakes.forEach(f=>{ctx.strokeStyle=`rgba(255,255,255,${f.a})`;ctx.lineWidth=f.s;ctx.beginPath();ctx.moveTo(f.x*W,f.y*H);ctx.lineTo(f.x*W-player.vx*18,f.y*H+7+f.s*4);ctx.stroke()});drawPlayer();particles.forEach(p=>{ctx.globalAlpha=Math.min(1,p.life*2);ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fill()});ctx.globalAlpha=1;popups.forEach(p=>{ctx.globalAlpha=p.life;ctx.textAlign='center';ctx.fillStyle='#fff';ctx.font='900 22px Barlow Condensed';ctx.fillText(p.text,p.x,p.y);ctx.fillStyle='#5edcf5';ctx.font='800 8px DM Sans';ctx.fillText(p.label,p.x,p.y+14)});ctx.restore()}
function frame(t){const dt=Math.min(.033,(t-last)/1000||0);last=t;if(state==='playing'&&player.crash>0)player.crash=Math.max(0,player.crash-dt);update(dt);draw();requestAnimationFrame(frame)}requestAnimationFrame(frame);renderUnlocks();updateUI();const revealGame=()=>{clearTimeout(window.__bootTimer);requestAnimationFrame(()=>$('bootScreen').classList.add('ready'))};if(resortImages[0].complete)revealGame();else{resortImages[0].addEventListener('load',revealGame,{once:true});resortImages[0].addEventListener('error',revealGame,{once:true})}

function drawRunTerrain(){const run=RUNS[routeIndex][runIndex],slices=[];for(let i=0;i<=18;i++){const t=i/18,ahead=(1-t)*courseLength*.2,s=courseSample((distance+ahead)/courseLength),perspective=.15+t*.9;slices.push({x:s.center*W,y:H*(.45+t*.58),half:Math.max(10,s.width*W*.5*perspective),t})}ctx.save();ctx.fillStyle='rgba(8,27,35,.2)';ctx.beginPath();ctx.moveTo(0,H*.43);slices.forEach(s=>ctx.lineTo(s.x-s.half,s.y));ctx.lineTo(0,H*1.04);ctx.closePath();ctx.fill();ctx.beginPath();ctx.moveTo(W,H*.43);slices.forEach(s=>ctx.lineTo(s.x+s.half,s.y));ctx.lineTo(W,H*1.04);ctx.closePath();ctx.fill();const snow=ctx.createLinearGradient(0,H*.45,0,H);snow.addColorStop(0,'rgba(241,249,251,.42)');snow.addColorStop(1,'rgba(225,241,245,.92)');ctx.fillStyle=snow;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half,s.y):ctx.moveTo(s.x-s.half,s.y));for(let i=slices.length-1;i>=0;i--){const s=slices[i];ctx.lineTo(s.x+s.half,s.y)}ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(56,105,121,.5)';ctx.lineWidth=2;for(const side of [-1,1]){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke()}ctx.strokeStyle='rgba(77,137,154,.18)';ctx.lineWidth=1.5;for(const lane of [-.5,0,.5]){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+s.half*lane,s.y):ctx.moveTo(s.x+s.half*lane,s.y));ctx.stroke()}if(['moguls','steep','spine'].includes(run.terrain)){ctx.fillStyle='rgba(109,151,164,.25)';for(let row=5;row<17;row+=2){const s=slices[row];for(let m=-2;m<=2;m++){ctx.beginPath();ctx.ellipse(s.x+m*s.half*.3+(row%4?5:-5),s.y,3+s.t*7,1.5+s.t*3,0,0,Math.PI*2);ctx.fill()}}}if(run.terrain==='park'){ctx.fillStyle='#ef643f';for(const row of [7,12,16]){const s=slices[row];ctx.fillRect(s.x-s.half*.24,s.y-2,Math.max(12,s.half*.48),3+s.t*3)}}if(run.terrain==='spine'){ctx.fillStyle='rgba(35,61,72,.35)';for(const side of [-1,1]){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*(s.half+12+s.t*35),s.y):ctx.moveTo(s.x+side*(s.half+12),s.y));slices.slice().reverse().forEach(s=>ctx.lineTo(s.x+side*s.half,s.y));ctx.closePath();ctx.fill()}}const treeTerrain=['glades','aspen'].includes(run.terrain),treeStep=treeTerrain?2:3;for(let row=3;row<17;row+=treeStep){const s=slices[row],size=12+s.t*50;pine(s.x-s.half-size*.22,s.y+size*.25,size,treeTerrain?.95:.7);pine(s.x+s.half+size*.22,s.y+size*.25,size,treeTerrain?.95:.7);if(treeTerrain&&row%4===1)pine(s.x+s.half*(row%3===0?-.35:.32),s.y+size*.15,size*.75,.78)}if(['alpine','bowl'].includes(run.terrain)){ctx.strokeStyle='rgba(55,75,86,.7)';ctx.lineWidth=2;const side=run.terrain==='bowl'?-1:1;for(const row of [5,10,15]){const s=slices[row],x=s.x+side*(s.half+18);ctx.beginPath();ctx.moveTo(x,s.y-20-s.t*22);ctx.lineTo(x,s.y+8);ctx.stroke();ctx.fillStyle='#344c57';ctx.fillRect(x-10,s.y-22-s.t*22,20,4)}}ctx.fillStyle='rgba(6,24,34,.82)';roundedPath(18,H*.47,152,38,6);ctx.fill();ctx.fillStyle='#fff';ctx.textAlign='left';ctx.font='800 11px DM Sans';ctx.fillText(run.name.toUpperCase(),31,H*.487);ctx.fillStyle='#8dddf0';ctx.font='700 8px DM Sans';ctx.fillText(`${run.difficulty} · ${run.terrain.toUpperCase()}`,31,H*.507);ctx.restore()}
function drawFullRunTerrain(){
 const run=RUNS[routeIndex][runIndex],slices=[];
 for(let i=0;i<=22;i++){const t=i/22,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.025+t*(.52+s.width*.14)),t})}
 ctx.save();ctx.fillStyle='rgba(7,25,34,.3)';ctx.fillRect(0,H*.42,W,H*.62);
 const snow=ctx.createLinearGradient(0,H*.42,0,H);snow.addColorStop(0,'rgba(242,250,252,.55)');snow.addColorStop(.5,'rgba(224,240,245,.94)');snow.addColorStop(1,'#d3e8ee');ctx.fillStyle=snow;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half,s.y):ctx.moveTo(s.x-s.half,s.y));for(let i=slices.length-1;i>=0;i--){const s=slices[i];ctx.lineTo(s.x+s.half,s.y)}ctx.closePath();ctx.fill();
 ctx.strokeStyle='rgba(53,104,121,.46)';ctx.lineWidth=2;for(const side of [-1,1]){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke()}
 /* moving corduroy and fall-line texture makes forward travel readable */
 ctx.strokeStyle='rgba(94,146,161,.17)';ctx.lineWidth=1;for(let lane=-5;lane<=5;lane++){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+s.half*lane/6,s.y):ctx.moveTo(s.x+s.half*lane/6,s.y));ctx.stroke()}for(let row=4;row<22;row+=2){const s=slices[row];ctx.globalAlpha=.08+s.t*.14;ctx.beginPath();ctx.moveTo(s.x-s.half*.88,s.y);ctx.lineTo(s.x+s.half*.88,s.y);ctx.stroke()}ctx.globalAlpha=1;for(const row of [5,9,13,17,21]){const s=slices[Math.min(row,slices.length-1)],poleH=10+s.t*30;ctx.strokeStyle='rgba(35,63,73,.8)';ctx.lineWidth=2;for(const side of [-1,1]){const x=s.x+side*(s.half+8+s.t*10);ctx.beginPath();ctx.moveTo(x,s.y);ctx.lineTo(x,s.y-poleH);ctx.stroke();ctx.fillStyle=side<0?'#ff6542':'#62d9f5';ctx.fillRect(x-2,s.y-poleH,4,5)}}
 if(['moguls','steep','spine'].includes(run.terrain)){ctx.fillStyle='rgba(91,137,152,.28)';for(let row=6;row<20;row+=2){const s=slices[row];for(let m=-3;m<=3;m++){ctx.beginPath();ctx.ellipse(s.x+m*s.half*.2+(row%4?5:-5),s.y,3+s.t*8,1.5+s.t*3,0,0,Math.PI*2);ctx.fill()}}}
 if(run.terrain==='park'){ctx.fillStyle='#ef643f';for(const row of [8,13,18]){const s=slices[row];roundedPath(s.x-s.half*.18,s.y-3,Math.max(16,s.half*.36),4+s.t*4,2);ctx.fill()}}
 if(run.terrain==='spine'){ctx.fillStyle='rgba(30,55,67,.4)';for(const side of [-1,1]){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*(s.half+10+s.t*34),s.y):ctx.moveTo(s.x+side*(s.half+10),s.y));slices.slice().reverse().forEach(s=>ctx.lineTo(s.x+side*s.half,s.y));ctx.closePath();ctx.fill()}}
 const dense=['glades','aspen'].includes(run.terrain);for(let row=4;row<20;row+=dense?2:3){const s=slices[row],size=11+s.t*55;pine(s.x-s.half-size*.2,s.y+size*.25,size,dense?.98:.7);pine(s.x+s.half+size*.2,s.y+size*.25,size,dense?.98:.7);if(dense&&row%4===0)pine(s.x+s.half*(row%3===0?-.42:.36),s.y+size*.12,size*.72,.82)}
 ctx.fillStyle='rgba(5,20,30,.86)';roundedPath(18,H*.455,184,44,7);ctx.fill();const black=run.difficulty.includes('BLACK');ctx.fillStyle=black?'#111820':'#2d85d3';ctx.save();ctx.translate(37,H*.485);if(black)ctx.rotate(Math.PI/4);ctx.fillRect(-6.5,-6.5,13,13);ctx.restore();if(run.difficulty==='DOUBLE BLACK'){ctx.save();ctx.translate(47,H*.485);ctx.rotate(Math.PI/4);ctx.fillRect(-6.5,-6.5,13,13);ctx.restore()}ctx.fillStyle='#fff';ctx.font='800 12px DM Sans';ctx.textAlign='left';ctx.fillText(run.name.toUpperCase(),run.difficulty==='DOUBLE BLACK'?62:52,H*.48);ctx.fillStyle='#8dddf0';ctx.font='700 8px DM Sans';ctx.fillText(`${ROUTES[routeIndex].name} · ${run.difficulty}`,run.difficulty==='DOUBLE BLACK'?62:52,H*.502);ctx.restore()
}
function drawBaseArea(){const remaining=Math.max(0,courseLength-distance);if(remaining>350)return;const progress=(350-remaining)/350,y=H*(.48+progress*.2),scale=.24+progress*.88;ctx.save();ctx.translate(W*(.7-progress*.08),y);ctx.scale(scale,scale);if(routeIndex===0){ctx.fillStyle='rgba(16,31,38,.24)';ctx.beginPath();ctx.ellipse(-35,41,115,19,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#4a362c';ctx.fillRect(-87,-4,138,48);ctx.fillStyle='#23323a';ctx.beginPath();ctx.moveTo(-99,-4);ctx.lineTo(-70,-35);ctx.lineTo(66,-30);ctx.lineTo(78,-4);ctx.closePath();ctx.fill();ctx.fillStyle='#b7d9e1';for(let i=0;i<5;i++)ctx.fillRect(-70+i*25,8,16,17);ctx.fillStyle='#f0a65a';for(let i=0;i<5;i++)ctx.fillRect(-68+i*25,10,12,13);ctx.fillStyle='#d6c1a2';ctx.fillRect(-82,33,151,6);ctx.strokeStyle='#d6c1a2';ctx.lineWidth=3;for(let i=-78;i<68;i+=18){ctx.beginPath();ctx.moveTo(i,33);ctx.lineTo(i,51);ctx.stroke()}ctx.fillStyle='#30414a';ctx.fillRect(-150,11,48,32);ctx.beginPath();ctx.moveTo(-156,11);ctx.lineTo(-126,-11);ctx.lineTo(-96,11);ctx.fill();ctx.fillStyle='#f1b05e';ctx.fillRect(-138,20,13,12);ctx.fillStyle='#f7fbfd';ctx.font='800 10px DM Sans';ctx.textAlign='center';ctx.fillText('ECHO MOUNTAIN',-17,-11);ctx.strokeStyle='#687984';ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(104,-18);ctx.lineTo(104,45);ctx.stroke();ctx.fillStyle='#263843';ctx.beginPath();ctx.ellipse(104,-19,25,8,0,0,Math.PI*2);ctx.fill()}else{ctx.fillStyle='rgba(18,39,48,.75)';roundedPath(-72,-12,144,38,7);ctx.fill();ctx.fillStyle='#fff';ctx.font='800 13px DM Sans';ctx.textAlign='center';ctx.fillText(`${ROUTES[routeIndex].name} BASE` ,0,12)}ctx.restore();if(remaining<130){const gateProgress=(130-remaining)/130,gy=H*(.5+gateProgress*.26),half=W*(.08+gateProgress*.24);ctx.strokeStyle='#ff6542';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(W/2-half,gy-34);ctx.lineTo(W/2-half,gy+35);ctx.moveTo(W/2+half,gy-34);ctx.lineTo(W/2+half,gy+35);ctx.stroke();ctx.fillStyle='rgba(7,27,39,.88)';ctx.fillRect(W/2-half,gy-34,half*2,22);ctx.fillStyle='#fff';ctx.font='800 10px DM Sans';ctx.textAlign='center';ctx.fillText('FINISH',W/2,gy-19)}}
const renderTerrain=mountain;mountain=function(){renderTerrain();drawFullRunTerrain();drawBaseArea()};
const simulateRun=update;update=function(dt){simulateRun(dt);if(state!=='playing')return;const remaining=Math.max(0,courseLength-distance);ui.distance.textContent=`${Math.ceil(remaining).toLocaleString()} M`;if(remaining<150)obstacles=obstacles.filter(o=>o.y>.32);if(distance>=courseLength&&!courseComplete){courseComplete=true;state='finishing';score+=1500;toast('FINISH LINE',`${RUNS[routeIndex][runIndex].name.toUpperCase()} · +1,500`);beep(980,.3);setTimeout(finish,650)}};
const updateDownhillTraffic=update;update=function(dt){updateDownhillTraffic(dt);if(state!=='playing')return;obstacles.forEach(o=>{if(o.type!=='snowboarder')return;o.x+=o.laneV*dt*speed;if(o.x<.12||o.x>.88){o.x=Math.max(.12,Math.min(.88,o.x));o.laneV*=-1}})};
function drawSnowboarder(o){if(o.y<.4)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),x=o.x*W,y=o.y*H,scale=.16+t*1.05,carve=Math.sin(o.wobble+o.y*10)*.22+o.side*.08;ctx.save();ctx.translate(x,y);ctx.scale(scale,scale);ctx.fillStyle='rgba(12,40,50,.22)';ctx.beginPath();ctx.ellipse(0,32,27,6,0,0,Math.PI*2);ctx.fill();ctx.rotate(carve);ctx.strokeStyle='#142f3f';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-24,29);ctx.lineTo(25,24);ctx.stroke();ctx.strokeStyle='#263f4d';ctx.lineWidth=6;ctx.beginPath();ctx.moveTo(-5,6);ctx.lineTo(-11,18);ctx.lineTo(-14,26);ctx.moveTo(5,6);ctx.lineTo(10,17);ctx.lineTo(13,25);ctx.stroke();const jacket=ctx.createLinearGradient(-12,-15,12,12);jacket.addColorStop(0,o.color);jacket.addColorStop(1,o.color==='#ff5f40'?'#c93428':'#167e9e');ctx.fillStyle=jacket;roundedPath(-12,-13,24,25,7);ctx.fill();ctx.strokeStyle='#263f4d';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(-9,-7);ctx.lineTo(-18,9);ctx.moveTo(9,-7);ctx.lineTo(18,8);ctx.stroke();ctx.fillStyle='#0b2332';ctx.beginPath();ctx.arc(0,-21,9,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#72d8ed';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,-21,7,Math.PI*.1,Math.PI*.9);ctx.stroke();ctx.restore()}
const drawGroundedObstacle=drawObstacle;drawObstacle=function(o){if(o.y<.4)return;if(o.type==='snowboarder'){drawSnowboarder(o);return}const t=Math.max(0,Math.min(1,(o.y-.4)/.48));ctx.save();ctx.translate(o.x*W,o.y*H);ctx.fillStyle=`rgba(9,38,48,${.08+t*.16})`;ctx.beginPath();ctx.ellipse(0,25,10+t*20,2+t*5,0,0,Math.PI*2);ctx.fill();ctx.restore();ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(.22+t*.9,.22+t*.9);ctx.translate(-o.x*W,-o.y*H);drawGroundedObstacle(o);ctx.restore()};
let boostEnergy=20,boostTime=0,carveSide=0,carveTime=0,carveCooldown=0;
const boostFill=$('boostFill'),boostValue=$('boostValue');
function updateBoostUI(){boostFill.style.transform=`scaleX(${boostEnergy/100})`;boostValue.textContent=`${Math.round(boostEnergy)}%`;boostFill.style.background=boostEnergy>=35?'linear-gradient(90deg,#62d9f5,#ffd260)':'#597783'}
function activateBoost(){if(state!=='playing'||boostTime>0)return;if(boostEnergy<35){toast('BOOST CHARGING',`${Math.ceil(35-boostEnergy)}% NEEDED`);beep(130,.08);return}boostEnergy-=35;boostTime=2.2;game.classList.add('boosted');toast('MOUNTAIN BOOST','FULL SEND');burst(player.x*W,player.y*H+20,'#62d9f5',30);beep(760,.25,'sawtooth',.055);updateBoostUI()}
addEventListener('keydown',e=>{if(e.key==='x'||e.key==='X')activateBoost()});$('boostButton').addEventListener('pointerdown',e=>{e.preventDefault();activateBoost()});
const resetArcade=reset;reset=function(){resetArcade();boostEnergy=20;boostTime=0;carveSide=0;carveTime=0;carveCooldown=0;game.classList.remove('boosted');updateBoostUI()};
const scoreArcade=scorePop;scorePop=function(points,x,y,label){scoreArcade(points,x,y,label);boostEnergy=Math.min(100,boostEnergy+Math.min(14,Math.max(2,points/120)));updateBoostUI()};
const hitArcade=hitObstacle;hitObstacle=function(o,dx){if(o.type!=='boost'){hitArcade(o,dx);return}if(o.hit)return;o.hit=true;boostPickups++;recordChallenge('boost');boostEnergy=Math.min(100,boostEnergy+28);scorePop(250,o.x*W,o.y*H-12,'BOOST PICKUP');burst(o.x*W,o.y*H,'#63e7ff',25);toast('BOOST ENERGY','+28%');beep(920,.16);updateBoostUI()};
const clearArcade=clearObstacle;clearObstacle=function(o,dx){if(o.type==='boost'){hitObstacle(o,dx);return}clearArcade(o,dx)};
const spawnArcade=spawn;spawn=function(){if(Math.random()<.16){obstacles.push({type:'boost',x:.16+Math.random()*.68,y:.05,wobble:Math.random()*6.28,hit:false,cleared:false,side:1,laneV:0});return}spawnArcade()};
const drawArcadeObstacle=drawObstacle;drawObstacle=function(o){if(o.type!=='boost'){drawArcadeObstacle(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),x=o.x*W,y=o.y*H,scale=.25+t*.85,pulse=1+Math.sin(performance.now()*.008+o.wobble)*.12;ctx.save();ctx.translate(x,y);ctx.scale(scale*pulse,scale*pulse);const glow=ctx.createRadialGradient(0,0,2,0,0,34);glow.addColorStop(0,'rgba(255,238,139,.95)');glow.addColorStop(.45,'rgba(95,225,255,.72)');glow.addColorStop(1,'rgba(95,225,255,0)');ctx.fillStyle=glow;ctx.fillRect(-35,-35,70,70);ctx.rotate(Math.PI/4);ctx.fillStyle='#effcff';ctx.fillRect(-9,-9,18,18);ctx.fillStyle='#56dff8';ctx.fillRect(-5,-5,10,10);ctx.restore()};
const updateArcade=update;update=function(dt){updateArcade(dt);if(state!=='playing')return;carveCooldown=Math.max(0,carveCooldown-dt);const side=Math.abs(player.edge)>.62?Math.sign(player.edge):0;if(side&&side!==carveSide){if(carveSide&&carveTime>.38&&carveCooldown===0){const pts=180*combo;scorePop(pts,player.x*W,player.y*H-38,'CARVE LINK');combo=Math.min(8,combo+1);comboTime=3.2;bestCombo=Math.max(bestCombo,combo);carveCooldown=.35;toast('CARVE LINK',`+${pts}`);beep(620+combo*28,.08)}carveSide=side;carveTime=0}else if(side)carveTime+=dt;if(boostTime>0){boostTime-=dt;speed=Math.max(speed,3.85);score+=dt*75;particles.push({x:player.x*W+(Math.random()-.5)*28,y:player.y*H+28,vx:(Math.random()-.5)*70,vy:30+Math.random()*90,life:.25+Math.random()*.25,size:2+Math.random()*4,color:Math.random()>.5?'#62d9f5':'#ffd260'});if(boostTime<=0)game.classList.remove('boosted')}};
updateBoostUI();
populateRuns();
const cupButton=$('cupButton');$('cupDate').textContent=cupDate;$('cupRoute').textContent=`${ROUTES[cupRouteIndex].name} · ${RUNS[cupRouteIndex][cupRunIndex].name}`;$('cupDetails').textContent=`${RUNS[cupRouteIndex][cupRunIndex].difficulty} · ${RUNS[cupRouteIndex][cupRunIndex].length.toLocaleString()} M · same line for every rider`;
async function loadCupLeaderboard(){const board=$('cupLeaderboard');if(!board)return;try{const response=await fetch(`/v1/leaderboards/${cupRouteIndex}/${cupRunIndex}/freeride`,{headers:{accept:'application/json'},signal:AbortSignal.timeout?.(1400)});if(!response.ok)throw new Error('leaderboard unavailable');const data=await response.json();const items=(data.items||[]).slice(0,5);board.innerHTML=items.length?items.map((item,i)=>`<li><b>${i+1}. ${String(item.player||'RIDER').replace(/[<>&]/g,'')}</b><span>${Number(item.score||0).toLocaleString()}</span></li>`).join(''):'<li>Be the first name on the board.</li>'}catch{let local=0;try{local=Number(localStorage.getItem(`alpineCup:${cupKey}`)||0)}catch{}board.innerHTML=local?`<li><b>LOCAL BEST</b><span>${local.toLocaleString()}</span></li>`:'<li>Local event · scores saved here</li>'}}
cupButton?.addEventListener('click',()=>{routeIndex=cupRouteIndex;runIndex=cupRunIndex;document.querySelectorAll('.route-card').forEach((card,i)=>card.classList.toggle('selected',i===routeIndex));populateRuns();$('runSelect').value=String(runIndex);updatePreview();updateTrailStatus(routeIndex);syncTrailAccess();updateMountainEvent();loadSnowReport(routeIndex);loadCupLeaderboard();toast('DAILY CUP',`${ROUTES[routeIndex].name} · ${RUNS[routeIndex][runIndex].name}`)});loadCupLeaderboard();

/* Production gameplay layer: modes, records, controller input, audio and previews. */
let runStartedAt=0,padJump=false,padBoost=false,padPause=false,ghostTrace=[],ghostBest=null,ghostSampleAt=0;
function runId(){return`${routeIndex}:${runIndex}:${AlpinePro.settings.mode}`}
function updatePreview(){const run=RUNS[routeIndex][runIndex],route=ROUTES[routeIndex];if(!run)return;$('previewResort').textContent=route.name;$('previewRun').textContent=run.name.toUpperCase();$('previewDifficulty').textContent=run.difficulty;$('previewLength').textContent=AlpinePro.settings.units==='imperial'?`${Math.round(run.length*3.28084).toLocaleString()} FT`:`${run.length.toLocaleString()} M`;$('previewTerrain').textContent=run.terrain.toUpperCase();const forecast={auto:`FORECAST · ${route.weather.toUpperCase()}`,groomed:'FORECAST · FRESH GROOM',powder:'FORECAST · DEEP POWDER',ice:'FORECAST · HARD ICE',night:'FORECAST · NIGHT RIDE',storm:'FORECAST · STORM FRONT'}[AlpinePro.settings.condition||'auto'];if($('previewForecast'))$('previewForecast').textContent=forecast;AlpinePro.drawPreview($('trailPreview'),run,route);const record=AlpinePro.medals()[runId()];$('medalCase').querySelector('b').textContent=record?`${record.rank.toUpperCase()} · ${record.score.toLocaleString()}`:'NO MEDAL YET';paintMastery()}
function paintMastery(){const name=$('masteryName'),stats=$('masteryStats'),fill=$('masteryFill'),reward=$('masteryReward');if(!name)return;const medals=AlpinePro.medals(),runs=RUNS[routeIndex],medaled=new Set(Object.keys(medals).filter(key=>key.startsWith(`${routeIndex}:`)).map(key=>key.split(':')[1])),count=runs.filter((_,i)=>medaled.has(String(i))).length;name.textContent=ROUTES[routeIndex].name;stats.textContent=`${count} / ${runs.length} RUNS MEDALED`;if(fill)fill.style.width=`${count/runs.length*100}%`;if(reward)reward.textContent=count===runs.length?'RESORT MASTER · TITLE UNLOCKED':'MASTER EVERY LINE TO UNLOCK A TITLE'}
const populateProfessional=populateRuns;populateRuns=function(){populateProfessional();updatePreview()};$('runSelect').addEventListener('change',updatePreview);
$('modeSelect').value=AlpinePro.settings.mode;$('modeSelect').addEventListener('change',e=>{AlpinePro.setSetting('mode',e.target.value);updatePreview()});
const spawnModes=spawn;spawn=function(){spawnModes()};
const resetProfessional=reset;reset=function(){resetProfessional();runStartedAt=performance.now();ghostTrace=[];ghostSampleAt=0;try{ghostBest=JSON.parse(localStorage.getItem(`alpineGhost:${runId()}`)||'null')}catch{ghostBest=null}if(AlpinePro.settings.tutorial){const tips=AlpinePro.settings.mode==='park'?['RAMPS AND RAILS CHARGE TRICKS','HOLD Q OR E IN THE AIR','RELEASE TO LAND CLEAN']:AlpinePro.settings.mode==='slalom'?['THREAD THE GATES','CARVE EARLY FOR THE NEXT GATE','MISSED GATES COST SPEED']:['HOLD ← OR → TO CARVE','JUMP OBSTACLES FOR FLOW','X USES BOOST AT 35%'];tips.forEach((tip,i)=>setTimeout(()=>state==='playing'&&toast('PRO TIP',tip),1200+i*1900));AlpinePro.setSetting('tutorial',false)}};
const finishProfessional=finish;finish=function(){if(state==='over')return;const elapsed=Math.max(1,(performance.now()-runStartedAt)/1000),mode=AlpinePro.settings.mode;if(mode==='timetrial'){score+=Math.max(0,Math.round((courseLength/38-elapsed)*110));if(!ghostBest||elapsed<ghostBest.time){try{localStorage.setItem(`alpineGhost:${runId()}`,JSON.stringify({time:elapsed,trace:ghostTrace}))}catch{}}}else if(mode==='downhill'){score+=Math.round(speed*420);toast('DOWNHILL RESULT',`${Math.round(34+speed*19)} KM/H PEAK`)}else if(mode==='bigair'){score+=trickCount*650;toast('BIG AIR RESULT',`${trickCount} TRICKS LOGGED`)}else if(mode==='patrol'){score+=clears*120;toast('PATROL RESULT',`${clears} HAZARDS CLEARED`)}finishProfessional();const rank=AlpinePro.awardMedal(runId(),Math.floor(score),elapsed),setup=SETUPS[AlpinePro.settings.setup]||SETUPS.racer;const highlight=`${rank.toUpperCase()} LINE · ${ROUTES[routeIndex].name} / ${RUNS[routeIndex][runIndex].name} · ${Math.floor(score).toLocaleString()} PTS · ${bestCombo}× FLOW · ${setup.name}`;try{localStorage.setItem('alpineRushLastHighlight',highlight)}catch{}$('highlightText').textContent=highlight;$('resultTitle').textContent=`${rank.toUpperCase()} · ${RUNS[routeIndex][runIndex].name.toUpperCase()}`;updatePreview()};
const finishCup=finish;finish=function(){finishCup();if(routeIndex!==cupRouteIndex||runIndex!==cupRunIndex){$('cupResult').textContent='';return}const cupScore=Math.floor(score);let previous=0;try{previous=Number(localStorage.getItem(`alpineCup:${cupKey}`)||0);if(cupScore>previous)localStorage.setItem(`alpineCup:${cupKey}`,String(cupScore))}catch{}const bestCup=Math.max(previous,cupScore);$('cupResult').textContent=`DAILY CUP BEST · ${bestCup.toLocaleString()} PTS · ${cupScore>=bestCup?'NEW EVENT BEST':'KEEP PUSHING'}`};
const updateProfessional=update;
update=function(dt){
 const pad=AlpinePro.gamepad();
 if(pad){keys.left=pad.steer<-.14;keys.right=pad.steer>.14;if(pad.jump&&!padJump)jump();if(pad.boost&&!padBoost)activateBoost();if(pad.pause&&!padPause&&['playing','paused'].includes(state))togglePause();padJump=pad.jump;padBoost=pad.boost;padPause=pad.pause}
 updateProfessional(dt);
 if(state==='playing'){
  const condition=snowSurface();
  if(condition==='WHITEOUT')speed=Math.max(.72,speed-dt*.18);
  if(condition==='TRACKED POWDER'&&player.jump===0)speed=Math.max(.72,speed-dt*.06);
  const setup=SETUPS[AlpinePro.settings.setup]||SETUPS.racer;
  const upgrades=AlpinePro.upgrades();
  speed=Math.max(.72,Math.min(3.8,speed+upgrades.edge*.012+upgrades.float*(condition==='POWDER'||condition==='TRACKED POWDER'?.008:0)));
  if(player.jump>0&&upgrades.pop)player.jumpV+=upgrades.pop*18*dt;
  if((condition==='POWDER'||condition==='TRACKED POWDER')&&player.jump===0)speed*=Math.pow(setup.powder,dt);
  speed=Math.max(.72,Math.min(3.55,speed+setup.speed*dt));
  if(player.jump===0)player.vx*=Math.pow(setup.grip,dt);
  if(player.jump>0&&setup.pop)player.jumpV+=setup.pop*dt;
 }
 if(state==='playing'){
  const run=RUNS[routeIndex][runIndex],grounded=player.jump===0;
  if(grounded&&keys.backFlip)speed=Math.min(3.5,speed+dt*.72);
  if(grounded&&keys.frontFlip)speed=Math.max(.72,speed-dt*.85);
  if(run.terrain==='groomer')speed=Math.min(3.4,speed+dt*.08);
  if(['glades','aspen'].includes(run.terrain))speed*=Math.pow(.985,dt);
  if(run.terrain==='moguls'&&Math.random()<dt*7){player.compression=Math.min(1,player.compression+.12);shake=Math.max(shake,1.5)}
  const elapsed=(performance.now()-runStartedAt)/1000;
  if(elapsed>=ghostSampleAt){ghostTrace.push([+elapsed.toFixed(2),+player.x.toFixed(3),Math.round(distance)]);ghostSampleAt=elapsed+.14}
  const mode=AlpinePro.settings.mode;
  ui.objectiveText.textContent=mode==='timetrial'?'BEAT YOUR GHOST':mode==='downhill'?'MAX SPEED TO THE BOTTOM':mode==='slalom'?'CLEAR EVERY GATE':mode==='park'?'BEAT THE TRICK RIVAL':mode==='bigair'?'BEAT THE TRICK RIVAL':mode==='patrol'?'OUTRUN THE PATROL':dailyLabel;
  ui.objectiveProgress.textContent=mode==='timetrial'?`${elapsed.toFixed(1)} S`:mode==='downhill'?`${Math.round(34+speed*19)} KM/H`:mode==='slalom'?`${gatesCleared} GATES`:mode==='park'||mode==='bigair'?`${trickCount} / ${trickBattleTarget} TRICKS`:mode==='patrol'?`${clears} CLEARS`:`${Math.min(challengeProgress(),dailyTarget)} / ${dailyTarget}`;
  if(AlpinePro.settings.units==='imperial')ui.distance.textContent=`${Math.ceil(Math.max(0,courseLength-distance)*3.28084).toLocaleString()} FT`;
 }
 AlpinePro.setRideAudio(state==='playing',speed,player.edge)
};
const drawProfessional=draw;draw=function(){drawProfessional();if(state!=='playing'||AlpinePro.settings.mode!=='timetrial'||!ghostBest?.trace?.length)return;const elapsed=(performance.now()-runStartedAt)/1000,trace=ghostBest.trace;let point=trace.find(item=>item[0]>=elapsed)||trace[trace.length-1];const relative=(point[2]-distance)*.42,y=Math.max(H*.48,Math.min(H*.82,player.y*H-relative));ctx.save();ctx.globalAlpha=.35;drawSkier(point[1]*W,y,0,'#62d9f5',false);ctx.fillStyle='#8eeaff';ctx.textAlign='center';ctx.font='800 8px DM Sans';ctx.fillText('PERSONAL BEST',point[1]*W,y-38);ctx.restore()};
const updateUnits=updateUI;updateUI=function(){updateUnits();if(AlpinePro.settings.units==='imperial'){ui.speed.textContent=`${Math.round((34+speed*19)*.621371)} MPH`;if(state==='playing')ui.distance.textContent=`${Math.ceil(Math.max(0,courseLength-distance)*3.28084).toLocaleString()} FT`}};
$('fullscreenButton').onclick=()=>{if(document.fullscreenElement)document.exitFullscreen?.();else game.requestFullscreen?.()};
const setupSelect=$('setupSelect'),setupDescription=$('setupDescription');if(setupSelect){setupSelect.value=AlpinePro.settings.setup||'racer';const syncSetup=()=>{const setup=SETUPS[setupSelect.value]||SETUPS.racer;setupDescription.textContent=setup.description;AlpinePro.setSetting('setup',setupSelect.value)};setupSelect.addEventListener('change',syncSetup);syncSetup()}
const conditionSelect=$('conditionSelect');if(conditionSelect){conditionSelect.value=AlpinePro.settings.condition||'auto';conditionSelect.addEventListener('change',e=>{AlpinePro.setSetting('condition',e.target.value);toast('SNOW DAY',e.target.options[e.target.selectedIndex].text.toUpperCase());updatePreview()})}
const trickSpinSelect=$('trickSpin'),trickGrabSelect=$('trickGrab');[trickSpinSelect,trickGrabSelect].forEach(input=>{if(input){input.value=AlpinePro.settings[input.id]||'any';input.addEventListener('change',e=>AlpinePro.setSetting(input.id,e.target.value))}});
const musicSelect=$('musicSetting');if(musicSelect&&!musicSelect.querySelector('option[value="roadhouse"]'))[['roadhouse','The Roadhouse Rhythm'],['roadkill','Copperhead Roadkill']].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;musicSelect.appendChild(option)});
const musicQuickSelect=$('musicQuickSelect'),quickMusicLabel=$('quickMusicLabel');if(musicSelect&&musicQuickSelect){const quickLabels={random:'RANDOM ROTATION',copperhead:'THE COPPERHEAD STOMP',highway:'HIGHWAY FEVER',roadhouse:'THE ROADHOUSE RHYTHM',roadkill:'COPPERHEAD ROADKILL',tiroler:'TIROLER POLKA',ecstasy:'THE ECSTASY OF GOLD'};[['ecstasy','The Ecstasy of Gold']].forEach(([value,label])=>{if(!musicSelect.querySelector(`option[value="${value}"]`)){const option=document.createElement('option');option.value=value;option.textContent=label;musicSelect.appendChild(option)}if(!musicQuickSelect.querySelector(`option[value="${value}"]`)){const option=document.createElement('option');option.value=value;option.textContent=label;musicQuickSelect.appendChild(option)}});const syncQuickMusic=()=>{musicQuickSelect.value=musicSelect.value;quickMusicLabel.textContent=quickLabels[musicSelect.value]||'RANDOM ROTATION'};musicQuickSelect.value=musicSelect.value;musicQuickSelect.addEventListener('input',()=>{musicSelect.value=musicQuickSelect.value;musicSelect.dispatchEvent(new Event('input',{bubbles:true}));syncQuickMusic()});musicSelect.addEventListener('input',syncQuickMusic);syncQuickMusic()}
introMusic&&$('soundButton').addEventListener('click',()=>{introMusic.muted=!sound});
const shareResultButton=$('shareResultButton');shareResultButton?.addEventListener('click',async()=>{const text=$('highlightText')?.textContent||'Alpine Rush line';try{if(navigator.share)await navigator.share({title:'Alpine Rush line',text});else await navigator.clipboard.writeText(text);toast('LINE COPIED','SHARE YOUR RUN')}catch{} });
const photoModeButton=$('photoModeButton'),photoDialog=$('photoDialog'),photoCanvas=$('photoCanvas');photoModeButton?.addEventListener('click',()=>{if(!photoCanvas)return;const pctx=photoCanvas.getContext('2d');pctx.clearRect(0,0,photoCanvas.width,photoCanvas.height);pctx.drawImage(canvas,0,0,photoCanvas.width,photoCanvas.height);pctx.fillStyle='rgba(4,17,25,.72)';pctx.fillRect(24,photoCanvas.height-76,photoCanvas.width-48,50);pctx.fillStyle='#fff';pctx.font='900 22px Barlow Condensed';pctx.fillText(`${ROUTES[routeIndex].name} · ${RUNS[routeIndex][runIndex].name}`.toUpperCase(),42,photoCanvas.height-44);pctx.fillStyle='#ffd260';pctx.font='700 13px DM Sans';pctx.fillText(`${Math.floor(score).toLocaleString()} PTS · ${bestCombo}× FLOW · ${riderType==='snowboarder'?'SNOWBOARD':'SKIS'}`,42,photoCanvas.height-25);photoDialog?.showModal()});$('downloadPhotoButton')?.addEventListener('click',()=>{if(!photoCanvas)return;const link=document.createElement('a');link.download=`alpine-rush-${ROUTES[routeIndex].name.toLowerCase().replaceAll(' ','-')}.png`;link.href=photoCanvas.toDataURL('image/png');link.click()});
const dialog=$('settingsDialog'),settingInputs={quality:$('qualitySetting'),motion:$('motionSetting'),contrast:$('contrastSetting'),volume:$('volumeSetting'),music:$('musicSetting'),units:$('unitsSetting')};Object.entries(settingInputs).forEach(([name,input])=>{if(!input)return;input.value=AlpinePro.settings[name]||input.value;if(name==='contrast')document.documentElement.dataset.contrast=input.value;input.addEventListener('input',()=>{AlpinePro.setSetting(name,input.value);if(name==='contrast')document.documentElement.dataset.contrast=input.value;if(name==='music'){activeMusicId=null;if(sound&&['playing','countdown','select'].includes(state))setMusic(true)}if(name==='units'){updatePreview();updateUI();loadSnowReport(routeIndex)}})});$('settingsButton').onclick=()=>dialog.showModal();dialog.addEventListener('close',()=>AlpinePro.ensureAudio());
addEventListener('pointerdown',()=>{AlpinePro.ensureAudio();if(state==='title')setIntroMusic(true);else setMusic(true)},{once:true});addEventListener('keydown',()=>{if(state==='title')setIntroMusic(true);else setMusic(true)},{once:true});updatePreview();
const drawWeatherLayer=draw;draw=function(){drawWeatherLayer();if(state!=='playing'||snowSurface()!=='WHITEOUT')return;ctx.save();const veil=ctx.createLinearGradient(0,0,0,H);veil.addColorStop(0,'rgba(232,248,255,.08)');veil.addColorStop(.48,'rgba(236,249,255,.2)');veil.addColorStop(1,'rgba(236,249,255,.42)');ctx.fillStyle=veil;ctx.fillRect(0,0,W,H);ctx.fillStyle='rgba(255,255,255,.75)';ctx.font='800 9px DM Sans';ctx.textAlign='center';ctx.fillText('WHITEOUT · FIND THE FALL LINE',W*.5,H*.2);ctx.restore()};
const updateUIFrame=updateUI;let uiPaintAt=0;updateUI=function(){const now=performance.now();if(state==='playing'&&now-uiPaintAt<80)return;uiPaintAt=now;updateUIFrame()};
const patrolSpawn=spawn;spawn=function(){const before=obstacles.length;patrolSpawn();for(let i=before;i<obstacles.length;i++){const o=obstacles[i];if(o.type==='speedPatrol')o.laneV=(Math.random()>.5?1:-1)*(.07+Math.random()*.035);if(o.type==='skiPatrol')o.laneV=(Math.random()>.5?1:-1)*(.018+Math.random()*.012)}};
const patrolUpdate=update;update=function(dt){patrolUpdate(dt);if(state!=='playing')return;obstacles.forEach(o=>{if(!['speedPatrol','skiPatrol'].includes(o.type))return;o.x+=o.laneV*dt*speed;if(o.x<.1||o.x>.9){o.x=Math.max(.1,Math.min(.9,o.x));o.laneV*=-1}})};
const patrolDraw=drawObstacle;drawObstacle=function(o){if(!['speedPatrol','skiPatrol'].includes(o.type)){patrolDraw(o);return}if(o.y<.4)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.22+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);ctx.fillStyle=o.type==='speedPatrol'?'#e84d3d':'#2e8f9f';ctx.fillRect(-17,-31,34,8);ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText(o.type==='speedPatrol'?'SPEED PATROL':'SKI PATROL',0,-25);drawSkier(0,0,Math.sin(o.wobble+o.y*9)*.16,o.type==='speedPatrol'?'#e84d3d':'#2e8f9f',false);ctx.restore()};
const drawDesignBase=drawRiderDetails;drawRiderDetails=function(){drawDesignBase();ctx.save();ctx.strokeStyle=GOGGLES[goggleIndex]||GOGGLES[0];ctx.lineWidth=2.8;ctx.beginPath();ctx.moveTo(-7,-22);ctx.lineTo(7,-22);ctx.stroke();ctx.strokeStyle=SKINS[skinIndex].b;ctx.lineWidth=2;if(styleIndex===1){ctx.beginPath();ctx.moveTo(-9,-7);ctx.lineTo(9,9);ctx.stroke()}else if(styleIndex===2){ctx.beginPath();ctx.moveTo(0,-10);ctx.lineTo(0,12);ctx.stroke();ctx.strokeStyle='rgba(255,255,255,.65)';ctx.beginPath();ctx.moveTo(-8,0);ctx.lineTo(8,0);ctx.stroke()}ctx.restore()};
const shortcutHit=hitObstacle;hitObstacle=function(o,dx){if(o.type==='shortcut'){o.cleared=true;const points=650*combo;score+=points;speed=Math.min(3.9,speed+.55);combo=Math.min(8,combo+2);comboTime=3.8;bestCombo=Math.max(bestCombo,combo);scorePop(points,o.x*W,o.y*H,'SHORTCUT SEND');toast('SECRET LINE',`+${points}`);burst(o.x*W,o.y*H,'#ffd260',30);beep(1040,.2);return}if(o.type==='patch'){o.cleared=true;const points=900*combo;score+=points;combo=Math.min(8,combo+1);comboTime=4.2;bestCombo=Math.max(bestCombo,combo);scorePop(points,o.x*W,o.y*H,'PATCH FOUND');toast('MOUNTAIN PATCH',`+${points}`);burst(o.x*W,o.y*H,'#69e1f5',34);beep(1180,.22);return}shortcutHit(o,dx)};
const shortcutClear=clearObstacle;clearObstacle=function(o,dx){if(o.type==='shortcut'||o.type==='patch'){return hitObstacle(o,dx)}shortcutClear(o,dx)};
const shortcutDraw=drawObstacle;drawObstacle=function(o){if(o.type!=='shortcut'&&o.type!=='patch'){shortcutDraw(o);return}if(o.y<.4)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.22+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);if(o.type==='shortcut'){ctx.strokeStyle='#ffd260';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-27,22);ctx.lineTo(-27,-24);ctx.quadraticCurveTo(0,-45,27,-24);ctx.lineTo(27,22);ctx.stroke();ctx.fillStyle='#142832';ctx.fillRect(-23,-17,46,13);ctx.fillStyle='#ffd260';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('SHORTCUT',0,-8)}else{ctx.fillStyle='#69e1f5';ctx.beginPath();ctx.arc(0,0,24,0,Math.PI*2);ctx.fill();ctx.fillStyle='#102a38';ctx.font='900 10px DM Sans';ctx.textAlign='center';ctx.fillText('PATCH',0,4);ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.stroke()}ctx.restore()};
const lossMessages=['YOU POSER!','THE MOUNTAIN WINS THIS ROUND.','THAT WAS MORE SIDE-SLIP THAN SEND.','APRES BEFORE PROGRESS.','SKI PATROL HAS ENTERED THE CHAT.','YOUR EDGES NEED A TALK.','SEND IT? MORE LIKE BEND IT.','BUN RUN DENIED.','POWDER DAY, PUZZLE PERFORMANCE.','THE LIFT LINE IS ALREADY LAUGHING.'];
const snowHumor={lift:['LIFT LINE TIP: Pretend you meant to ski that slow.','CHAIR 3 IS LOADING… PLEASE KEEP YOUR POLES INSIDE THE DRAMA.','WELCOME UPHILL. PLEASE EXIT WITH STYLE.','GOGGLE CHECK: yes, they are still fogging.'],patrol:['PATROL RADIO: We saw that. Everybody saw that.','PATROL SAYS: helmet on, ego optional.','THAT WAS A TECHNICAL LINE. VERY TECHNICAL.','PATROL CLEARANCE GRANTED… EMOTIONALLY, NOT LEGALLY.'],weather:['FLAT LIGHT: the mountain has enabled stealth mode.','WIND GUST: excellent technique, terrible timing.','POWDER BURST: snow is just cold confetti.','ICE PATCH: congratulations, you found the mountain’s banana peel.'],run:['CROWD UPDATE: one rider, zero brakes, unlimited confidence.','STYLE CHECK: aggressively enthusiastic.','THAT LINE HAD EVERYTHING EXCEPT A PLAN.','YOU ARE NOW ENTERING THE “HOPE THIS WORKS” ZONE.','LOCAL TIP: point downhill and look expensive.']};
const finishLoss=finish;finish=function(){const lost=lives<=0&&!courseComplete;finishLoss();$('resultsScreen').classList.toggle('loss-state',lost);if(lost){$('resultKicker').innerHTML='<span></span> RUN OVER';$('resultTitle').textContent=lossMessages[Math.floor(Math.random()*lossMessages.length)];$('highlightText').textContent='Back to the bun run honeybun';$('cupResult').textContent='Shake it off and drop back in.'}else $('resultKicker').innerHTML='<span></span> RUN COMPLETE'};
const mobileBudgetUpdate=update;update=function(dt){mobileBudgetUpdate(dt);if(mobileDevice){if(particles.length>120)particles.splice(0,particles.length-120);if(trails.length>70)trails.splice(0,trails.length-70)}};

/* Character designer preview: render the exact live loadout beside the controls. */
function updateCharacterPreview(){
  const preview=$('characterPreview');if(!preview)return;
  const previewCtx=preview.getContext('2d'),previousCtx=ctx,previousW=W,previousH=H;
  const pw=220,ph=220;preview.width=pw;preview.height=ph;ctx=previewCtx;W=pw;H=ph;
  previewCtx.setTransform(1,0,0,1,0,0);previewCtx.clearRect(0,0,pw,ph);
  const glow=previewCtx.createRadialGradient(pw*.5,ph*.3,4,pw*.5,ph*.42,pw*.62);glow.addColorStop(0,'rgba(255,226,143,.3)');glow.addColorStop(1,'rgba(255,255,255,0)');previewCtx.fillStyle=glow;previewCtx.fillRect(0,0,pw,ph);
  previewCtx.save();previewCtx.translate(pw*.5,ph*.59);previewCtx.scale(2.18,2.18);
  const snapshot={jump:player.jump,edge:player.edge,compression:player.compression,tilt:player.tilt,anim:player.anim};Object.assign(player,{jump:0,edge:0,compression:.18,tilt:0,anim:1.2});
  drawRider();Object.assign(player,snapshot);previewCtx.restore();
  ctx=previousCtx;W=previousW;H=previousH;
  const skinNames=['CORAL','CYAN','GOLD'],helmetNames=['NIGHT','ICE','FLAME'],goggleNames=['ICE','GOLD','ROSE'];
  $('characterPreviewName').textContent=riderType==='snowboarder'?'SNOWBOARDER':'SKIER';$('characterPreviewDetails').textContent=`${skinNames[skinIndex]||'CORAL'} · ${helmetNames[helmetIndex]||'NIGHT'} · ${goggleNames[goggleIndex]||'ICE'}`;
}
document.querySelectorAll('[data-rider],[data-helmet],[data-goggle],[data-style],.swatches button').forEach(button=>button.addEventListener('click',()=>requestAnimationFrame(updateCharacterPreview)));
requestAnimationFrame(updateCharacterPreview);

/* Rival riders: named, moving targets make a run feel shared instead of empty. */
let rivalPasses=0;
const rivalNames=['MAYA','KAI','LUCA','NOVA'];
const rivalSpawn=spawn;
spawn=function(){
  const before=obstacles.length;
  rivalSpawn();
  if(state==='playing'&&spawnCount>2&&spawnCount%5===0&&Math.random()<.72){
    const ahead=Math.min(1,(distance+courseLength*.14)/courseLength),sample=courseSample(ahead);
    const lane=Math.max(.08,sample.width*.56),x=Math.max(.09,Math.min(.91,sample.center+(Math.random()-.5)*lane));
    const personality=['BLOCKER','SPEED DEMON','TRICK HOUND'][Math.floor(Math.random()*3)];obstacles.push({type:'rival',x,y:.03,wobble:Math.random()*6.28,hit:false,cleared:false,color:['#f15b4f','#9b73ff','#f0b84c','#42c9b8'][Math.floor(Math.random()*4)],side:x<sample.center?-1:1,laneV:(Math.random()>.5?1:-1)*(.012+Math.random()*.018),name:rivalNames[Math.floor(Math.random()*rivalNames.length)],personality,pace:personality==='SPEED DEMON'?1.12:personality==='BLOCKER'?.72:.9});
  }
};

/* Dynamic resort hazards and changing weather. */
let weatherState='CLEAR',weatherTimer=7,weatherPulse=0;
const resortHazardSpawn=spawn;
spawn=function(){
  resortHazardSpawn();
  if(state!=='playing'||spawnCount<6||spawnCount%13!==0)return;
  const types=['wildlife','snowcat','liftTower','fallenSign','crowd','rescue'],type=types[Math.floor(Math.random()*types.length)],s=courseSample(Math.min(1,(distance+courseLength*.13)/courseLength));
  obstacles.push({type,x:Math.max(.08,Math.min(.92,s.center+(Math.random()-.5)*s.width*.72)),y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:type==='wildlife'?'#9a6b46':type==='snowcat'?'#e7b84d':type==='rescue'?'#55c9dc':'#e65d49',side:Math.random()>.5?1:-1,laneV:type==='wildlife'?(Math.random()>.5?1:-1)*.06:type==='crowd'?(Math.random()>.5?1:-1)*.025:0});
};
const resortHazardHit=hitObstacle;
 hitObstacle=function(o,dx){if(['wildlife','snowcat','liftTower','fallenSign','crowd'].includes(o.type)){if(o.hit)return;o.hit=true;crashPlayer(o.type==='wildlife'?'WILDLIFE CROSSING':o.type==='snowcat'?'SNOWCAT BLOCK':'PISTE OBSTACLE');return}if(o.type==='rescue'){if(o.hit)return;o.hit=true;o.cleared=true;rescueFinds++;recordChallenge('rescue');window.AlpineRushReplay?.mark('RESCUE COMPLETE');scorePop(500,o.x*W,o.y*H,'RESCUE COMPLETE');toast('RESCUE COMPLETE','YOU SAVED THE RIDER');beep(920,.16);return}resortHazardHit(o,dx)};
const resortHazardUpdate=update;
update=function(dt){
  resortHazardUpdate(dt);if(state!=='playing')return;
  obstacles.forEach(o=>{if(!['wildlife','crowd'].includes(o.type)||o.hit)return;o.x+=o.laneV*dt*speed;if(o.x<.08||o.x>.92){o.x=Math.max(.08,Math.min(.92,o.x));o.laneV*=-1}});
  weatherTimer-=dt;if(weatherTimer<=0){const events=['WIND GUST','FLAT LIGHT','ICE PATCH','POWDER BURST'];weatherState=events[Math.floor(Math.random()*events.length)];weatherPulse=5.2;weatherTimer=10+Math.random()*9;window.AlpineRushReplay?.mark(weatherState);toast(weatherState,weatherState==='WIND GUST'?'HOLD YOUR EDGE':weatherState==='FLAT LIGHT'?'FIND THE FALL LINE':weatherState==='ICE PATCH'?'EASY ON THE CARVE':'FLOAT THE LANDING')}
  if(weatherPulse>0){weatherPulse-=dt;if(weatherState==='WIND GUST'){player.vx+=(Math.sin(performance.now()*.004)*.34)*dt;cameraRoll+=(Math.sin(performance.now()*.004)*.006-cameraRoll)*dt*3}if(weatherState==='ICE PATCH'&&player.jump===0){speed=Math.min(3.65,speed+dt*.12);player.vx*=Math.pow(.32,dt)}if(weatherState==='POWDER BURST'&&player.jump===0){speed=Math.max(.72,speed-dt*.05)}}else weatherState='CLEAR';
};
const resortHazardDraw=drawObstacle;
drawObstacle=function(o){
  if(!['wildlife','snowcat','liftTower','fallenSign','crowd','rescue'].includes(o.type)){resortHazardDraw(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);
  if(o.type==='wildlife'){ctx.fillStyle='#855b3e';ctx.beginPath();ctx.ellipse(0,4,19,10,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#6f4933';ctx.beginPath();ctx.arc(-16,-5,8,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#6f4933';ctx.lineWidth=3;for(const leg of [-10,5]){ctx.beginPath();ctx.moveTo(leg,9);ctx.lineTo(leg-3,22);ctx.stroke()}ctx.fillStyle='#fff';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText('WILDLIFE',0,-18)}
  else if(o.type==='snowcat'){ctx.fillStyle='#e1ad3e';ctx.fillRect(-29,-13,58,26);ctx.fillStyle='#375361';ctx.fillRect(-10,-25,22,13);ctx.strokeStyle='#263f4c';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-31,15);ctx.lineTo(31,15);ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText('SNOWCAT',0,-31)}
  else if(o.type==='liftTower'){ctx.strokeStyle='#394f5a';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(0,-42);ctx.lineTo(0,23);ctx.moveTo(-18,-32);ctx.lineTo(18,-32);ctx.stroke();ctx.fillStyle='#d95345';ctx.beginPath();ctx.arc(-20,-20,6,0,Math.PI*2);ctx.arc(20,-20,6,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('LIFT TOWER',0,35)}
  else if(o.type==='fallenSign'){ctx.rotate(o.side*.28);ctx.fillStyle='#e85e43';ctx.fillRect(-31,-7,62,14);ctx.fillStyle='#fff';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText('SLOW',0,3);ctx.strokeStyle='#394f5a';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(0,7);ctx.lineTo(12,25);ctx.stroke()}
  else if(o.type==='rescue'){ctx.fillStyle='#55c9dc';ctx.beginPath();ctx.arc(0,0,23,0,Math.PI*2);ctx.fill();ctx.fillStyle='#123344';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText('RESCUE',0,3);ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.stroke()}
  else{for(let i=-2;i<=2;i++){ctx.fillStyle=i%2?'#e85e43':'#45b6c6';ctx.beginPath();ctx.arc(i*10,Math.sin(o.wobble+i)*4,7,0,Math.PI*2);ctx.fill()}ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('CROWD',0,-17)}ctx.restore();
};
const weatherReset=reset;reset=function(){weatherReset();weatherState='CLEAR';weatherTimer=7;weatherPulse=0};
const weatherDraw=draw;draw=function(){weatherDraw();if(state!=='playing'||weatherPulse<=0)return;ctx.save();if(weatherState==='FLAT LIGHT'){ctx.fillStyle='rgba(238,248,252,.28)';ctx.fillRect(0,0,W,H)}if(weatherState==='WIND GUST'){ctx.strokeStyle='rgba(255,255,255,.34)';ctx.lineWidth=2;for(let i=0;i<7;i++){const y=H*(.28+i*.09),x=((performance.now()*.12+i*97)%W);ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x-45,y+5);ctx.stroke()}}if(weatherState==='POWDER BURST'){ctx.fillStyle='rgba(255,255,255,.22)';ctx.fillRect(0,H*.4,W,H*.5)}ctx.restore()};
const timelineFinish=finish;finish=function(){timelineFinish();const replay=window.AlpineRushReplay?.latest?.(),line=$('replayTimeline');if(line){const markers=replay?.markers||[];line.textContent=markers.length?`RUN TIMELINE · ${markers.slice(-5).map(m=>`${(m[0]/1000).toFixed(0)}s ${m[1]}`).join(' · ')}`:'RUN TIMELINE · No highlights recorded yet'}};

const contractClear=clearObstacle;
clearObstacle=function(o,dx){if(['speedPatrol','skiPatrol'].includes(o.type))patrolClears++;contractClear(o,dx)};
function paintContract(){const c=AlpinePro.contract(),label=$('contractLabel'),progress=$('contractProgress');if(label)label.textContent=c.label;if(progress)progress.textContent=`${c.done?c.target:Math.min(c.target,0)} / ${c.target}`}
paintContract();
let contractAwarded=false;
const contractReset=reset;reset=function(){contractReset();contractAwarded=false};
const contractFinish=finish;finish=function(){
  if(!contractAwarded){contractAwarded=true;const c=AlpinePro.submitContract({trickCount,riskLines,patrolClears,downhill:AlpinePro.settings.mode==='downhill'?1:0});if(c.completed){score+=c.reward;toast('SPONSOR CONTRACT',`+${c.reward} PTS`)}const result=$('contractResult');if(result)result.textContent=c.completed?`CONTRACT COMPLETE · +${c.reward} PTS`:c.done?'CONTRACT COMPLETE · KEEP PUSHING':`CONTRACT: ${c.label}`;}
  contractFinish();
};

/* Personality and exploration layer. */
const rivalBanter={overtake:['CLEAN LINE!','HEY, WAIT UP!','THAT WAS FAST.','OKAY, HOTSHOT.'],contact:['WATCH YOUR EDGES!','HEY! MY LINE!','SKI PATROL!','NO FIS RULES IN HERE!']};
const banterClear=clearObstacle;clearObstacle=function(o,dx){if(o.type==='rival'){toast(rivalBanter.overtake[Math.floor(Math.random()*rivalBanter.overtake.length)],`${o.name} · OVERTAKEN`)}banterClear(o,dx)};
const banterHit=hitObstacle;hitObstacle=function(o,dx){if(o.type==='rival'){toast(rivalBanter.contact[Math.floor(Math.random()*rivalBanter.contact.length)],`${o.name} · CONTACT`)}banterHit(o,dx)};
const stashSpawn=spawn;spawn=function(){
  stashSpawn();
  if(state==='playing'&&spawnCount>5&&spawnCount%11===0&&distance<courseLength*.9){const s=courseSample(Math.min(1,(distance+courseLength*.16)/courseLength));obstacles.push({type:'stash',x:Math.max(.1,Math.min(.9,s.center+(Math.random()-.5)*s.width*.6)),y:.03,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#ffd260',side:1,laneV:0})}
};
const stashHit=hitObstacle;hitObstacle=function(o,dx){if(o.type==='stash'){if(o.hit)return;o.hit=true;o.cleared=true;stashFinds++;recordChallenge('stash');const points=1100*combo;scorePop(points,o.x*W,o.y*H-15,'SECRET STASH');combo=Math.min(8,combo+2);comboTime=4.2;bestCombo=Math.max(bestCombo,combo);toast('MOUNTAIN EASTER EGG',`+${points} · FOUND IT`);burst(o.x*W,o.y*H,'#ffd260',34);beep(1200,.22);return}stashHit(o,dx)};
const stashClear=clearObstacle;clearObstacle=function(o,dx){if(o.type==='stash'){hitObstacle(o,dx);return}stashClear(o,dx)};
const stashDraw=drawObstacle;drawObstacle=function(o){
  if(o.type!=='stash'){stashDraw(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.9,pulse=1+Math.sin(performance.now()*.007+o.wobble)*.12;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale*pulse,scale*pulse);const g=ctx.createRadialGradient(0,0,2,0,0,34);g.addColorStop(0,'rgba(255,241,156,.95)');g.addColorStop(1,'rgba(255,210,96,0)');ctx.fillStyle=g;ctx.fillRect(-34,-34,68,68);ctx.fillStyle='#ffd260';ctx.rotate(Math.PI/4);ctx.fillRect(-12,-12,24,24);ctx.rotate(-Math.PI/4);ctx.fillStyle='#152b37';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('SECRET',0,3);ctx.restore()};
let battleReported=false;
const battleReset=reset;reset=function(){battleReset();battleReported=false};
const battleFinish=finish;finish=function(){
  if(!battleReported){battleReported=true;const mode=AlpinePro.settings.mode,elapsed=Math.max(1,(performance.now()-runStartedAt)/1000),result=$('battleResult');let message='';
    if(mode==='park'||mode==='bigair'){message=trickCount>=trickBattleTarget?`TRICK BATTLE WON · ${trickCount} LANDED`:`TRICK BATTLE LOST · RIVAL LANDED ${trickBattleTarget}`}
    else if(mode==='downhill'||mode==='timetrial'){const rivalTime=courseLength/(mode==='downhill'?43:40),gap=elapsed-rivalTime;message=Math.abs(gap)<.45?'PHOTO FINISH · TOO CLOSE TO CALL':gap<0?'PHOTO FINISH · YOU WIN':'PHOTO FINISH · RIVAL TAKES IT'}
    if(result&&message)result.textContent=message;
  }
  battleFinish();
};
function paintBadges(){const earned=AlpinePro.badges(),count=Object.keys(earned).length,total=AlpinePro.badgeDefinitions.length;if($('badgeCount'))$('badgeCount').textContent=`${count} / ${total} EARNED`;if($('badgeNames'))$('badgeNames').textContent=count?Object.values(earned).slice(-3).map(b=>b.name).join(' · '):'FIRST LINE AWAITS'}
paintBadges();
function paintModifier(){const m=AlpinePro.weeklyModifier();if($('modifierName'))$('modifierName').textContent=m.name;if($('modifierDescription'))$('modifierDescription').textContent=m.description}paintModifier();
async function loadWeeklyLeaderboard(){const board=$('weeklyLeaderboard');if(!board)return;const mode=AlpinePro.settings.mode||'freeride';$('weeklyBoardTitle').textContent=`${ROUTES[routeIndex].name} · ${RUNS[routeIndex][runIndex].name}`;$('weeklyBoardStatus').textContent=`${AlpinePro.weeklyModifier().name} · ${mode.toUpperCase()}`;board.innerHTML='<li>Loading online scores…</li>';try{const response=await fetch(`/v1/leaderboards/${routeIndex}/${runIndex}/${mode}`,{headers:{accept:'application/json'},signal:AbortSignal.timeout?.(1600)});if(!response.ok)throw new Error('offline');const data=await response.json(),items=(data.items||[]).slice(0,5);board.innerHTML=items.length?items.map((item,i)=>`<li><b>${i+1}. ${String(item.player||'RIDER').replace(/[<>&]/g,'')}</b><span>${Number(item.score||0).toLocaleString()}</span></li>`).join(''):'<li>Be the first rider on this line.</li>'}catch{board.innerHTML='<li>Online board unavailable · ride locally</li>'}}
document.getElementById('refreshWeeklyButton')?.addEventListener('click',loadWeeklyLeaderboard);document.getElementById('runSelect')?.addEventListener('change',loadWeeklyLeaderboard);document.getElementById('modeSelect')?.addEventListener('change',loadWeeklyLeaderboard);loadWeeklyLeaderboard();
function paintUpgrades(){const u=AlpinePro.upgrades();document.querySelectorAll('[data-upgrade]').forEach(button=>{button.querySelector('b').textContent=u[button.dataset.upgrade]||0})}
document.querySelectorAll('[data-upgrade]').forEach(button=>button.addEventListener('click',()=>{AlpinePro.buyUpgrade(button.dataset.upgrade);paintUpgrades();toast('EQUIPMENT UPGRADE',`${button.dataset.upgrade.toUpperCase()} TUNED`)}));paintUpgrades();
let badgesReported=false;
const badgesReset=reset;reset=function(){badgesReset();badgesReported=false};
const badgesFinish=finish;finish=function(){
  if(!badgesReported){badgesReported=true;const earned=AlpinePro.awardBadges({runs:AlpinePro.career().runs+1,trickCount,riskLines,patrolClears,stashFinds,plateFinds,mode:AlpinePro.settings.mode,routeIndex}),result=$('badgeResult');paintBadges();if(result&&earned.newBadges.length)result.textContent=`BADGE EARNED · ${earned.newBadges.join(' · ')}`;}
  badgesFinish();
};
const rivalReset=reset;
reset=function(){rivalReset();rivalPasses=0};
const rivalHit=hitObstacle;
hitObstacle=function(o,dx){
  if(o.type==='rival'){
    if(o.hit)return;o.hit=true;o.cleared=true;speed=Math.max(.72,speed-.26);combo=1;comboTime=0;shake=Math.max(shake,5);toast('RIDER CONTACT','GIVE THEM ROOM');beep(150,.12,'sawtooth',.04);return;
  }
  rivalHit(o,dx);
};
const rivalClear=clearObstacle;
clearObstacle=function(o,dx){
  if(o.type==='rival'){
    if(o.cleared)return;o.cleared=true;rivalPasses++;recordChallenge('rival');recordRival(o.name);const points=300*combo;scorePop(points,o.x*W,o.y*H,'OVERTAKE');combo=Math.min(8,combo+1);comboTime=3.4;bestCombo=Math.max(bestCombo,combo);toast('CLEAN OVERTAKE',`${o.name} · +${points}`);burst(o.x*W,o.y*H,'#ffd260',16);beep(860,.12);return;
  }
  rivalClear(o,dx);
};
const rivalUpdate=update;
update=function(dt){
  rivalUpdate(dt);
  if(state!=='playing')return;
  if(AlpinePro.weeklyModifier().id==='all-patrol'&&spawnCount%4===0)obstacles.push({type:Math.random()>.5?'speedPatrol':'skiPatrol',x:.18+Math.random()*.64,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#e84d3d',side:1,laneV:.06});
  obstacles.forEach(o=>{if(o.type!=='rival'||o.hit)return;const gap=player.x-o.x;if(o.personality==='BLOCKER'&&Math.abs(gap)<.28)o.laneV+=(Math.sign(gap)*.045-o.laneV)*dt*2.4;if(o.personality==='SPEED DEMON'&&o.y>player.y-.04)o.y-=dt*.035;if(o.personality==='TRICK HOUND'&&o.y>player.y-.12&&o.y<player.y+.04&&player.jump>0){o.wobble+=dt*8;if(!o.taunted){o.taunted=true;toast('SIGNATURE TRICK',`${o.name} · ${riderType==='snowboarder'?'BOARD':'SKI'} SPIN`);}}o.x+=o.laneV*dt*speed;if(o.x<.11||o.x>.89){o.x=Math.max(.11,Math.min(.89,o.x));o.laneV*=-1}o.y+=dt*.018*(o.pace-1)});
};
const rivalDraw=drawObstacle;
drawObstacle=function(o){
  if(o.type!=='rival'){rivalDraw(o);return}
  if(o.y<.4||o.hit)return;
  const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.92;
  ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);
  ctx.fillStyle='rgba(8,31,41,.25)';ctx.beginPath();ctx.ellipse(0,31,24,5,0,0,Math.PI*2);ctx.fill();
  drawSkier(0,0,Math.sin(o.wobble+o.y*9)*.18,o.color,false);
  ctx.fillStyle='rgba(7,27,39,.88)';ctx.fillRect(-34,-53,68,17);ctx.fillStyle='#fff';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText(`${o.name} · ${o.personality||'RIVAL'}`,0,-42);
  ctx.restore();
};

/* Branching piste lines: choose the safe groomer or the risky shortcut. */
let lineChoices=0;
const forkReset=reset;
reset=function(){forkReset();lineChoices=0};
const forkSpawn=spawn;
spawn=function(){
  forkSpawn();
  if(state!=='playing'||spawnCount<4||spawnCount%8!==0||distance>courseLength*.84)return;
  const ahead=Math.min(1,(distance+courseLength*.14)/courseLength),sample=courseSample(ahead),gap=Math.min(.15,Math.max(.09,sample.width*.46)),id=`fork-${spawnCount}`,riskSide=Math.random()>.5?1:-1;
  for(const side of [-1,1])obstacles.push({type:'fork',forkId:id,side,x:Math.max(.09,Math.min(.91,sample.center+side*gap)),center:sample.center,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,risk:side===riskSide,color:side===riskSide?'#ffd260':'#62d9f5'});
};
function resolveFork(o){
  if(o.cleared)return;
  o.cleared=true;obstacles.forEach(other=>{if(other.type==='fork'&&other.forkId===o.forkId){other.cleared=true;other.hit=true}});lineChoices++;recordChallenge('line');if(o.risk)riskLines++;
  const points=(o.risk?850:260)*combo;scorePop(points,o.x*W,o.y*H-12,o.risk?'RISKY LINE':'CLEAN LINE');combo=Math.min(8,combo+(o.risk?2:1));comboTime=3.8;bestCombo=Math.max(bestCombo,combo);
  if(o.risk){speed=Math.min(3.9,speed+.38);toast('SHORTCUT SEND',`RISK LINE · +${points}`);burst(o.x*W,o.y*H,'#ffd260',25);beep(1040,.18)}else{toast('LINE CHOICE',`GROOMER · +${points}`);burst(o.x*W,o.y*H,'#62d9f5',16);beep(760,.12)}
}
const forkHit=hitObstacle;
hitObstacle=function(o,dx){if(o.type==='fork'){resolveFork(o);return}forkHit(o,dx)};
const forkClear=clearObstacle;
clearObstacle=function(o,dx){if(o.type==='fork'){resolveFork(o);return}forkClear(o,dx)};
const forkUpdate=update;
update=function(dt){
  forkUpdate(dt);
  if(state!=='playing')return;
  const missed=new Set();obstacles.forEach(o=>{if(o.type==='fork'&&!o.cleared&&o.y>player.y+.16)missed.add(o.forkId)});
  missed.forEach(id=>{obstacles.filter(o=>o.type==='fork'&&o.forkId===id).forEach(o=>{o.cleared=true;o.hit=true});combo=1;comboTime=0;speed=Math.max(.72,speed-.12);toast('FORK MISSED','STAY ON THE FALL LINE')});
};
const forkDraw=drawObstacle;
drawObstacle=function(o){
  if(o.type!=='fork'){forkDraw(o);return}if(o.y<.4||o.hit)return;
  const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.22+t*.92;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);
  ctx.strokeStyle=o.color;ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(0,24);ctx.lineTo(0,-27);ctx.stroke();ctx.fillStyle=o.color;ctx.beginPath();ctx.moveTo(0,-31);ctx.lineTo(o.side*10,-18);ctx.lineTo(o.side*3,-18);ctx.lineTo(o.side*3,-7);ctx.lineTo(o.side*14,-7);ctx.lineTo(0,7);ctx.lineTo(-o.side*14,-7);ctx.lineTo(-o.side*3,-7);ctx.lineTo(-o.side*3,-18);ctx.lineTo(-o.side*10,-18);ctx.closePath();ctx.fill();ctx.fillStyle='rgba(7,27,39,.9)';ctx.fillRect(-34,-47,68,12);ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText(o.risk?'RISK LINE':'GROOMER',0,-38);ctx.restore();
};

function paintDropBriefing(){
  const route=ROUTES[routeIndex],run=RUNS[routeIndex]?.[runIndex];if(!route||!run)return;
  const resort=$('briefingResort'),line=$('briefingRun'),advice=$('briefingAdvice');
  if(resort)resort.textContent=`${route.name} · ${route.place}`;
  if(line)line.textContent=`${run.name.toUpperCase()} · ${run.difficulty} · ${run.length.toLocaleString()} ${route.elevationUnit==='M'?'M':'M'}`;
  if(advice){const adviceByTerrain={park:'Use the ramps and rails to build a trick combo.',groomer:'A smooth groomer—perfect for learning clean carves.',glades:'Keep your eyes up and leave room for the trees.',moguls:'Stay loose through the bumps and keep your knees soft.',steep:'Control your speed before the pitch gets serious.',alpine:'Watch the surface and commit to one clean edge.',spine:'Pick your line early—there is no easy reset.',bowl:'Carry speed across the open snow and link your turns.'};advice.textContent=adviceByTerrain[run.terrain]||'Pick a line, stay loose, and enjoy the ride.'}
}
const haptic=(pattern=8)=>{if(AlpinePro.settings.motion==='reduced'||!navigator.vibrate)return;try{navigator.vibrate(pattern)}catch{}};
const hapticLand=landTrick;landTrick=function(){const result=hapticLand();haptic(result?12:[18,24,32]);return result};
const hapticClear=clearObstacle;clearObstacle=function(o,dx){hapticClear(o,dx);haptic(o?.type==='rail'?[10,18]:7)};
const hapticCrash=crashPlayer;crashPlayer=function(reason){hapticCrash(reason);haptic([32,18,45])};

/* Colorado flavor pack: I-70 traffic, 14er goals, wildlife jokes, and local notes. */
const coloradoNotes=[
  ['BLUEBIRD OR BUST','Sunny at the trailhead does not mean sunny above treeline.','14ER READY'],
  ['POWDER DAY SICK','The group chat called in. Nobody is answering emails today.','FIRST TRACKS'],
  ['I-70 WILDCARD','Brake lights ahead. The mountain is moving faster than the highway.','BEAT TRAFFIC'],
  ['CORN SNOW SEASON','Soft turns, loud jackets, and one last run before lunch.','SPRING SLUSH']
];
function paintColoradoCard(){const title=$('coloradoTitle'),details=$('coloradoDetails'),badge=$('coloradoBadge');if(!title)return;const passNotes=[['LOVELAND PASS WIND','The pass is howling. Hold your edge and keep your goggles on.','DIVIDE WIND'],['BERTHOUD POWDER','Fresh snow, tight trees, and a local who says the stash is “secret.”','NORTHWEST POW'],['MONARCH BLUEBIRD','A long drive, a quiet lift, and the kind of sky Colorado brags about.','SOUTHWEST SKY'],['SAN JUAN SEND','Big views, steep lines, and a good reason to check the exit plan.','SAN JUAN LINE']];const note=routeIndex<5?coloradoNotes[(routeIndex+new Date().getDate())%coloradoNotes.length]:passNotes[(routeIndex-5)%passNotes.length];title.textContent=note[0];details.textContent=routeIndex>=5?`German alpine line selected · ${note[1]}`:note[1];badge.textContent=note[2]}
paintColoradoCard();
const coloradoSpawn=spawn;
spawn=function(){const before=obstacles.length;coloradoSpawn();if(state!=='playing'||AlpinePro.settings.mode!=='colorado')return;const sample=courseSample(Math.min(1,(distance+courseLength*.13)/courseLength)),x=Math.max(.12,Math.min(.88,sample.center+(Math.random()-.5)*sample.width*.72)),type=spawnCount%3===0?'chainZone':'traffic';obstacles.push({type,x,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:type==='traffic'?'#e34d3d':'#f0b84c',side:x<sample.center?-1:1,laneV:type==='traffic'?(Math.random()>.5?1:-1)*(.025+Math.random()*.025):0})};
const coloradoHit=hitObstacle;
hitObstacle=function(o,dx){if(o.type==='chainZone'){o.hit=true;speed=Math.max(.72,speed-.32);combo=1;toast('CHAIN-UP ZONE','COLORADO TRAFFIC TAX');beep(180,.16);return}if(o.type==='traffic'){coloradoHit(o,dx);return}coloradoHit(o,dx)};
const coloradoClear=clearObstacle;
clearObstacle=function(o,dx){if(o.type==='traffic'){const points=420*combo;scorePop(points,o.x*W,o.y*H,'BEAT THE TRAFFIC');toast('I-70 PASS','+ '+points);recordChallenge('clear');combo=Math.min(8,combo+1);bestCombo=Math.max(bestCombo,combo);return}coloradoClear(o,dx)};
const coloradoDraw=drawObstacle;
drawObstacle=function(o){if(!['traffic','chainZone'].includes(o.type)){coloradoDraw(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);if(o.type==='traffic'){ctx.fillStyle='#263947';ctx.fillRect(-30,2,60,23);ctx.fillStyle='#e34d3d';ctx.fillRect(-21,-7,42,12);ctx.fillStyle='#ffe38c';ctx.fillRect(-25,11,7,5);ctx.fillRect(18,11,7,5);ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('I-70',0,30)}else{ctx.fillStyle='#f0b84c';ctx.fillRect(-36,-4,72,8);ctx.fillStyle='#223542';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('CHAIN-UP ZONE',0,-12)}ctx.restore()};
const coloradoUpdate=update;
update=function(dt){coloradoUpdate(dt);if(state!=='playing')return;if(AlpinePro.settings.mode==='colorado'){ui.objectiveText.textContent='BEAT THE I-70 TRAFFIC';ui.objectiveProgress.textContent=`${clears} CLEAN PASSES`;obstacles.forEach(o=>{if(o.type!=='traffic')return;o.x+=o.laneV*dt*speed;if(o.x<.12||o.x>.88){o.x=Math.max(.12,Math.min(.88,o.x));o.laneV*=-1}})}};
const coloradoReset=reset;reset=function(){coloradoReset();if(AlpinePro.settings.mode==='colorado')toast('COLORADO DAY','WATCH FOR I-70 TRAFFIC AND CHAIN-UP ZONES')};

let plateFinds=0,pondSkims=0;
const coloradoCollectibleSpawn=spawn;
spawn=function(){const before=obstacles.length;coloradoCollectibleSpawn();if(state!=='playing')return;if(AlpinePro.settings.mode==='colorado'&&spawnCount%4===0){const s=courseSample(Math.min(1,(distance+courseLength*.14)/courseLength));obstacles.push({type:'plate',x:Math.max(.14,Math.min(.86,s.center+(Math.random()-.5)*s.width*.55)),y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#f5c95b',side:1,laneV:0})}if(AlpinePro.settings.mode==='pondskim'&&spawnCount%3===0){const s=courseSample(Math.min(1,(distance+courseLength*.12)/courseLength));obstacles.push({type:'pond',x:s.center,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#63d8ed',side:1,laneV:0})}};
const coloradoCollectibleHit=hitObstacle;
hitObstacle=function(o,dx){if(o.type==='plate'){if(o.hit)return;o.hit=true;o.cleared=true;plateFinds++;recordColoradoPlate();score+=850;recordChallenge('stash');toast('COUNTY PLATE FOUND',`${plateFinds} COLLECTED`);beep(1040,.16);return}if(o.type==='pond'){if(o.hit)return;if(player.jump>25){o.cleared=true;pondSkims++;score+=700*combo;combo=Math.min(8,combo+1);bestCombo=Math.max(bestCombo,combo);toast('POND SKIM','STAY DRY · +'+(700*combo));beep(760,.18);return}coloradoCollectibleHit(o,dx);return}coloradoCollectibleHit(o,dx)};
const coloradoCollectibleClear=clearObstacle;
clearObstacle=function(o,dx){if(o.type==='plate'){o.cleared=true;plateFinds++;recordColoradoPlate();score+=850;toast('COUNTY PLATE FOUND',`${plateFinds} COLLECTED`);beep(1040,.16);return}if(o.type==='pond'){o.cleared=true;pondSkims++;score+=700*combo;combo=Math.min(8,combo+1);bestCombo=Math.max(bestCombo,combo);toast('POND SKIM','CLEAN SPLASH · +'+(700*combo));return}coloradoCollectibleClear(o,dx)};
const coloradoCollectibleDraw=drawObstacle;
drawObstacle=function(o){if(!['plate','pond'].includes(o.type)){coloradoCollectibleDraw(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);if(o.type==='plate'){ctx.fillStyle='#f5c95b';ctx.fillRect(-27,-18,54,36);ctx.strokeStyle='#fff3b1';ctx.lineWidth=2;ctx.strokeRect(-27,-18,54,36);ctx.fillStyle='#233d4b';ctx.font='900 8px DM Sans';ctx.textAlign='center';ctx.fillText('COLORADO',0,-4);ctx.font='900 6px DM Sans';ctx.fillText('MILE HIGH',0,8)}else{ctx.fillStyle='rgba(63,207,234,.55)';ctx.beginPath();ctx.ellipse(0,12,42,18,0,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#d9fbff';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(-33,0);ctx.quadraticCurveTo(0,-20,33,0);ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('POND SKIM',0,-26)}ctx.restore()};
const coloradoCollectibleReset=reset;reset=function(){coloradoCollectibleReset();plateFinds=0;pondSkims=0};
const coloradoCollectibleUpdate=update;
update=function(dt){coloradoCollectibleUpdate(dt);if(state!=='playing')return;const mode=AlpinePro.settings.mode;if(mode==='pondskim'){ui.objectiveText.textContent='CLEAR THE POND SKIM';ui.objectiveProgress.textContent=`${pondSkims} / 3 SPLASHES`}if(mode==='colorado')ui.objectiveProgress.textContent=`${clears} PASSES · ${plateFinds} PLATES`};
const lodgeTrivia=[['What does “bluebird” mean?','A clear, sunny day after a storm.'],['What is a 14er?','A Colorado peak over 14,000 feet.'],['What is corn snow?','Soft, spring snow that turns creamy in the sun.'],['What does “one more run” usually mean?','At least three more runs.']];
$('lodgeTriviaButton')?.addEventListener('click',()=>{const item=lodgeTrivia[Math.floor(Math.random()*lodgeTrivia.length)];toast(`COLORADO TRIVIA · ${item[0]}`,item[1])});
const chileQuests=['Find the greenest chile in the lodge kitchen.','Trade one lodge credit for extra heat.','Ask the cook whether “mild” is actually mild.','Finish a run, then report directly to the burrito line.'];
$('lodgeChileButton')?.addEventListener('click',()=>{const quest=chileQuests[Math.floor(Math.random()*chileQuests.length)];toast('GREEN CHILE QUEST',quest);try{store.set('alpineRushChileQuest',new Date().toISOString().slice(0,10))}catch{}});

let parkingCones=0;
const parkingSpawn=spawn;
spawn=function(){const before=obstacles.length;parkingSpawn();if(state!=='playing'||AlpinePro.settings.mode!=='parking')return;const s=courseSample(Math.min(1,(distance+courseLength*.11)/courseLength));const type=spawnCount%3===0?'shuttle':'cone';obstacles.push({type,x:Math.max(.12,Math.min(.88,s.center+(Math.random()-.5)*s.width*.7)),y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:type==='shuttle'?'#2e7fa8':'#f28e3d',side:1,laneV:type==='shuttle'?(Math.random()>.5?1:-1)*.04:0})};
const parkingHit=hitObstacle;
hitObstacle=function(o,dx){if(o.type==='cone'){if(o.hit)return;o.hit=true;parkingCones++;toast('PARKING CONE','NICE SAVE · SHUTTLE LINE MOVING');beep(260,.1);return}if(o.type==='shuttle'){if(o.hit)return;o.hit=true;toast('SHUTTLE BLOCK','THE LOT IS FULL');parkingHit(o,dx);return}parkingHit(o,dx)};
const parkingClear=clearObstacle;
clearObstacle=function(o,dx){if(o.type==='cone'){o.cleared=true;parkingCones++;score+=240*combo;combo=Math.min(8,combo+1);toast('CONE CLEARED','LEGAL PARKING LINE');return}parkingClear(o,dx)};
const parkingDraw=drawObstacle;
drawObstacle=function(o){if(!['cone','shuttle'].includes(o.type)){parkingDraw(o);return}if(o.y<.4||o.hit)return;const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.9;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);if(o.type==='cone'){ctx.fillStyle='#f28e3d';ctx.beginPath();ctx.moveTo(0,-26);ctx.lineTo(-13,21);ctx.lineTo(13,21);ctx.closePath();ctx.fill();ctx.fillStyle='#fff';ctx.fillRect(-8,-2,16,5);ctx.fillRect(-11,10,22,5)}else{ctx.fillStyle='#2e7fa8';ctx.fillRect(-34,-18,68,38);ctx.fillStyle='#cceef4';ctx.fillRect(-24,-11,18,13);ctx.fillRect(6,-11,18,13);ctx.fillStyle='#fff';ctx.font='900 7px DM Sans';ctx.textAlign='center';ctx.fillText('MOUNTAIN SHUTTLE',0,29)}ctx.restore()};
const parkingReset=reset;reset=function(){parkingReset();parkingCones=0};
const parkingUpdate=update;update=function(dt){parkingUpdate(dt);if(state!=='playing'||AlpinePro.settings.mode!=='parking')return;ui.objectiveText.textContent='BEAT THE SHUTTLE RUSH';ui.objectiveProgress.textContent=`${parkingCones} LOT CLEARS`;obstacles.forEach(o=>{if(o.type!=='shuttle')return;o.x+=o.laneV*dt*speed;if(o.x<.12||o.x>.88){o.x=Math.max(.12,Math.min(.88,o.x));o.laneV*=-1}})};
function coloradoPassport(){try{return JSON.parse(localStorage.getItem('alpineRushColoradoPassport')||'{}')}catch{return{}}}
function paintColoradoPassport(){const p=coloradoPassport(),regions=[['regionFrontRange',0],['regionSummit',1],['regionNorth',2],['regionWest',3],['regionSanJuan',4]],stamps=Object.values(p).filter(Boolean).length;regions.forEach(([id,index])=>{const node=$(id);if(node)node.textContent=`${Number(p[index]||0)} / 2`});const details=$('coloradoPassportDetails');if(details)details.textContent=stamps>=10?'COLORADO PASSPORT COMPLETE · YOU EARNED YOUR LAST-CHAIR LEGEND TITLE':`${stamps} / 10 regional stamps · find county plates during Colorado Day runs`}
function recordColoradoPlate(){const p=coloradoPassport(),region=routeIndex<2?0:routeIndex<4?1:routeIndex===4?2:routeIndex===9?4:3;p[region]=Math.min(2,Number(p[region]||0)+1);try{localStorage.setItem('alpineRushColoradoPassport',JSON.stringify(p))}catch{}paintColoradoPassport()}
paintColoradoPassport();
const mountainRadio=['RADIO: I-70 is parked, but your line is moving.','PATROL: Bluebird conditions above treeline. Keep your crew together.','LODGE RADIO: The shuttle is full; the après fries are not.','WEATHER: Wind at the pass, sun in town, classic Colorado math.','LOCAL UPDATE: One more run has been approved by absolutely nobody.'];
$('lodgeRadioButton')?.addEventListener('click',()=>toast('MOUNTAIN RADIO',mountainRadio[Math.floor(Math.random()*mountainRadio.length)]));

/* Retention and presentation polish: make the next worthwhile action obvious. */
function paintProgression(){
  const goal=$('progressionGoal'),details=$('progressionDetails'),reward=$('progressionReward');if(!goal)return;
  const c=AlpinePro.career(),badges=AlpinePro.badges(),medals=AlpinePro.medals();
  if(!c.runs){goal.textContent='COMPLETE YOUR FIRST RUN';details.textContent='Finish a run to earn XP, a course medal, and your first badge.';reward.textContent='+XP · FIRST BADGE';return}
  if(c.level<3){goal.textContent=`REACH LEVEL 3 · ${Math.max(0,3-c.level)} LEVEL${3-c.level===1?'':'S'} TO GO`;details.textContent='Keep chaining clean clears and tricks to unlock stronger equipment.';reward.textContent='NEW SETUP UNLOCK';return}
  if(!badges['alpine-pass']){goal.textContent='DISCOVER THE ALPINE PASS';details.textContent='Ride a German mountain to add a new badge to your passport.';reward.textContent='MOUNTAIN BADGE';return}
  const routeMedals=Object.keys(medals).filter(k=>k.startsWith(`${routeIndex}:`)).length,total=RUNS[routeIndex]?.length||1;
  if(routeMedals<total){goal.textContent=`MASTER ${ROUTES[routeIndex].name}`;details.textContent=`Earn ${total-routeMedals} more course medal${total-routeMedals===1?'':'s'} on this mountain.`;reward.textContent='RESORT TITLE';return}
  goal.textContent='CHASE A PERSONAL BEST';details.textContent='Switch to Time Trial and race your ghost on a mastered line.';reward.textContent='NEW RECORD';
}
paintProgression();
const resultFinishForPolish=finish;
finish=function(){resultFinishForPolish();const nextRun=RUNS[routeIndex]?.[(runIndex+1)%Math.max(1,RUNS[routeIndex]?.length||1)],tip=$('nextRunTip');if(tip&&nextRun)tip.textContent=`NEXT UP · ${nextRun.name.toUpperCase()} · ${nextRun.difficulty} ${nextRun.terrain.toUpperCase()} · TRY A ${AlpinePro.settings.setup==='powder'?'FLOATING':'CLEAN'} LINE`;paintProgression()};
const originalEnterRoutesForPolish=enterRoutes;
enterRoutes=function(){originalEnterRoutesForPolish();paintProgression();if(store.get('alpineRushTutorialSeen','0')!=='1')toast('FIRST DROP','OPEN THE SKI & SNOWBOARD TUTORIAL OR TRY TRAINING RUN')};
if(ui.objectiveText)ui.objectiveText.setAttribute('aria-live','polite');

/* Career progression: every completed run contributes to a persistent season. */
let careerAwarded=false;
function paintCareer(c=AlpinePro.career()){
  const rank=$('careerRank'),runs=$('careerRuns'),label=$('careerXpLabel'),fill=$('careerXpFill');if(!rank)return;
  const progress=Number.isFinite(c.progress)?c.progress:(Number(c.xp||0)%1800);rank.textContent=`LEVEL ${c.level} · ${c.sponsor}`;runs.textContent=`${c.runs} RUN${c.runs===1?'':'S'} LOGGED`;label.textContent=`${progress} / 1800 XP`;if(fill)fill.style.transform=`scaleX(${Math.min(1,progress/1800)})`;
}
const careerReset=reset;reset=function(){careerReset();careerAwarded=false};
const careerFinish=finish;finish=function(){
  if(!careerAwarded){careerAwarded=true;const earned=AlpinePro.awardCareer(Math.floor(score),bestCombo);paintCareer(earned);const result=$('careerResult');if(result)result.textContent=earned.leveled?`CAREER LEVEL UP · LEVEL ${earned.level} · ${earned.sponsor}`:`CAREER XP +${Math.max(40,Math.round(score*.12)+Math.round(bestCombo*35))}`;if(earned.leveled)toast('CAREER LEVEL UP',`${earned.sponsor} SPONSOR TIER`)}
  careerFinish();
};
paintCareer();

/* Event-specific course dressing. */
const eventSpawn=spawn;
spawn=function(){
  const before=obstacles.length;eventSpawn();
  if(state!=='playing')return;
  const mode=AlpinePro.settings.mode;
  if(mode==='bigair'&&spawnCount%3===0)obstacles.push({type:'ramp',x:.22+Math.random()*.56,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#ff9d52',side:1,laneV:0});
  if(mode==='patrol'&&spawnCount%3===0){const type=Math.random()>.5?'speedPatrol':'skiPatrol';obstacles.push({type,x:.2+Math.random()*.6,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:type==='speedPatrol'?'#e84d3d':'#2e8f9f',side:1,laneV:(Math.random()>.5?1:-1)*(type==='speedPatrol'?.08:.025)});}
};
paintChallenge();
function loadRivalSeries(){try{return JSON.parse(localStorage.getItem('alpineRushRivalSeries')||'null')}catch{return null}}function saveRivalSeries(series){try{if(series)localStorage.setItem('alpineRushRivalSeries',JSON.stringify(series));else localStorage.removeItem('alpineRushRivalSeries')}catch{}}
const campaignFinish=finish;finish=function(){if(rivalSeries){const won=score>3500,series={...rivalSeries,wins:rivalSeries.wins+(won?1:0),losses:rivalSeries.losses+(won?0:1)};saveRivalSeries(series.wins>=2||series.losses>=2?null:series);const result=$('battleResult');if(result)result.textContent='CAMPAIGN ROUND '+(series.wins+series.losses)+' · '+series.rival+' '+(series.wins>=2?'CAMPAIGN WON':series.losses>=2?'CAMPAIGN LOST':series.wins+'-'+series.losses);rivalSeries=null}campaignFinish()};$('rivalRematchButton')?.addEventListener('click',()=>{const rival=rivalNames[(routeIndex+runIndex)%rivalNames.length],saved=loadRivalSeries();rivalSeries=saved&&saved.rival===rival?saved:{rival,wins:0,losses:0};const round=rivalSeries.wins+rivalSeries.losses+1;toast(round===3?'CAMPAIGN FINALE':'BEST OF THREE',round===3?rival+' · ALL RIVALS WANT THE LAST WORD':rival+' · ROUND '+round+' OF 3')});
function paintGear(){const label=$('gearWearText');if(!label)return;const condition=gearWear>=70?'NEEDS REPAIR':gearWear>=35?'SCUFFED':'PRISTINE';label.textContent=`GEAR CONDITION · ${condition} · ${gearWear}% WEAR`;label.style.color=gearWear>=70?'#ff8066':gearWear>=35?'#ffd260':''}paintGear();$('lodgeRepairButton')?.addEventListener('click',()=>{gearWear=0;store.set('alpineRushGearWear','0');paintGear()});
const rivalShowdowns={MAYA:{mode:'timetrial',condition:'groomed',run:0,objective:'Beat MAYA’s clean-line time'},KAI:{mode:'bigair',condition:'powder',run:2,objective:'Beat KAI’s trick score'},LUCA:{mode:'downhill',condition:'ice',run:1,objective:'Outrun LUCA on the ice'},NOVA:{mode:'patrol',condition:'storm',run:0,objective:'Clear NOVA’s patrol gauntlet'}};function armRivalShowdown(){const rival=rivalNames[(routeIndex+runIndex)%rivalNames.length],event=rivalShowdowns[rival]||rivalShowdowns.MAYA;rivalShowdown={rival,...event};runIndex=Math.min(event.run,RUNS[routeIndex].length-1);$('runSelect').value=String(runIndex);if($('conditionSelect'))$('conditionSelect').value=event.condition;AlpinePro.setSetting('condition',event.condition);AlpinePro.setSetting('mode',event.mode);$('modeSelect').value=event.mode;updatePreview();toast('RIVAL SHOWDOWN',`${rival} · ${event.objective}`)}$('rivalRematchButton')?.addEventListener('click',armRivalShowdown);const showdownFinish=finish;finish=function(){showdownFinish();if(rivalShowdown){const result=$('battleResult');if(result)result.textContent=`SHOWDOWN · ${rivalShowdown.rival} · ${rivalShowdown.objective.toUpperCase()} · ${score>3500?'YOU TAKE THE MOUNTAIN':'RIVAL HOLDS THE LINE'}`;rivalShowdown=null}};
const rivalChapters=[{min:0,title:'FIRST ENCOUNTER',line:r=>`${r} is watching your line. Make the first pass count.`},{min:1,title:'LINE THIEF',line:r=>`${r} still talks about the line you stole. Rematch?`},{min:3,title:'PERSONAL',line:r=>`${r} has started taking your favorite shortcut. This is getting awkward.`},{min:6,title:'MOUNTAIN GRUDGE',line:r=>`${r} has a custom strategy for you. Bring your fastest setup.`},{min:10,title:'SUMMIT SHOWDOWN',line:r=>`${r} has declared a final showdown. The mountain is the tiebreaker.`}];function rivalryProfile(){let profile={};try{profile=JSON.parse(localStorage.getItem('alpineRushRivals')||'{}')}catch{}return profile}function rivalChapter(wins){return rivalChapters.reduce((chapter,item)=>wins>=item.min?item:chapter,rivalChapters[0])}function paintRival(){const name=$('rivalName'),story=$('rivalStory'),record=$('rivalRecord');if(!name)return;const profile=rivalryProfile(),rival=rivalNames[(routeIndex+runIndex)%rivalNames.length],wins=Number(profile[rival]||0),chapter=rivalChapter(wins);name.textContent=`${rival} · ${chapter.title}`;record.textContent=`${wins} OVERTAKE${wins===1?'':'S'}`;story.textContent=chapter.line(rival)}function recordRival(name){const profile=rivalryProfile(),before=Number(profile[name]||0),after=before+1;profile[name]=after;try{localStorage.setItem('alpineRushRivals',JSON.stringify(profile))}catch{}paintRival();const chapter=rivalChapter(after),previous=rivalChapter(before),result=$('battleResult');if(result)result.textContent=`RIVALRY UPDATE · ${name} REMEMBERED THAT OVERTAKE`;if(chapter.title!==previous.title)toast(chapter.title,`${name} · NEW CHAPTER UNLOCKED`)}paintRival();$('rivalRematchButton')?.addEventListener('click',()=>{const rival=rivalNames[(routeIndex+runIndex)%rivalNames.length],chapter=rivalChapter(Number(rivalryProfile()[rival]||0));toast('RIVAL REMATCH',`${rival} · ${chapter.title}`);$('modeSelect').value='downhill';AlpinePro.setSetting('mode','downhill');updatePreview()});
function refreshSelectionUi(){updateTrailStatus(routeIndex);syncTrailAccess();updateMountainEvent();paintReputation();paintLegendary();paintSeason();paintPartner();paintColoradoCard();paintColoradoPassport();const weeklyStatus=$('weeklyBoardStatus');if(weeklyStatus)weeklyStatus.textContent=`${AlpinePro.weeklyModifier().name} · ${(snowSurface()||'GROOMED').toUpperCase()} · ${AlpinePro.settings.mode.toUpperCase()}`}
function startSelectionRefresh(){if(selectionRefreshTimer)return;selectionRefreshTimer=setInterval(()=>{if(state==='select')refreshSelectionUi()},2000)}
function stopSelectionRefresh(){if(selectionRefreshTimer){clearInterval(selectionRefreshTimer);selectionRefreshTimer=null}}
const challengeUpdate=update;
update=function(dt){challengeUpdate(dt);if(state==='playing'){paintChallenge();paintGear();if(gearWear>0)speed=Math.max(.72,speed-dt*gearWear*.0007)}};
const showdownHazards={MAYA:['gate','gateMate'],KAI:['ramp','rail'],LUCA:['patch','patch'],NOVA:['speedPatrol','skiPatrol']};const showdownSpawn=spawn;spawn=function(){showdownSpawn();if(state!=='playing'||!rivalShowdown||spawnCount%3!==0)return;const finale=rivalSeries&&rivalSeries.wins+rivalSeries.losses>=2,choices=finale?['gate','ramp','rail','patch','speedPatrol','skiPatrol']:(showdownHazards[rivalShowdown.rival]||['gate']),type=choices[Math.floor(Math.random()*choices.length)],sample=courseSample(Math.min(1,(distance+courseLength*.14)/courseLength)),x=Math.max(.1,Math.min(.9,sample.center+(Math.random()-.5)*sample.width*.58));obstacles.push({type,x,y:.035,wobble:Math.random()*6.28,hit:false,cleared:false,color:type==='ramp'?'#ff9d52':type==='patch'?'#69e1f5':type.includes('Patrol')?'#e84d3d':'#ff5f40',side:Math.random()>.5?1:-1,laneV:type==='speedPatrol'?.07:type==='skiPatrol'?.025:0})};
const funnyBeginCountdown=beginCountdown;beginCountdown=function(){funnyBeginCountdown();setTimeout(()=>{if(state==='countdown')toast('LIFT ANNOUNCER',snowHumor.lift[Math.floor(Math.random()*snowHumor.lift.length)])},180)};
let humorTimer=12;const humorReset=reset;reset=function(){humorReset();humorTimer=12};const humorUpdate=update;update=function(dt){humorUpdate(dt);if(state!=='playing')return;humorTimer-=dt;if(humorTimer<=0){humorTimer=16+Math.random()*13;const line=snowHumor.run[Math.floor(Math.random()*snowHumor.run.length)];toast('MOUNTAIN ANNOUNCER',line);window.AlpineRushReplay?.mark('ANNOUNCER: '+line)}};
const funnyHitObstacle=hitObstacle;hitObstacle=function(o,dx){if(['speedPatrol','skiPatrol'].includes(o.type)){toast('PATROL RADIO',snowHumor.patrol[Math.floor(Math.random()*snowHumor.patrol.length)])}funnyHitObstacle(o,dx)};
const funnyWeatherToast=toast;toast=function(label,value=''){const weatherLabels=['WIND GUST','FLAT LIGHT','ICE PATCH','POWDER BURST'];if(weatherLabels.includes(label)&&Math.random()<.42)value=`${value} · ${snowHumor.weather[Math.floor(Math.random()*snowHumor.weather.length)]}`;funnyWeatherToast(label,value)};
function patrolProfile(){let profile={actions:0,rescues:0,clears:0};try{profile={...profile,...JSON.parse(localStorage.getItem('alpineRushPatrol')||'{}')}}catch{}return profile}
function paintPatrol(profile=patrolProfile()){const rank=$('patrolRank'),stats=$('patrolStats'),next=$('patrolNext');if(!rank)return;const ranks=[['ROOKIE PATROLLER',0],['TRAIL GUIDE',3],['LEAD PATROLLER',10],['MOUNTAIN MARSHAL',25],['PATROL LEGEND',50]],current=ranks.reduce((chosen,item)=>profile.actions>=item[1]?item:chosen,ranks[0]),upcoming=ranks.find(item=>profile.actions<item[1]);rank.textContent=current[0];stats.textContent=`${profile.rescues} rescues · ${profile.clears} hazards cleared`;next.textContent=upcoming?`NEXT RANK · ${upcoming[1]-profile.actions} PATROL ACTIONS`:'TOP RANK · MOUNTAIN TRUSTED'}
paintPatrol();let patrolRunAwarded=false;const patrolCareerReset=reset;reset=function(){patrolCareerReset();patrolRunAwarded=false};const patrolCareerFinish=finish;finish=function(){if(!patrolRunAwarded){patrolRunAwarded=true;const profile=patrolProfile(),actions=patrolClears+rescueFinds;profile.actions+=actions;profile.clears+=patrolClears;profile.rescues+=rescueFinds;try{localStorage.setItem('alpineRushPatrol',JSON.stringify(profile))}catch{}const before=patrolProfile(),result=$('patrolResult');paintPatrol(profile);if(result)result.textContent=actions?`PATROL CAREER · +${actions} ACTION${actions===1?'':'S'} · ${patrolProfile().actions} TOTAL`:'PATROL CAREER · NO ACTIONS THIS RUN';if(actions&&Math.floor(profile.actions/10)>Math.floor((profile.actions-actions)/10))toast('PATROL RANK UP','THE MOUNTAIN TRUSTS YOU')}patrolCareerFinish()};
const lodgeGossip=['MAYA says your last line was “bold.” LUCA says that means terrifying.','KAI claims the powder stash was his idea. NOVA has witnesses.','The lodge bartender says snowboarders tip in wax. Skiers tip in lift tickets.','Patrol wants it known: the warning sign was not a suggestion.'];function lodgeCredits(){return Number(store.get('alpineRushLodgeCredits','0'))||0}function paintApres(){const label=$('apresCredits');if(label)label.textContent=`${lodgeCredits()} LODGE CREDIT${lodgeCredits()===1?'':'S'}`}paintApres();$('lodgeGossipButton')?.addEventListener('click',()=>toast('LODGE GOSSIP',lodgeGossip[Math.floor(Math.random()*lodgeGossip.length)]));$('lodgeJukeboxButton')?.addEventListener('click',()=>{$('settingsDialog')?.showModal();toast('JUKEBOX','PICK THE SOUNDTRACK FOR YOUR NEXT LAP')});$('lodgeRepairButton')?.addEventListener('click',()=>{const credits=lodgeCredits();if(credits<5){toast('GEAR SHOP','NEED 5 LODGE CREDITS');return}store.set('alpineRushLodgeCredits',credits-5);paintApres();toast('GEAR SHOP','EDGES WAXED · POP TUNED · READY TO SEND')});let lodgeRunAwarded=false;const lodgeReset=reset;reset=function(){lodgeReset();lodgeRunAwarded=false};const lodgeFinish=finish;finish=function(){if(!lodgeRunAwarded){lodgeRunAwarded=true;const earned=Math.max(1,Math.floor(score/1800));store.set('alpineRushLodgeCredits',lodgeCredits()+earned);paintApres();const result=$('highlightText');if(result&&earned>1)result.textContent+=` · LODGE +${earned}`}lodgeFinish()};
let liftGameActive=false,liftGameTaps=0,liftGameSeconds=0,liftGameTimer=null;function endLiftGame(success){clearInterval(liftGameTimer);liftGameTimer=null;liftGameActive=false;const button=$('lodgeLiftButton'),status=$('liftLineStatus');if(success){store.set('alpineRushLodgeCredits',lodgeCredits()+3);paintApres();toast('LIFT LINE CHAMP','+3 LODGE CREDITS');if(status)status.textContent='Chair unlocked. Zero poles lost.'}else{toast('LIFT LINE WIPEOUT','You dropped the balance bar.');if(status)status.textContent='The liftie saw everything. Try again.'}if(button){button.textContent='LIFT LINE GAME';button.disabled=false}}$('lodgeLiftButton')?.addEventListener('click',()=>{const button=$('lodgeLiftButton'),status=$('liftLineStatus');if(!liftGameActive){liftGameActive=true;liftGameTaps=0;liftGameSeconds=6;button.textContent='TAP BALANCE · 6';if(status)status.textContent='Tap six times before the chair arrives.';liftGameTimer=setInterval(()=>{liftGameSeconds--;if(button)button.textContent=`TAP BALANCE · ${liftGameSeconds}`;if(liftGameSeconds<=0)endLiftGame(false)},1000);return}liftGameTaps++;button.textContent=`TAP BALANCE · ${Math.max(0,6-liftGameTaps)}`;if(liftGameTaps>=6)endLiftGame(true)});
/* Rival campaign interstitial: a tiny comic-book beat between rounds. */
const rivalSceneLines={MAYA:{win:'MAYA checks the split time twice. “Fine. One clean line does not make a champion.”',loss:'MAYA points at the gate you missed. “The mountain keeps receipts.”'},KAI:{win:'KAI high-fives the air. “That trick was sick. Now do it again with less panic.”',loss:'KAI lands the victory grab and laughs. “Style points are not participation trophies.”'},LUCA:{win:'LUCA taps the ice with a pole. “You won the sprint. I still own the scary bit.”',loss:'LUCA disappears into the spray. “Try keeping up when the piste gets honest.”'},NOVA:{win:'NOVA radios patrol. “Rider located, ego upgraded. One more run for the title.”',loss:'NOVA blows the whistle. “That was a yard sale, not a strategy.”'}};
function closeRivalCutscene(){const panel=$('rivalCutscene');if(panel)panel.classList.add('hidden')}
function showRivalCutscene(campaign,round,won,final){const panel=$('rivalCutscene'),title=$('rivalCutsceneTitle'),line=$('rivalCutsceneLine'),record=$('rivalCutsceneRecord'),kicker=$('rivalCutsceneKicker'),button=$('rivalCutsceneButton');if(!panel||!title||!line||!record)return;const rival=campaign.rival,dialogue=rivalSceneLines[rival]||rivalSceneLines.MAYA;kicker.textContent=final?'RIVAL CAMPAIGN · CHAMPIONSHIP':`RIVAL CAMPAIGN · ROUND ${round}`;title.textContent=final?(won?'MOUNTAIN CHAMPION':'THE RIVAL TAKES IT'):(won?'LINE ADVANTAGE':'RIVAL HOLDS THE LINE');line.textContent=final?(won?`${rival} concedes the mountain. The trophy is yours — until the next storm.`:`${rival} keeps the crown. The rematch board is already lighting up.`):(won?dialogue.win:dialogue.loss);record.textContent=`${rival} · ${campaign.wins+(won?1:0)}–${campaign.losses+(won?0:1)} · ${final?'CAMPAIGN COMPLETE':'BEST OF THREE'}`;if(button){button.innerHTML=final?'<b>BACK TO RESULTS</b><span>✓</span>':'<b>CONTINUE CAMPAIGN</b><span>↘</span>';button.onclick=closeRivalCutscene}panel.classList.remove('hidden');button?.focus()}
const campaignSceneFinish=finish;finish=function(){const campaign=rivalSeries?{...rivalSeries}:null;campaignSceneFinish();if(!campaign)return;const won=score>3500,round=campaign.wins+campaign.losses+1,wins=campaign.wins+(won?1:0),losses=campaign.losses+(won?0:1);showRivalCutscene(campaign,round,won,wins>=2||losses>=2)};
/* Final lifecycle guard: every run can award results exactly once. */
let finishLock=false;
const auditedFinish=finish;
finish=function(){if(finishLock||state==='over')return;finishLock=true;auditedFinish();const mode=AlpinePro.settings.mode,result=$('battleResult');if(result&&mode==='colorado')result.textContent=`COLORADO DAY · ${plateFinds} COUNTY PLATE${plateFinds===1?'':'S'} · ${clears} TRAFFIC PASS${clears===1?'':'ES'}`;if(result&&mode==='pondskim')result.textContent=`POND SKIM · ${pondSkims} CLEAN SPLASH${pondSkims===1?'':'ES'} · SPRING FESTIVAL COMPLETE`;if(result&&mode==='parking')result.textContent=`PARKING LOT SURVIVOR · ${parkingCones} LOT CLEAR${parkingCones===1?'':'S'} · SHUTTLE RUSH BEATEN`};
const auditedReset=reset;
reset=function(){finishLock=false;auditedReset()};
/* Long-term resort reputation, rotating legendary objectives, and seasonal flavor. */
const legendaryChallenges=[{name:'NO-SLIDE SUNDAY',details:'Finish the line without losing a life.',test:()=>lives===3},{name:'SEND IT CLEAN',details:'Land eight tricks in one run.',test:()=>trickCount>=8},{name:'LOCAL ROCKET',details:'Break 5,000 points before the finish.',test:()=>score>=5000},{name:'PATROL HERO',details:'Clear four hazards or rescues.',test:()=>patrolClears+rescueFinds>=4},{name:'SECRET LINE',details:'Find a hidden mountain stash.',test:()=>stashFinds>0}];
const seasonEvents=[['POWDER FESTIVAL','Fresh lines pay double reputation. Powder setups earn +25% bonus.'],['SPRING SLUSH CUP','Fast landings are worth extra points. Keep the wax warm.'],['SUMMER MAINTENANCE','The lifts are quiet, but legendary challenges award double rewards.'],['AUTUMN NIGHT LAPS','Night conditions are live. Lights, layers, and loud music recommended.']];
function legendaryToday(){const day=Math.floor(Date.now()/86400000);return legendaryChallenges[(day+routeIndex)%legendaryChallenges.length]}
function paintLegendary(){const challenge=legendaryToday(),name=$('legendaryName'),details=$('legendaryDetails'),date=$('legendaryDate');if(!name)return;name.textContent=challenge.name;details.textContent=challenge.details;date.textContent=new Date().toLocaleDateString(undefined,{month:'short',day:'numeric'}).toUpperCase()}
function reputationProfile(){let profile={};try{profile=JSON.parse(localStorage.getItem('alpineRushResortRep')||'{}')}catch{}return profile}
function paintReputation(){const name=$('reputationName'),details=$('reputationDetails'),points=$('reputationPoints');if(!name)return;const profile=reputationProfile(),rep=Number(profile[routeIndex]||0),ranks=[['LOCAL ROOKIE',0],['LIFTLINE REGULAR',500],['MOUNTAIN LOCAL',1500],['TRAIL LEGEND',3500]],current=ranks.reduce((pick,item)=>rep>=item[1]?item:pick,ranks[0]),next=ranks.find(item=>rep<item[1]);name.textContent=current[0];points.textContent=`${rep.toLocaleString()} REP`;details.textContent=next?`${next[1]-rep} rep to ${next[0].toLowerCase()} · unlocks local perks`:'All local perks unlocked · the lifties know your name'}
function paintSeason(){const month=new Date().getMonth(),event=seasonEvents[month>=11||month<=1?0:month<=3?1:month<=8?2:3],name=$('seasonName'),details=$('seasonDetails');if(name)name.textContent=event[0];if(details)details.textContent=event[1]}
paintLegendary();paintReputation();paintSeason();
const seasonalFinish=finish;finish=function(){const challenge=legendaryToday(),completed=challenge.test(),profile=reputationProfile(),baseRep=Number(profile[routeIndex]||0),month=new Date().getMonth(),seasonBonus=month>=4&&month<=8?completed?2:1:1,reward=completed?2000*seasonBonus:0;if(reward)score+=reward;const repGain=Math.max(10,Math.round(score/300))*seasonBonus;profile[routeIndex]=baseRep+repGain;try{localStorage.setItem('alpineRushResortRep',JSON.stringify(profile))}catch{}seasonalFinish();paintReputation();paintLegendary();if(completed){toast('LEGENDARY CHALLENGE',`${challenge.name} · +${reward.toLocaleString()}`);const result=$('battleResult');if(result)result.textContent=`LEGENDARY CHALLENGE · ${challenge.name} · +${reward.toLocaleString()} PTS`}const season=$('seasonDetails');if(season&&repGain>0)season.textContent+=` · +${repGain} REP THIS RUN`};
const resortPartners=[
 {name:'ECHO MOUNTAIN',message:'Night laps, terrain-park energy, and a short drive from Denver.',safety:'Check lighting and patrol updates before a night lap.',run:'HOLLYWOOD · TERRAIN PARK',url:'https://echomtn.com/'},
 {name:'ELDORA',message:'Front Range glades and progressive terrain for every kind of rider.',safety:'Stay in control through the trees and respect closed signs.',run:'CORONA · GLADES',url:'https://www.eldora.com/'},
 {name:'LOVELAND',message:'High-alpine bowls, deep snow, and an unmistakable Continental Divide skyline.',safety:'Altitude changes quickly · hydrate and pace your first run.',run:"CAT'S MEOW · DIVIDE LINE",url:'https://skiloveland.com/'},
 {name:'ARAPAHOE BASIN',message:'Steeps, exposure, and big-mountain lines above treeline.',safety:'Know your ability and check avalanche conditions before entering steep terrain.',run:'THE SPINE · HIGH ALPINE',url:'https://www.arapahoebasin.com/'},
 {name:'STEAMBOAT',message:'Tree runs, champagne powder, and legendary western hospitality.',safety:'Powder can hide hazards · ride with a buddy and regroup often.',run:'VAGABOND · TREE LINE',url:'https://www.steamboat.com/'},
 {name:'HAUSBERG',message:'Classic Garmisch pistes with Kandahar race energy.',safety:'Follow piste markers and respect German mountain signage.',run:'KANDAHAR · CLASSIC PISTE',url:'https://zugspitze.de/'},
 {name:'ZUGSPITZE',message:'Glacier views and high-alpine terrain at Germany’s highest summit.',safety:'Weather changes fast above treeline · check the mountain report.',run:'GLACIER RUN · SUMMIT',url:'https://zugspitze.de/'},
 {name:'ALPSPITZE',message:'Technical faces and Wetterstein views for riders who want a challenge.',safety:'Steep terrain demands an honest ability check and a safe exit plan.',run:'NORDWAND CHUTE · TECHNICAL',url:'https://zugspitze.de/'},
 {name:'HOEDOWN HILL',message:'Family laps, park features, and a welcoming place to learn your next trick.',safety:'Park smart: inspect features, take turns, and land in control.',run:'CLUNK YARD · PARK SESSION',url:'https://www.windsorgov.com/'},
 {name:'PURGATORY',message:'Southwest Colorado powder, glades, and big views above Durango.',safety:'Tree terrain is shared terrain · keep speed controlled and look uphill.',run:'PURGATORY PLUNGE · POWDER LINE',url:'https://www.purgatory.com/'}
];
function paintPartner(){const partner=resortPartners[routeIndex]||resortPartners[0],name=$('partnerName'),message=$('partnerMessage'),safety=$('partnerSafety'),challenge=$('partnerChallenge'),link=$('partnerLink');if(!name)return;name.textContent=partner.name;message.textContent=partner.message;safety.textContent=partner.safety;challenge.textContent=`FEATURED LINE · ${partner.run}`;if(link){link.href=partner.url;link.setAttribute('aria-label',`Explore ${partner.name} resort information`)}}
paintPartner();
const tutorialDialog=$('tutorialDialog');$('tutorialStartButton')?.addEventListener('click',()=>{store.set('alpineRushTutorialSeen','1');AlpinePro.setSetting('tutorial',false)});$('tutorialSkipButton')?.addEventListener('click',()=>{store.set('alpineRushTutorialSeen','1');AlpinePro.setSetting('tutorial',false);tutorialDialog?.close()});
$('learnBackButton')?.addEventListener('click',enterRoutes);$('learnTrainingButton')?.addEventListener('click',()=>{hide(ui.learn);show(ui.routes);const mode=$('modeSelect');if(mode){mode.value='training';AlpinePro.setSetting('mode','training');updatePreview()}toast('TRAINING READY','DROP IN WHEN YOU FEEL READY')});
const onboardingBegin=beginCountdown;beginCountdown=function(){if(state==='select'&&AlpinePro.settings.tutorial&&store.get('alpineRushTutorialSeen','0')!=='1'){tutorialDialog?.showModal();return}onboardingBegin()};
const difficultySelect=$('difficultySelect');if(difficultySelect){difficultySelect.value=AlpinePro.settings.difficulty||'rider';difficultySelect.addEventListener('change',e=>{AlpinePro.setSetting('difficulty',e.target.value);toast('DIFFICULTY',e.target.options[e.target.selectedIndex].text.toUpperCase())})}
function paintRouteStats(){document.querySelectorAll('.route-card').forEach((card,index)=>{const route=ROUTES[index],label=card.querySelector('b');if(!route||!label)return;label.textContent=formatElevation(route)})}paintRouteStats();
const trainingCrash=crashPlayer;crashPlayer=function(reason){if(AlpinePro.settings.mode!=='training'){trainingCrash(reason);return}const hearts=lives,wear=gearWear;trainingCrash(reason);lives=hearts;gearWear=wear;store.set('alpineRushGearWear',String(wear));toast('TRAINING MODE','NO PENALTY · TRY THE LINE AGAIN')};
const coachFinish=finish;finish=function(){coachFinish();let recap=$('coachRecap');if(!recap){recap=document.createElement('p');recap.id='coachRecap';recap.className='coach-recap';$('highlightText')?.after(recap)}const mode=AlpinePro.settings.mode||'freeride',difficulty=AlpinePro.settings.difficulty||'rider',advice=mode==='training'?'TRAINING COMPLETE · Try the same line faster or add a trick.':bestCombo>=5?'EXCELLENT FLOW · Keep linking carves before your next jump.':trickCount===0?'COACH NOTE · Add one clean jump to build combo and score.':clears<3?'COACH NOTE · Use jumps and side-to-side carving to clear more hazards.':'SOLID RUN · Replay the timeline to find your next time-saving line.';recap.textContent=`${difficulty.toUpperCase()} · ${advice}`};
function challengeCode(){const raw=`${routeIndex}|${runIndex}|${Math.floor(score)}|${bestCombo}|${trickCount}`;return`AR-${btoa(raw).replace(/[^A-Z0-9]/gi,'').slice(0,10).toUpperCase()}`}
$('shareResultButton')?.addEventListener('click',async()=>{const text=`ALPINE RUSH · ${ROUTES[routeIndex].name} / ${RUNS[routeIndex][runIndex].name} · ${Math.floor(score).toLocaleString()} PTS · BEAT MY LINE: ${challengeCode()}`;try{if(navigator.share)await navigator.share({title:'Beat my Alpine Rush line',text});else await navigator.clipboard.writeText(text);toast('CHALLENGE READY',challengeCode())}catch{} });
let trainingStep=0;const trainingReset=reset;reset=function(){trainingReset();trainingStep=0};const trainingGuideUpdate=update;update=function(dt){trainingGuideUpdate(dt);if(state!=='playing'||AlpinePro.settings.mode!=='training')return;const steps=[.16,.4,.66];if(trainingStep<steps.length&&distance/courseLength>=steps[trainingStep]){const messages=['CARVE NOW · HOLD ← OR → TO FEEL THE EDGE','NEXT FEATURE · JUMP WITH SPACE','FINAL LESSON · BOOST AND LINK YOUR EXIT'];toast(`TRAINING ${trainingStep+1}/3`,messages[trainingStep]);window.AlpineRushReplay?.mark(`TRAINING: ${messages[trainingStep]}`);trainingStep++}};
let lastSurfaceSeen='';const tactileReset=reset;reset=function(){tactileReset();lastSurfaceSeen=''};const tactileUpdate=update;update=function(dt){tactileUpdate(dt);if(state!=='playing')return;const surface=snowSurface();if(surface!==lastSurfaceSeen){if(lastSurfaceSeen){const feedback={GROOMED:'CORDUROY · BUILD SPEED',POWDER:'POWDER · SOFT EDGES','PACKED ICE':'ICE PATCH · EDGE GRIP',HARDPACK:'HARDPACK · STAY CENTERED',MOGULS:'MOGULS · UNLOAD YOUR KNEES','SPRING SLUSH':'SLUSH · KEEP IT MOVING','NIGHT GROOM':'NIGHT GROOM · LIGHTS ON',WHITEOUT:'WHITEOUT · FIND THE FALL LINE'}[surface]||surface;toast('SURFACE CHANGE',feedback);beep(surface==='POWDER'?310:surface==='PACKED ICE'?170:520,.08)}lastSurfaceSeen=surface}};
const difficultyUpdate=update;update=function(dt){difficultyUpdate(dt);if(state!=='playing')return;const level=AlpinePro.settings.difficulty||'rider',rules={beginner:{score:.9,density:.12},rider:{score:1,density:0},pro:{score:1.1,density:-.07},legend:{score:1.22,density:-.13}}[level]||{score:1,density:0};score+=dt*8*speed*(rules.score-1);spawnTimer+=dt*rules.density;if(level==='beginner'&&lives===1&&Math.random()<dt*.12){lives=2;toast('ASSIST MODE','ONE EXTRA HEART · KEEP RIDING')}};
function drawPurgatoryMountain(r,bank,h){const sky=ctx.createLinearGradient(0,0,0,h);sky.addColorStop(0,'#234c68');sky.addColorStop(.58,'#d78f72');sky.addColorStop(1,'#f1d2aa');ctx.fillStyle=sky;ctx.fillRect(0,0,W,H);const sun=ctx.createRadialGradient(W*.76,H*.14,0,W*.76,H*.14,W*.3);sun.addColorStop(0,'rgba(255,235,181,.8)');sun.addColorStop(1,'rgba(255,235,181,0)');ctx.fillStyle=sun;ctx.fillRect(0,0,W,H*.55);ctx.save();ctx.translate(bank*.12,0);
  ctx.fillStyle='#31535b';ctx.beginPath();ctx.moveTo(-W*.08,h);ctx.lineTo(W*.06,H*.28);ctx.lineTo(W*.18,H*.34);ctx.lineTo(W*.31,H*.16);ctx.lineTo(W*.43,H*.3);ctx.lineTo(W*.57,H*.12);ctx.lineTo(W*.71,H*.29);ctx.lineTo(W*.88,H*.2);ctx.lineTo(W*1.08,h);ctx.closePath();ctx.fill();
  ctx.fillStyle='#edf7f5';ctx.beginPath();ctx.moveTo(W*.06,H*.28);ctx.lineTo(W*.13,H*.42);ctx.lineTo(W*.2,H*.35);ctx.lineTo(W*.31,H*.16);ctx.lineTo(W*.36,H*.35);ctx.lineTo(W*.43,H*.3);ctx.lineTo(W*.57,H*.12);ctx.lineTo(W*.63,H*.33);ctx.lineTo(W*.71,H*.29);ctx.lineTo(W*.79,H*.36);ctx.lineTo(W*.88,H*.2);ctx.lineTo(W*.94,H*.46);ctx.lineTo(W*.08,H*.49);ctx.closePath();ctx.fill();
  ctx.fillStyle='#244638';for(let row=0;row<7;row++){const y=H*(.43+row*.065),spread=W*(.25+row*.09);for(let i=0;i<9;i++){const x=W*.5-spread/2+i*spread/8+(row%2?8:-6);ctx.beginPath();ctx.moveTo(x,y-25-row*2);ctx.lineTo(x-14-row*2,y+8);ctx.lineTo(x+14+row*2,y+8);ctx.closePath();ctx.fill()}}
  ctx.fillStyle='#eaf7f8';ctx.beginPath();ctx.moveTo(W*.43,H);ctx.quadraticCurveTo(W*.47,H*.68,W*.5,H*.49);ctx.quadraticCurveTo(W*.55,H*.68,W*.63,H);ctx.closePath();ctx.fill();ctx.fillStyle='rgba(93,151,166,.34)';ctx.beginPath();ctx.moveTo(W*.485,H);ctx.quadraticCurveTo(W*.5,H*.68,W*.52,H*.5);ctx.quadraticCurveTo(W*.56,H*.7,W*.6,H);ctx.closePath();ctx.fill();
  ctx.strokeStyle='rgba(26,48,55,.76)';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(W*.18,H*.35);ctx.lineTo(W*.83,H*.35);ctx.stroke();for(const x of [W*.24,W*.42,W*.62,W*.8]){ctx.beginPath();ctx.moveTo(x,H*.35);ctx.lineTo(x,H*.64);ctx.stroke();ctx.fillStyle='#273d43';ctx.fillRect(x-13,H*.34,26,5)}ctx.fillStyle='rgba(5,25,32,.82)';roundedPath(20,H*.43,186,43,7);ctx.fill();ctx.fillStyle='#ffd260';ctx.font='900 12px DM Sans';ctx.textAlign='left';ctx.fillText('PURGATORY · SAN JUAN',34,H*.458);ctx.fillStyle='#d6e8eb';ctx.font='700 8px DM Sans';ctx.fillText('POWDER BOWL  ·  DURANGO, COLORADO',34,H*.482);ctx.restore()}
/* Final lifecycle guards: delayed callbacks must never end a newer run. */
const lifecycleReset=reset;reset=function(){clearTimeout(finishTimer);finishTimer=null;runToken++;lifecycleReset()};
const lifecycleFinish=finish;finish=function(){if(finishLock||!['playing','finishing'].includes(state))return;lifecycleFinish()};
/* Cancel superseded weather requests and enforce a short network deadline. */
const nativeFetch=window.fetch.bind(window);window.fetch=(input,init={})=>{if(String(input).includes('open-meteo.com')){snowReportController?.abort();snowReportController=new AbortController();const timeout=setTimeout(()=>snowReportController?.abort(),7000);return nativeFetch(input,{...init,signal:snowReportController.signal}).finally(()=>clearTimeout(timeout))}return nativeFetch(input,init)};
/* Sanitize persisted settings before the first interactive run. */
const settingChoices={mode:['freeride','downhill','timetrial','slalom','park','bigair','patrol','colorado','pondskim','parking'],setup:['balanced','race','powder','freestyle'],quality:['high','balanced','low'],motion:['full','reduced'],contrast:['normal','high'],units:['metric','imperial'],music:['random','copperhead','highway','tiroler','roadhouse','roadkill','ecstasy']};for(const [name,choices] of Object.entries(settingChoices)){if(!choices.includes(AlpinePro.settings[name]))AlpinePro.setSetting(name,AlpinePro.defaults?.[name]||choices[0])}if(!Number.isFinite(Number(AlpinePro.settings.volume)))AlpinePro.setSetting('volume',.8);
const validSetup=['racer','powder','freestyle'];if(!validSetup.includes(AlpinePro.settings.setup))AlpinePro.setSetting('setup','racer');if(AlpinePro.settings.mode==='training')AlpinePro.setSetting('mode','freeride');
/* Keep the resort picker focused; optional status, Colorado flavor, and progression live on a details screen. */
const mountainDetailsScreen=$('mountainDetailsScreen'),mountainDetailsPanels=$('mountainDetailsPanels');if(mountainDetailsScreen&&mountainDetailsPanels){const optionalPanels=['daily-cup','quick-music','challenge-card','snowReport','partnerCard','trailStatus','mountainEvent','coloradoCard','coloradoPassport','career-card','progression-card','contract-card','patrol-card','apres-card','badge-card','mastery-card','reputation-card','rival-card','legendary-card','season-card','modifier-card','weekly-board'];optionalPanels.forEach(className=>{const panel=document.querySelector(`.${className}`);if(panel)mountainDetailsPanels.append(panel)});const detailsButton=document.createElement('button');detailsButton.type='button';detailsButton.className='text-button mountain-details-link';detailsButton.textContent='MOUNTAIN DETAILS';detailsButton.onclick=()=>{hide(ui.routes);show(mountainDetailsScreen);$('detailsTitle').textContent=ROUTES[routeIndex].name;refreshSelectionUi()};ui.routes.querySelector('.menu-heading')?.append(detailsButton);$('detailsBackButton')?.addEventListener('click',()=>{hide(mountainDetailsScreen);show(ui.routes);refreshSelectionUi()})}
/* High-contrast piste boundary overlay: the playable snow corridor reads clearly on desktop. */
function drawPisteBoundaries(){const run=RUNS[routeIndex][runIndex],slices=[];for(let i=0;i<=24;i++){const t=i/24,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.025+t*(.52+s.width*.14)),t})}ctx.save();ctx.lineCap='round';for(const side of [-1,1]){ctx.strokeStyle='rgba(5,27,37,.72)';ctx.lineWidth=11;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke();ctx.strokeStyle=side<0?'rgba(255,104,70,.95)':'rgba(91,221,239,.95)';ctx.lineWidth=4;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke()}for(const row of [4,8,12,16,20,24]){const s=slices[row],poleH=12+s.t*34;for(const side of [-1,1]){const x=s.x+side*(s.half+10+s.t*12);ctx.strokeStyle='rgba(13,38,48,.9)';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x,s.y+2);ctx.lineTo(x,s.y-poleH);ctx.stroke();ctx.fillStyle=side<0?'#ff6546':'#6be4f2';ctx.fillRect(x-3,s.y-poleH,6,8)}}ctx.restore()}
const mountainWithPisteBoundaries=mountain;mountain=function(){mountainWithPisteBoundaries();if(['playing','countdown'].includes(state))drawPisteBoundaries()};
/* Replace the broad painted track with a natural piste corridor that matches collision width. */
function drawNaturalPiste(){const slices=[];for(let i=0;i<=24;i++){const t=i/24,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.018+t*(s.width*1.12)),t})}ctx.save();const outside=ctx.createLinearGradient(0,H*.4,0,H);outside.addColorStop(0,'rgba(39,72,83,.86)');outside.addColorStop(.55,'rgba(24,56,67,.96)');outside.addColorStop(1,'rgba(10,31,40,.98)');ctx.fillStyle=outside;ctx.fillRect(0,H*.42,W,H*.62);const snow=ctx.createLinearGradient(0,H*.42,0,H);snow.addColorStop(0,'#e9f5f7');snow.addColorStop(.48,'#d9ebef');snow.addColorStop(1,'#bdd8df');ctx.fillStyle=snow;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half,s.y):ctx.moveTo(s.x-s.half,s.y));for(let i=slices.length-1;i>=0;i--){const s=slices[i];ctx.lineTo(s.x+s.half,s.y)}ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(255,255,255,.26)';ctx.lineWidth=7;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half*.96,s.y):ctx.moveTo(s.x-s.half*.96,s.y));slices.slice().reverse().forEach(s=>ctx.lineTo(s.x+s.half*.96,s.y));ctx.stroke();ctx.strokeStyle='rgba(83,128,141,.16)';ctx.lineWidth=2;for(let lane=-2;lane<=2;lane++){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+s.half*lane/5,s.y):ctx.moveTo(s.x+s.half*lane/5,s.y));ctx.stroke()}for(let row=5;row<23;row+=3){const s=slices[row];ctx.fillStyle=`rgba(255,255,255,${.08+s.t*.08})`;ctx.beginPath();ctx.ellipse(s.x-s.half*.72,s.y,12+s.t*24,2+s.t*4,-.08,0,Math.PI*2);ctx.ellipse(s.x+s.half*.62,s.y+3,10+s.t*20,2+s.t*3,.08,0,Math.PI*2);ctx.fill()}if(['glades','aspen'].includes(RUNS[routeIndex][runIndex].terrain)){for(let row=4;row<22;row+=4){const s=slices[row],size=16+s.t*30;pine(s.x-s.half-size*.32,s.y+size*.2,size,.8);pine(s.x+s.half+size*.32,s.y+size*.2,size,.8)}}ctx.restore();return slices}
function drawNaturalBoundaries(){const slices=[];for(let i=0;i<=24;i++){const t=i/24,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.018+t*(s.width*1.12)),t})}ctx.save();ctx.lineCap='round';for(const side of [-1,1]){ctx.strokeStyle='rgba(4,25,34,.92)';ctx.lineWidth=10;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke();ctx.strokeStyle=side<0?'#ef674d':'#5bd7df';ctx.lineWidth=3;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke()}for(const row of [4,8,12,16,20,24]){const s=slices[row],poleH=12+s.t*34;for(const side of [-1,1]){const x=s.x+side*(s.half+8+s.t*8);ctx.strokeStyle='#173642';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x,s.y+2);ctx.lineTo(x,s.y-poleH);ctx.stroke();ctx.fillStyle=side<0?'#ff684c':'#65dce8';ctx.fillRect(x-3,s.y-poleH,6,8)}}ctx.restore()}
const oldPistePresentation=mountain;mountain=function(){oldPistePresentation();if(['playing','countdown'].includes(state)){drawNaturalPiste();drawNaturalBoundaries()}};
/* Final piste tuning: widen the playable fall line and keep the mountain visible around it. */
const baseCourseSample=courseSample;courseSample=function(progress){const sample=baseCourseSample(progress);return{...sample,width:Math.min(.48,sample.width*1.34)}};
drawNaturalPiste=function(){const slices=[];for(let i=0;i<=24;i++){const t=i/24,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.02+t*(s.width*.9)),t})}ctx.save();const snow=ctx.createLinearGradient(0,H*.42,0,H);snow.addColorStop(0,'rgba(246,252,253,.32)');snow.addColorStop(.5,'rgba(226,241,245,.58)');snow.addColorStop(1,'rgba(198,222,229,.68)');ctx.fillStyle=snow;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half,s.y):ctx.moveTo(s.x-s.half,s.y));for(let i=slices.length-1;i>=0;i--){const s=slices[i];ctx.lineTo(s.x+s.half,s.y)}ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(255,255,255,.22)';ctx.lineWidth=9;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x-s.half*.98,s.y):ctx.moveTo(s.x-s.half*.98,s.y));slices.slice().reverse().forEach(s=>ctx.lineTo(s.x+s.half*.98,s.y));ctx.stroke();ctx.strokeStyle='rgba(92,138,150,.11)';ctx.lineWidth=1.5;for(let lane=-2;lane<=2;lane++){ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+s.half*lane/6,s.y):ctx.moveTo(s.x+s.half*lane/6,s.y));ctx.stroke()}for(let row=5;row<23;row+=3){const s=slices[row];ctx.fillStyle=`rgba(255,255,255,${.05+s.t*.07})`;ctx.beginPath();ctx.ellipse(s.x-s.half*.72,s.y,12+s.t*24,2+s.t*4,-.08,0,Math.PI*2);ctx.ellipse(s.x+s.half*.62,s.y+3,10+s.t*20,2+s.t*3,.08,0,Math.PI*2);ctx.fill()}ctx.restore()};
drawNaturalBoundaries=function(){const slices=[];for(let i=0;i<=24;i++){const t=i/24,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.02+t*(s.width*.9)),t})}ctx.save();ctx.lineCap='round';for(const side of [-1,1]){ctx.strokeStyle='rgba(21,61,70,.46)';ctx.lineWidth=6;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke();ctx.strokeStyle=side<0?'rgba(239,117,82,.78)':'rgba(101,207,216,.78)';ctx.lineWidth=2;ctx.beginPath();slices.forEach((s,i)=>i?ctx.lineTo(s.x+side*s.half,s.y):ctx.moveTo(s.x+side*s.half,s.y));ctx.stroke()}for(const row of [5,10,15,20]){const s=slices[row],poleH=10+s.t*28;for(const side of [-1,1]){const x=s.x+side*(s.half+7+s.t*6);ctx.strokeStyle='rgba(23,54,63,.68)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(x,s.y+1);ctx.lineTo(x,s.y-poleH);ctx.stroke();ctx.fillStyle=side<0?'rgba(255,108,78,.88)':'rgba(106,216,224,.88)';ctx.fillRect(x-2,s.y-poleH,4,6)}}ctx.restore()};
/* Bypass the legacy full-width track renderer so the mountain background stays intact. */
mountain=function(){renderTerrain();if(['playing','countdown'].includes(state)){drawNaturalPiste();drawNaturalBoundaries();drawBaseArea()}};
/* Persistent resort identity keeps the run presentation grounded in a real mountain and line. */
const hudIdentity=document.createElement('div');hudIdentity.className='run-identity';hudIdentity.innerHTML='<small></small><strong></strong>';ui.hud.append(hudIdentity);function paintHudIdentity(){hudIdentity.querySelector('small').textContent=ROUTES[routeIndex].name;hudIdentity.querySelector('strong').textContent=RUNS[routeIndex][runIndex].name.toUpperCase()}const baseHudUpdate=updateUI;updateUI=function(){baseHudUpdate();paintHudIdentity()};paintHudIdentity();
/* Carry the selected resort’s visual identity through gameplay and the result poster. */
function syncResortTheme(){const image=resortImages[routeIndex];document.documentElement.dataset.resort=String(routeIndex);const results=$('resultsScreen');if(results&&image?.src)results.style.setProperty('--resort-image',`url("${image.src}")`)}
const themeUpdatePreview=updatePreview;updatePreview=function(){themeUpdatePreview();syncResortTheme()};syncResortTheme();
/* Desktop presentation: remove the painted corridor and use only restrained piste markers. */
function drawDesktopPisteMarkers(){const slices=[];for(let i=0;i<=18;i++){const t=i/18,ahead=(1-t)*courseLength*.22,s=courseSample((distance+ahead)/courseLength),camera=t*t;slices.push({x:(s.center*(1-camera)+.5*camera)*W,y:H*(.42+t*.62),half:W*(.02+t*(s.width*.9)),t})}ctx.save();for(const row of [5,9,13,17]){const s=slices[row],poleH=10+s.t*25;for(const side of [-1,1]){const x=s.x+side*(s.half+8+s.t*6);ctx.strokeStyle='rgba(21,55,64,.62)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(x,s.y);ctx.lineTo(x,s.y-poleH);ctx.stroke();ctx.fillStyle=side<0?'rgba(239,117,82,.82)':'rgba(101,207,216,.82)';ctx.fillRect(x-2,s.y-poleH,4,6)}}ctx.restore()}
mountain=function(){renderTerrain();if(['playing','countdown'].includes(state)){if(mobileDevice){drawNaturalPiste();drawNaturalBoundaries()}else drawDesktopPisteMarkers()}drawBaseArea()};
/* Parallax mountain motion: distant ridges drift slowly while foreground terrain advances faster. */
function drawScrollingMountain(){const travel=Math.max(0,sceneryOffset),progress=travel%1;ctx.save();for(let layer=0;layer<3;layer++){const speed=[.12,.28,.52][layer],offset=(travel*speed*H*.18)%(H*.34),base=H*(.25+layer*.13)+offset;ctx.fillStyle=layer===0?'rgba(39,72,94,.16)':layer===1?'rgba(26,59,72,.2)':'rgba(13,40,51,.25)';ctx.beginPath();ctx.moveTo(-W*.08,H);ctx.lineTo(-W*.08,base+H*.1);for(let i=0;i<=8;i++){const x=W*(i/8),peak=base-(i%3)*H*(.035+layer*.012)-Math.sin(i*2.7+progress*6)*H*.012;ctx.lineTo(x,peak)}ctx.lineTo(W*1.08,H);ctx.closePath();ctx.fill()}ctx.globalAlpha=.18;ctx.strokeStyle='#f6fdff';ctx.lineWidth=1.5;for(let i=0;i<12;i++){const x=(i*W/11+travel*W*.025)%W,y=H*(.48+((i*29+travel*16)%170)/1000);ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+W*.045,y+H*.08);ctx.stroke()}ctx.restore()}
mountain=function(){renderTerrain();if(['playing','countdown'].includes(state)){drawScrollingMountain();if(mobileDevice){drawNaturalPiste();drawNaturalBoundaries()}else drawDesktopPisteMarkers()}drawBaseArea()};
/* Shared piste traffic and terrain bumps: riders travel with the mountain, not toward the camera. */
const downhillSpawn=spawn;spawn=function(){downhillSpawn();if(state!=='playing')return;const sample=courseSample(Math.min(1,(distance+courseLength*.18)/courseLength));if(spawnCount%5===0){obstacles.push({type:'pisteRider',x:Math.max(.12,Math.min(.88,sample.center+(Math.random()-.5)*sample.width*.72)),y:.06+Math.random()*.08,wobble:Math.random()*6.28,hit:false,cleared:false,color:Math.random()>.5?'#5ec8dc':'#f28b55',side:Math.random()>.5?1:-1,laneV:(Math.random()>.5?1:-1)*(.012+Math.random()*.018),pace:.9+Math.random()*.16})}if(spawnCount%4===0&&['groomer','moguls','steep','bowl'].includes(RUNS[routeIndex][runIndex].terrain)){obstacles.push({type:'pisteBump',x:Math.max(.12,Math.min(.88,sample.center+(Math.random()-.5)*sample.width*.55)),y:.045,wobble:Math.random()*6.28,hit:false,cleared:false,color:'#d7edf0',side:1,laneV:0})}};
const downhillObstacleDraw=drawObstacle;drawObstacle=function(o){if(o.type==='pisteRider'){const t=Math.max(0,Math.min(1,(o.y-.4)/.48)),scale=.2+t*.95;ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(scale,scale);drawSkier(0,0,Math.sin(o.wobble+o.y*8)*.18,o.color,false);ctx.restore();return}if(o.type==='pisteBump'){const t=Math.max(0,Math.min(1,(o.y-.4)/.48));ctx.save();ctx.translate(o.x*W,o.y*H);ctx.scale(.45+t*.75,.45+t*.75);ctx.fillStyle='rgba(46,91,103,.22)';ctx.beginPath();ctx.ellipse(0,14,30,7,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='#d9eef1';ctx.beginPath();ctx.ellipse(0,0,25,12,0,0,Math.PI*2);ctx.fill();ctx.fillStyle='rgba(255,255,255,.65)';ctx.beginPath();ctx.ellipse(-7,-4,12,5,-.2,0,Math.PI*2);ctx.fill();ctx.restore();return}downhillObstacleDraw(o)};
const downhillHit=hitObstacle;hitObstacle=function(o,dx){if(o.type==='pisteBump'){if(o.hit)return;o.hit=true;Object.assign(player,{jumpV:430+speed*30,jump:.08,compression:.95,spin:0,flip:0});jumps++;scorePop(120*combo,o.x*W,o.y*H-18,'MOGUL POP');burst(o.x*W,o.y*H,'#effcff',10);beep(420,.1);return}downhillHit(o,dx)};
const sharedPisteUpdate=update;update=function(dt){sharedPisteUpdate(dt);if(state!=='playing')return;obstacles.forEach(o=>{if(o.type!=='pisteRider')return;o.y-=dt*(.22+o.pace*.05)*speed;o.x+=o.laneV*dt*speed;if(o.x<.12||o.x>.88){o.x=Math.max(.12,Math.min(.88,o.x));o.laneV*=-1}o.wobble+=dt*2});};
/* Forward chase camera: the rider advances down the visible piste instead of staying pinned in one spot. */
const forwardReset=reset;reset=function(){forwardReset();player.y=.61};const forwardUpdate=update;update=function(dt){forwardUpdate(dt);if(state!=='playing')return;const progress=Math.min(1,Math.max(0,distance/courseLength)),targetY=.61+progress*.18;player.y+=(targetY-player.y)*Math.min(1,dt*3.8);cameraY+=((progress*.9)-cameraY)*dt*1.8};
/* World-space piste riders: z is downhill distance, x is lane position, and projection follows the piste curve. */
const worldTraffic=[];const worldTrafficReset=reset;reset=function(){worldTrafficReset();worldTraffic.length=0};const worldTrafficSpawn=spawn;spawn=function(){worldTrafficSpawn();if(state!=='playing'||spawnCount%4!==0)return;const z=260+Math.random()*260;worldTraffic.push({z,lane:.5+(Math.random()-.5)*.5,speed:.72+Math.random()*.32,laneSpeed:(Math.random()>.5?1:-1)*(.012+Math.random()*.012),color:Math.random()>.5?'#5ec8dc':'#f28b55',tilt:0})};
const worldTrafficUpdate=update;update=function(dt){worldTrafficUpdate(dt);if(state!=='playing')return;for(let i=worldTraffic.length-1;i>=0;i--){const rider=worldTraffic[i],relative=Math.max(.18,speed-rider.speed*.72);rider.z-=relative*dt*48;rider.lane+=rider.laneSpeed*dt;if(rider.lane<.14||rider.lane>.86){rider.lane=Math.max(.14,Math.min(.86,rider.lane));rider.laneSpeed*=-1}const sample=courseSample(Math.min(1,(distance+rider.z)/courseLength)),x=sample.center+(rider.lane-.5)*sample.width*.72;if(rider.z<48&&Math.abs(x-player.x)<.065&&player.jump<38){crashPlayer('RIDER MERGE');worldTraffic.splice(i,1);continue}if(rider.z< -35||rider.z>700)worldTraffic.splice(i,1)}};
const worldTrafficDraw=draw;draw=function(){worldTrafficDraw();if(!['playing','countdown'].includes(state))return;const visible=worldTraffic.slice().sort((a,b)=>b.z-a.z);for(const rider of visible){if(rider.z<0||rider.z>620)continue;const depth=1-rider.z/620,sample=courseSample(Math.min(1,(distance+rider.z)/courseLength)),x=sample.center+(rider.lane-.5)*sample.width*.72,y=H*(.4+depth*.58),scale=.12+depth*.96;ctx.save();ctx.translate(x*W,y);ctx.scale(scale,scale);drawSkier(0,0,rider.laneSpeed*3,rider.color,false);ctx.restore()}};
/* Grounded rider pass: stronger downhill stance, edge contact, and anchored snow interaction. */
const groundedRiderDraw=drawPlayer;drawPlayer=function(){groundedRiderDraw();if(player.jump!==0||state!=='playing')return;const x=player.x*W,y=player.y*H+38,edge=Math.max(-1,Math.min(1,player.edge)),lean=Math.abs(edge);ctx.save();ctx.globalAlpha=.2+.12*lean;ctx.strokeStyle='#244e5c';ctx.lineWidth=2;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(x-7-edge*8,y);ctx.lineTo(x-16-edge*18,y+18);ctx.moveTo(x+7-edge*8,y);ctx.lineTo(x+16-edge*18,y+18);ctx.stroke();ctx.globalAlpha=.1+.14*lean;ctx.strokeStyle='#f4fcfd';ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(x-9-edge*8,y+2);ctx.lineTo(x-18-edge*18,y+20);ctx.moveTo(x+9-edge*8,y+2);ctx.lineTo(x+18-edge*18,y+20);ctx.stroke();if(lean>.35){ctx.globalAlpha=.35;ctx.fillStyle='#fff';for(let i=0;i<3;i++){ctx.beginPath();ctx.arc(x-Math.sign(edge)*(16+i*8)+Math.random()*8,y+8+i*5,1.5+i*.4,0,Math.PI*2);ctx.fill()}}ctx.restore()};
const stanceRiderDraw=drawRider;drawRider=function(){ctx.translate(0,-11);ctx.rotate(.09+player.vx*.035);stanceRiderDraw()};
